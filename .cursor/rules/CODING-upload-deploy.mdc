---
description: CODING 上傳 GitHub
globs: upload.bat,managementconsole.html
alwaysApply: false
---

# CODING 上傳 GitHub

- 使用者要**備份＋commit＋push 到 `main`**（上傳／部署／PUSH／同步 GitHub）→ 在 CODING 根目錄執行 **`upload.bat`**（可加 `NONINTERACTIVE=1`）。
- 路徑：倉庫根 **`upload.bat`**；本機常為 `C:\Users\a9999\Dropbox\CodeBackups\CODING\upload.bat`。
- **不必**跑：僅 `git status`／log／diff 且不要 push；或使用者指定其他流程。
