---
description: CODING 上傳 GitHub
globs: upload.bat,managementconsole.html
alwaysApply: false
---

# CODING 上傳 GitHub

- 部署流程 → **`deploy-runbook`** skill（SPEC → LOG → 備份 → 部署；有問題暫停請示）
- 上線指令 → 倉庫根 **`upload.bat`**（可加 `NONINTERACTIVE=1`）
- **不必**跑：僅 `git status`／diff 且不要 push
