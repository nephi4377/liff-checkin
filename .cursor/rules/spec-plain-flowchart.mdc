---
description: SPEC 流程圖用白話，圖下附程式對照表
globs: SPEC/**
alwaysApply: false
---

# SPEC 流程圖：白話優先

人類記不住每個函式在做什麼；**圖給人看，表給程式對照**。

## 流程圖裡（禁止）

- 函式名、類別名、API action、檔名、錯誤碼、JSON 欄位名
- 用「誰 → 做什麼 → 結果」白話寫節點

## 流程圖下方（必填）

緊接圖後加 **「程式對照表」**：

| 白話（圖上寫的） | 程式對照（開發用） |
|------------------|-------------------|
| 向後端問今天有沒有打卡 | `apiBridge.getWorkInfo` |

- 圖上每個節點都要有一行
- 術語可查 `SPEC/專有名詞白話對照.md`；沒有就補一行

## 好／壞

**❌ 圖內**：`directCheckin` → `handleWorkInfoUpdate` → Firebase

**✅ 圖內**：按打卡 → 先顯示已打卡 → 推播提醒

**✅ 圖下**：對照表列出上列三步對應的程式名與檔案

## 維護

改程式後先更新對照表；白話流程沒變就不必重畫整張圖。
