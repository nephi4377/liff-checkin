---
description: 多工具共同開發交接規則
alwaysApply: true
---

# 共用開發紀錄

- 開始任何修改前，先執行 `git pull --ff-only origin main`，並完整閱讀根目錄 `SHARED_DEV_LOG.md`。
- 檢查「目前進行中」是否有人正在修改相同檔案或功能；有重疊時不要覆蓋，先向使用者說明。
- 超過 10 分鐘、跨多檔或高衝突風險的工作，開始時先在「目前進行中」登記並推送。
- 完成後更新 `SHARED_DEV_LOG.md`：清除自己的進行中項目，在「最近完成」最上方記錄修改範圍、結果、驗證、commit 與待處理事項。
- 不使用強制推送、`git reset --hard` 或會覆蓋他人尚未整合工作的操作。
