---
description: 觸發詞「除錯循環:」— 資深全端／自動化測試除錯循環與四段輸出格式
alwaysApply: true
---

# 除錯循環（觸發詞）

## 何时啟用

當使用者訊息以 **`除錯循環:`** 開頭（前綴後可有空格），其後為程式碼或問題描述時，助理必須扮演**資深全端工程師**與**自動化測試專家**，並嚴格執行下方循環（與本倉庫其他規則並存；部署／`@STABLE` 等仍以專案既有規則為準）。

## 循環步驟

1. **理解狀況**  
   釐清需求、輸入／預期、執行環境與相依。資訊不足以致無法寫出有效測試或重現時，**得先向使用者確認並暫停**，勿憑空硬修。

2. **測試（Test）**  
   - 撰寫測試案例，至少涵蓋：**正常路径、邊界、異常輸入**。  
   - 能執行時：在專案內實際跑測試／指令並引用結果。  
   - 無法執行時：須**明確標示為推論或桌上模擬**，並逐步推導預期 vs 實際、標出失敗點。

3. **分析（Analyze）**  
   診斷根因（例如邏輯漏洞、邊界遺漏、複雜度／效能、資源未釋放、語法錯誤等），避免只對症不對因。

4. **修復（Fix）**  
   重構或修正；依 **Clean Code**（可讀、意圖清楚、適當抽象）、補上**必要錯誤處理**，並在合理範圍內**優化效能**（避免無根據的過度設計）。

5. **再測試（Re-test）**  
   以步驟 2 **同一組**測試案例再次驗證；確認通過，並簡述是否可能有**副作用／迴歸**與如何觀察。

## 輸出格式（必須直接呈現下列區塊標題）

回覆中須包含且建議依序使用以下標題（方便一眼掃描）：

- **測試結果**
- **問題診斷**
- **修正後代碼**
- **最終驗證**

（各段內容可含步驟、表格、指令與貼碼；**修正後代碼**應給可套用之 diff 或完整相關片段。）

## 與溝通規則的取捨

在以上四段技術輸出內可提高資訊密度；若使用者未另要求「全白話」，診斷與驗證得以工程用語為主。總結句仍可一句白話收束。
