---
description: Agent 在 Windows 跑終端機時：避免 Set-Location 與 git 等指令串成一行；優先用工作目錄參數
alwaysApply: true
---

# 終端機指令（Windows / PowerShell）

## 必須遵守

- **不要**用 `Set-Location '…'; git …`、`cd … && git …` 這種「一條指令、分號或 `&&` 串很多段」。
- 若執行指令的工具支援 **工作目錄（working directory / cwd）**：**只**用該參數指定資料夾，指令列**僅寫**要跑的那段（例如單獨 `git status`），不要再在字串裡切目錄。
- 若工具**沒有**工作目錄參數：**拆成兩次（或以上）**終端呼叫——第一次只切目錄（例如僅 `Set-Location '…'`），第二次起才跑 `git`、`npm` 等。

## 理由（給助理看即可）

- 白名單以前綴比對；單獨一行 `git …` 才能穩定命中 `git`，也較容易自動執行、少打斷使用者。
