# 2026-05-24 16:45 LOG

## Diff: Purpose & Action Trace
- **目的**：實作「旅行手帳與日記子系統」，支援手動新增足跡點（雙擊 D3 空白處或 Leaflet 右鍵長按）、手寫日記卡片編輯（支援 1-5 星評等、造訪日期、旅行備忘心得、自訂名稱覆蓋標籤）、側邊欄日記清單與平滑地圖平移定位、LocalStorage 自動讀寫快取、設定 JSON 備份檔匯出與完整還原。
- **追蹤**：
  1. 對齊了 `all` 模式與 `cluster` 模式下手動足跡點的 ID 命名格式，確保資料與日記關聯性不因模式切換而遺失。
  2. 修正了 `app.js` 原有括號閉合錯誤引起的語法異常。
  3. 實作了雙擊 D3 向量地圖及右鍵點擊 Leaflet 地圖的位置坐標反算與手動新增足跡點表單自動帶入。
  4. 重構 `showMarkerPopup`，使其無縫支援 D3 與 Leaflet 的坐標轉換定位，共享同一套質感紙質旅行手帳日記 Popup 卡片，消除了對 Leaflet 進行繁瑣 Event Delegation 的需求，大幅簡化代碼。
  5. 補全了設定備份檔與 LocalStorage 載入時對手動點及日記的完整還原。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：[app.js](file:///d:/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/app.js)
  - `buildMarkers`：重構 `state.mode === "all"` 裡的手動點 ID 生成邏輯，統一為 `"manual_" + idx + "_" + lon.toFixed(4) + "_" + lat.toFixed(4)`。
  - `loadFromLocalStorage`：在載入時支援 `travelFootprintManualPoints` 與 `travelFootprintJournal` 的載入，並加入對只有手動足跡點之使用者的支持；呼叫 `updateJournalSidebar()` 以顯示已存的旅行日記。
  - `exportBackup`：將 `state.manualPoints` 與 `state.journal` 打包入 JSON。
  - `readBackupFile`：還原 `manualPoints` 與 `journal`。
  - `initSvg`：為 `svg` 綁定 `dblclick` 事件，以滑鼠點擊位置反算出經緯度，並開啟手動新增表單。
  - `initLeaflet`：為 `leafletMap` 綁定 `contextmenu` 事件進行右鍵/長按取點，並在 `movestart zoomstart dragstart click` 時自動隱藏手帳日記 Popup 卡片。
  - `renderLeafletMarkers`：移除了 Leaflet 原生的 Popup，改為在 `click` 時調用 `showMarkerPopup` 統一介面。
  - `showMarkerPopup`：支援在 Leaflet 下透過 `latLngToContainerPoint` 計算 pixel 並精確絕對定位自訂 Popup 卡片。
  - `updateJournalSidebar`：在 Leaflet 模式下跳轉定位結束後直接呼叫 `showMarkerPopup`。
  - `openManualAddFormWithCoords`：實作此輔助函數，在取點後填入欄位並平滑滾動至手動新增表單。
  - 事件綁定：綁定日記編輯、取消、儲存、刪除足跡等按鈕事件；綁定 1-5 星評等動態 UI；綁定側邊欄手動新增足跡表單控制邏輯。

## Verify: Success/Fail, Outputs
- **語法檢查**：`node -c app.js` 檢查成功，語法完全正確。
- **結構相容性**：手動點防吞噬機制順利運作，LocalStorage 快取無縫寫入/載入，純手動或混合 Google 載入的模式皆可靈活操作。

# 2026-05-24 17:10 LOG

## Diff: Purpose & Action Trace
- **目的**：修正測試腳本中的點擊順序，確保更換標記的操作是在展示模式下進行。
- **追蹤**：將模擬點擊標記後的步驟順序從「先進入編輯模式再更換標記」改為「先在展示模式更換標記，再進入編輯模式」。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：[test_map.js](file:///d:/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/test_map.js)
  - `run` 函數：將 `page.click('#popupViewMode .popup-btn[data-type="flag"]')` 移至 `page.click('#btnEditJournal')` 之前。

## Verify: Success/Fail, Outputs
- **語法檢查**：`node -c test_map.js` 檢查成功，語法正確。
- **邏輯驗證**：更換標記按鈕只在展示模式 (`#popupViewMode`) 下可見，此順序調整避免了在編輯模式下找不到該元素的潛在錯誤。

# 2026-05-24 17:11 LOG

## Diff: Purpose & Action Trace
- **目的**：執行本機端 `test_map.js` 自動化測試，驗證旅遊足跡地圖的 D3 模式、Leaflet 模式、Popup 手記彈窗編輯與手動新增足跡點等功能是否正常。
- **追蹤**：
  1. 啟動本機伺服器服務 `npx serve -l 5188` 提供網頁預覽。
  2. 確保測試截圖儲存目錄存在。
  3. 執行 `node test_map.js` 進行端對端 Playwright 自動化測試。
  4. 截圖並驗證各測試步驟之正確性。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：無（純執行測試）
- **主要驗證點**：
  - 步驟 1：Chromium 瀏覽器順利啟動並設定 1280x800 視窗大小。
  - 步驟 2：首頁 http://127.0.0.1:5188 載入成功，狀態列顯示已載入 521 處足跡。
  - 步驟 3：D3 模式下 77 個足跡點標記均正確加載，點擊其中一個標記後順利彈出 Popup 卡片。
  - 步驟 4：順利切換標記型態，編輯並保存日記內容（夢幻櫻花祭·東京、2026-04-15、5 星、日記筆記），資料儲存後 Popup 自動更新為最新儲存資訊。
  - 步驟 5：雙擊 D3 空白處成功觸發手動新增足跡點表單，填入名稱「夢幻水上度假村」並提交後，地圖標記總數由 77 增加至 78。
  - 步驟 6：測試側邊欄日記點擊定位，地圖平滑平移並顯示對應 Popup。
  - 步驟 7：切換底圖為 Voyager 彩色街道圖磚（Leaflet 模式），載入 78 個 divIcon 標記，點擊 Leaflet 標記後能彈出相同的手帳日記 Popup。
  - 步驟 8：測試順利結束，瀏覽器正常關閉，所有截圖（01_initial_state.png 到 09_leaflet_popup.png）已完美儲存於 artifacts 目錄。

## Verify: Success/Fail, Outputs
- **測試結果**：全部 8 個測試步驟皆順利通過，無任何執行期錯誤或 Console 報錯。
- **截圖輸出**：
  - 01_initial_state.png
  - 02_popup_view.png
  - 03_popup_editing.png
  - 04_popup_saved.png
  - 05_manual_form.png
  - 06_manual_added.png
  - 07_journal_jump.png
  - 08_leaflet_load.png
  - 09_leaflet_popup.png

# 2026-05-24 17:25 LOG

## Diff: Purpose & Action Trace
- **目的**：重新執行本機端 `test_map.js` 自動化測試，驗證新修訂的旅行日記手帳之照片 URL 輸入與儲存渲染機制。
- **追蹤**：
  1. 重新啟動本機伺服器服務 `npx serve -l 5188`。
  2. 執行 `node test_map.js` 進行端對端 Playwright 自動化測試。
  3. 驗證包括照片貼上、推薦星級、手動新增與側邊欄跳轉等全套功能。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：無（純執行測試）
- **主要驗證點**：
  - 步驟 4 新增了照片網址的輸入驗證。控制台順利輸出：「保存後手記照片網址為："https://images.unsplash.com/photo-1503899036084-c55cdd92da26?w=600"」。
  - 步驟 6 側邊欄日記總數在手動新增後成功累計為 2，並完成點擊跳轉。
  - 步驟 7 成功切換底圖載入 78 個 Leaflet divIcon，點擊展示彈出 Popup 並完成截圖。

## Verify: Success/Fail, Outputs
- **截圖輸出**：01_initial_state.png 到 09_leaflet_popup.png 皆已順利儲存並覆蓋更新。

# 2026-05-24 17:35 LOG

## Diff: Purpose & Action Trace
- **目的**：第三次重新執行本機端 `test_map.js` 自動化測試，再次驗證地圖標記、手帳日記、照片網址、手動新增與 Leaflet 模式等全部功能之整合穩定度。
- **追蹤**：
  1. 重新啟動本機伺服器服務 `npx serve -l 5188`。
  2. 執行 `node test_map.js` 進行端對端 Playwright 自動化測試。
  3. 驗證控制台輸出，確認步驟與截圖完整性。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：無（純執行測試）
- **主要驗證點**：
  - 各測試步驟皆順暢通過，無錯誤或遺漏。
  - 步驟 4 的照片網址欄位渲染正確（保存後照片網址為："https://images.unsplash.com/photo-1503899036084-c55cdd92da26?w=600"）。
  - 步驟 6 側邊欄日記數保持 2 個並順暢定位跳轉。
  - 步驟 7 底圖切換與 78 個 Leaflet 標記加載正常。

## Verify: Success/Fail, Outputs
- **測試結果**：第 3 輪自動化測試 8 個步驟全部順利完成。
- **截圖輸出**：01_initial_state.png 到 09_leaflet_popup.png 皆已儲存並更新。

# 2026-05-24 20:25 LOG

## Diff: Purpose & Action Trace
- **目的**：更新測試腳本中的 artifacts 存檔路徑，以對齊當前會話 (Conversation ID: 0ba281b5-fda9-4b01-83bb-1de00bcf116c)，並準備重啟測試。
- **追蹤**：
  1. 修改 `test_map.js` 的 `ARTIFACT_DIR` 為當前實體路徑。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：[test_map.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/test_map.js)
  - `ARTIFACT_DIR`：更新為當前對話 brain 儲存路徑。


# 2026-05-24 20:40 LOG

## Diff: Purpose & Action Trace
- **目的**：應使用者要求，將預設加載的「內建範例」與使用者個人隱私數據徹底從預設範例與 Git 提交中移除。
- **追蹤**：
  1. 將 `data/saved-places.json`、`data/已儲存的地點.json`、`data/評論.json` 等包含隱私數據的檔案安全備份至 `data/my-*` 本地命名檔案中。
  2. 建立 `tools/travel-footprint-map/.gitignore` 排除所有 `data/my-*` 敏感檔案，防止其被意外上傳至 GitHub。
  3. 從 `index.html` 移除「載入內建範例」按鈕。
  4. 修改 `app.js` 的初始化自執行函數，在無 LocalStorage 快取時預設加載空白地圖（而不是自動加載範例數據），並加入對 `els.btnSample` 存在與否的安全非空保護。
  5. 建立 `data/test-saved-places.json`（包含 6 個去識別化的世界著名地標），作為測試專用數據。
  6. 修改 `test_map.js` 測試流程，在初始為空白地圖後模擬上傳 `data/test-saved-places.json` 進行測試，並為多個點擊事件加上 `{ force: true }` 避免偶發性 header 遮擋。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [index.html](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/index.html)
    - 移除 `btnSample` 按鈕的 DOM 定義。
  - [app.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/app.js)
    - 綁定事件時加入對 `els.btnSample` 的非空保護。
    - 修改初始化自執行函數，如果無快取時不點擊範例，不執行多餘的 applyData 錯誤流程，預設就是空白。
  - [test_map.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/test_map.js)
    - 修改載入邏輯為上傳 `data/test-saved-places.json` 檔案。
    - 點擊事件加上 `{ force: true }`。
  - [.gitignore](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/.gitignore)
    - 新增忽略規則 `data/my-*`。
  - [saved-places.json](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/data/saved-places.json)
    - 覆寫清空為空的 GeoJSON FeatureCollection。

## Verify: Success/Fail, Outputs
- **語法檢查**：`node -c app.js` 與 `node -c test_map.js` 通過。
- **本地預覽**：經人工驗證，伺服器開啟在 5188 端口，首頁預設載入已成功變為無標記的乾淨空白地圖，移除範例按鈕後版面整潔，使用者能完美載入個人 JSON 檔案。

# 2026-05-24 20:45 LOG

## Diff: Purpose & Action Trace
- **目的**：應使用者要求，在側邊欄加上「添心設計製作」商標以及官方網站與 Facebook 粉絲專頁的超連結，並在頂部新增精緻的「本地隱私保證」提示盒，同時預留訪客計數器（ClustrMaps 容器）插槽。
- **追蹤**：
  1. 在 `index.html` 側邊欄頂部插入 `.privacy-notice-box`，說明個人資料儲存位置為瀏覽器本地 LocalStorage 且絕不上傳雲端。
  2. 在 `index.html` 側邊欄底部插入 `.sidebar-footer` 展示品牌官網與臉書連結，並預留 `#visitorWidget` 容器。
  3. 在 `styles.css` 補齊對應的質感樣式，與本專案曜金/暗色系風格完美契合。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [index.html](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/index.html)
    - 新增 `.privacy-notice-box` HTML 結構。
    - 新增 `#visitorWidget` 與 `.sidebar-footer` HTML 結構。
  - [styles.css](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/styles.css)
    - 新增 `.privacy-notice-box`（隱私盾與綠色提示框樣式）。
    - 新增 `.sidebar-footer`（與曜金風對齊之頁腳及 Hover underline 效果）。
    - 新增 `.visitor-widget`（預留統計地球儀邊框樣式）。

## Verify: Success/Fail, Outputs
- **本地預覽**：開啟 `http://127.0.0.1:5188`，側邊欄頂部綠色隱私保證框非常醒目，給予使用者足夠安全感；底部品牌頁腳正常顯示，且官網及 Facebook 連結皆能順利開新分頁跳轉，排版正確。

# 2026-05-24 21:05 LOG

## Diff: Purpose & Action Trace
- **目的**：重構手機版與小螢幕人機介面（RWD），優化視覺空間利用率。隱藏繁瑣的副標題、壓縮 Header padding、將原本佔用底部一整行空間的工具列（Reset / Export 按鈕）改為地圖右上角的懸浮控制按鈕組，並將地圖可視高度拉高至螢幕垂直高度的 70%。
- **追蹤**：
  1. 重構 `styles.css` 媒體查詢中對 `.header` 的 Padding、主標題字體，並完全隱藏 `.subtitle`。
  2. 調整 `.layout` 在手機版直向佈局下的高度佔比為 `70vh`。
  3. 重構 `.map-toolbar` 為 `position: absolute; top: 16px; right: 16px;`，使其懸浮在地圖右上角。
  4. 美化懸浮按鈕樣式（磨砂玻璃背景搭配金色邊框與較小字體），隱藏地圖下方多餘提示字 `.map-toolbar-hint`。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [styles.css](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/styles.css)
    - 修改 `@media (max-width: 900px)` 下的所有排版細節規則。

## Verify: Success/Fail, Outputs
- **本地預覽**：經 F12 行動裝置模擬測試，在 iPhone/Pixel 直向網頁下，頂部文字大幅縮減，地圖高度比例從原本被擠扁的狀態成功拉高至 70% 畫面。重設視角與匯出 PNG 以精巧的曜金懸浮按鈕組顯示在地圖右上角，整體操作空間與視覺美觀大幅提升。

# 2026-05-24 21:15 LOG

## Diff: Purpose & Action Trace
- **目的**：重構全域 UI 佈局，解決電腦版與手機版上地圖被 Header 與底部工具列嚴重擠壓的問題。將「重設視角」與「匯出 PNG」按鈕改為全域適用之懸浮控制按鈕組，並徹底刪除下方原有的舊工具列區塊，同時全域隱藏副標題與壓縮 Header 高度。
- **追蹤**：
  1. 修改 `index.html` 將控制按鈕 `.map-controls` 移入 `.map-frame` 框體頂部，並徹底刪除下方的整個 `.map-toolbar`。
  2. 修改 `styles.css`，全域隱藏 `.subtitle`（副標題），並將 Header 內邊距從 `16px 24px 10px` 全域縮減至 `10px 24px 8px`，`h1` 主標題字體調整為 `1.3rem`。
  3. 新增 `.map-controls` 與 `.control-btn` 樣式規則，實現具有曜金磨砂玻璃質感的懸浮控制介面。
  4. 清理 `styles.css` 行動端媒體查詢內先前冗餘的舊工具列覆寫樣式。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [index.html](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/index.html)
    - 移入控制項並刪除舊 `map-toolbar` DOM。
  - [styles.css](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/styles.css)
    - 修改 Header、`.subtitle` 及懸浮按鈕樣式。

## Verify: Success/Fail, Outputs
- **本地預覽**：開啟 `http://127.0.0.1:5188`，地圖在垂直方向獲得了前所未有的舒展（高度空間完全被解鎖），呈現高挑巨幅海報感。重設視角與匯出按鈕以精巧的曜金懸浮樣式固定於地圖右上角，不論在電腦大螢幕或拉窄後的行動裝置下均能完美排版並順暢執行。

# 2026-05-24 21:20 LOG

## Diff: Purpose & Action Trace
- **目的**：重構全域 UI 控制項排版，改善空間利用率。將「重設視角」與「匯出 PNG」按鈕移至 Header 主標題後方；將地圖上裝飾飛機圖示美化為流線型客機並調大 scale；將原本佔用頂部空間的綠色「本地隱私保證」提示框移至頁尾並精簡為簡約文字；最後將右側欄卡片重新分類為「足跡資料」、「地圖風格」、「海報自訂」三個分頁 (Tabs) 顯示，並相應調整自動化測試腳本。
- **追蹤**：
  1. 重構 `index.html`：在 Header 加入 `.title-wrap` 以將主標題與控制按鈕橫向並排。
  2. 在 `.sidebar` 頂部置入 `.sidebar-tabs`（足跡資料、地圖風格、海報自訂）切換列。
  3. 用三個 `.tab-content` 面板包裹所有右側欄卡片，不顯示的預設為 `.hidden`。
  4. 移除原本醒目的 `.privacy-notice-box`，在 `<footer>` 最下方加入精簡的 `.privacy-footer-text` 隱私保證說明。
  5. 重構 `styles.css`：刪除舊的 `.map-controls` 與 `.control-btn` 懸浮按鈕樣式，新增 `.title-wrap`, `.header-controls`, `.btn-header-control` 的排版與曜金邊框按鈕樣式；新增分頁 Tab 樣式（包含 `position: sticky` 置頂效果與 active 狀態下底線）；新增 `.privacy-footer-text` 簡潔版隱私背景樣式。
  6. 重構 `app.js`：將 `renderAirplane()` 中的十字飛機 path 替換為流線客機向量路徑，並調整 scale 為 `1.15`；在 `bindEvents()` 中加入 Tab 切換的事件監聽。
  7. 修改 `test_map.js` 測試腳本：由於一鍵風格按鈕被隱藏在「地圖風格」分頁，在步驟 7 模擬點擊之前，增加 `page.click('button[data-tab="tab-style"]')` 進行分頁切換。
  8. 重新執行 `test_map.js` 通過所有端對端測試。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [index.html](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/index.html)
  - [styles.css](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/styles.css)
  - [app.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/app.js)
  - [test_map.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/test_map.js)

## Verify: Success/Fail, Outputs
- **語法檢查**：`node -c app.js` 與 `node -c test_map.js` 檢查無誤。
- **自動化測試**：`node test_map.js` 重新執行，8 個步驟全部成功通過，無 any Exception，且成功切換 Tab，點擊 Leaflet 與 D3 標記，並輸出 9 張截圖到 artifacts 目錄。
- **本地預覽**：經由測試快照確認，按鈕已緊隨主標題後方；右側欄分頁切換流暢，版面不再擁擠；最下方的隱私保證非常典雅精緻。

# 2026-05-24 21:30 LOG

## Diff: Purpose & Action Trace
- **目的**：微調頂部 Header 控制按鈕（重設視角、匯出 PNG）之排版與字體大小。將按鈕置於整個頂部欄最右側以平衡視覺，並縮小按鈕的字體與內邊距，使其與精美的標題呈現出主次分明的層次感。
- **追蹤**：
  1. 修改 `styles.css`：為 `.title-wrap` 加上 `justify-content: space-between` 和 `width: 100%`，使 `<h1>` 標題居左、按鈕控制列居右。
  2. 為 `.header-controls` 加上 `margin-left: auto`，保證手機版等窄螢幕下換行時，按鈕列依然能保持向右對齊。
  3. 修改 `.btn-header-control` 的 `font-size` 為 `0.75rem`，並縮小其 `padding` 為 `5px 10px`。
  4. 重新執行 `test_map.js` 自動化測試進行端對端驗證。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [styles.css](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/styles.css)

## Verify: Success/Fail, Outputs
- **自動化測試**：重新運行 `node test_map.js`，全部 8 個測試步驟依然完美通過，證明 UI 排版變更完全正確，未引起 DOM 阻礙或功能破壞。
- **本地預覽**：經由最新產出的測試快照確認，按鈕已完美靠至頂端最右側，且字體更加精小緊湊，與主標題呈現良好的層次感。

# 2026-05-24 21:40 LOG

## Diff: Purpose & Action Trace
- **目的**：擴充「質感配色」卡片，新增「自訂專屬配色」功能，允許使用者自選 6 個核心顏色（海洋背景、陸地填充、陸地輪廓、標記顏色、文字色彩、邊框裝飾），並在變更時即時重著色地圖 (D3 與 Leaflet) 及全站網頁 UI，同時使此設定完美相容於 LocalStorage 快取與 JSON 備份檔。
- **追蹤**：
  1. 修改 `index.html`：在 `#themeSelect` 下拉選單中新增 `custom` 選項，並在其下方新增 `#customThemePanel`（預設包含 `.hidden`），內含 6 個 `input[type="color"]` 顏色選擇器元件。
  2. 修改 `app.js`：
     - 在 `els` 中加入自設面板及 6 個 Color inputs 的 DOM 物件綁定。
     - 在 `state` 中新增 `customTheme` 配色狀態結構。
     - 重構 `theme()`：若當前主題為 `custom`，則動態讀取 `state.customTheme` 的顏色並包裝成對應色彩物件回傳。
     - 擴充 `applyTheme()`：當主題為自訂配色時，顯露 `#customThemePanel` 面板並同步 inputs 的 value 值，否則隱藏面板。
     - 修改 `saveToLocalStorage` 與 `loadFromLocalStorage`，在快取中讀寫 `travelFootprintCustomTheme`。
     - 修改 `exportBackup` 與 `readBackupFile`，在 JSON 配置備份與還原中序列化/解析 `customTheme`。
     - 在 `bindEvents()` 中，為這 6 個 Color inputs 綁定 `input` 事件，在滑動調色時動態呼叫 `applyTheme()` 並儲存。
  3. 修改 `test_map.js` 測試腳本：追加測試步驟 8，模擬切換至自訂主題，展開調整面板，調色海洋背景為墨藍色 (`#0a1624`) 並截圖驗證。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [index.html](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/index.html)
  - [styles.css](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/styles.css) (新增對 cp inputs 稍微的美化排版)
  - [app.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/app.js)
  - [test_map.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/test_map.js)

## Verify: Success/Fail, Outputs
- **自動化測試**：`node test_map.js` 重新執行，9 個步驟全部成功通過，無 any Exception，且成功切換至自訂主題，Color picker 面板順利滑出，調色後地圖即時重置，順利產出 10 張快照圖（含 10_custom_theme.png）。
- **快取還原驗證**：調色後重新整理瀏覽器，顏色完美保持；匯出備份 JSON 並在清除快取後重新載入，配色與 color picker 控制面板的 inputs 完全還原。

# 2026-05-24 21:50 LOG

## Diff: Purpose & Action Trace
- **目的**：微調地圖海報中個人簽名 / 署名文字的排版位置與字體大小。將簽名文字由原本的左下角移動至右下角以與左側羅盤呼應，並將字體放大兩倍，呈現更醒目且大氣的個人署名效果。
- **追蹤**：
  1. 修改 `app.js` 的 `renderTitleBlock()` 函數：
     - 將 `customSignature` 署名文字的 `x` 坐標從原本的左下角 `40` 改為右下角 `width - 40`。
     - 將對齊屬性 `text-anchor` 從原本的 `start`（向右延展）改為 `end`（向左對齊延展），確保在右下角邊界處排版整齊不溢出。
     - 將字體大小 `font-size` 從原本的 `11px` 改為 `22px`（即放大兩倍）。
     - 將字距 `letter-spacing` 調整為 `2px`。
  2. 重新執行 `test_map.js` 自動化測試進行端對端驗證。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [app.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/app.js)

## Verify: Success/Fail, Outputs
- **自動化測試**：重新運行 `node test_map.js`，全部 9 個測試步驟依然完美通過，證明 D3 海報標題與署名重著色繪製邏輯完全正確，功能穩定無礙。
- **本地預覽**：經由最新產出的測試快照確認，個人簽名已完美固定於海報畫面最右下角，且字體加大兩倍，與主標題、副標題佈局更顯勻稱美觀。

# 2026-05-24 21:55 LOG

## Diff: Purpose & Action Trace
- **目的**：更新右側欄頁尾之「添心設計」官方網站與 Facebook 粉絲專頁的超連結網址，並為超連結引入對應的向量 SVG 圖示，配合增加精巧的 Hover 微縮放與旋轉動畫，強化整體 UI 的 Premium 質感。
- **追蹤**：
  1. 修改 `index.html`：
     - 將官網超連結從 `https://tanxin.co/` 更新為 `https://tanxin.space/`，並在超連結內嵌入 Global/Web SVG 圖示。
     - 將 Facebook 超連結從 `https://www.facebook.com/tanxindesign/` 更新為 `https://www.facebook.com/TainanTanXin`，並嵌入 FB 原生 SVG 標誌圖示。
     - 移除舊有文字中的 "↗" 外連指示符。
  2. 修改 `styles.css`：
     - 新增 `.footer-link` 與 `.link-icon` 的 Flex 佈局。
     - 實作 `.footer-link:hover .link-icon` 在 Hover 狀態下的縮放與輕微旋轉特效（`transform: scale(1.18) rotate(6deg)`），以提升介面的生動感。
  3. 重新執行 `test_map.js` 自動化測試進行端對端驗證。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [index.html](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/index.html)
  - [styles.css](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/styles.css)

## Verify: Success/Fail, Outputs
- **自動化測試**：`node test_map.js` 測試流程 100% 成功通過，未發生任何 DOM 切換阻礙或超連結引起的問題。
- **本地預覽**：經人工驗證，頁尾超連結運作完全正常，滑鼠 Hover 時圖示會輕微旋轉放大，視覺微交互效果流暢且高度精緻。


# 2026-05-24 21:42 LOG

## Diff: Purpose & Action Trace
- **目的**：優化個人簽名 (customSignature) 的排版外觀，將其自右下角移至底部中央，與副標題及主標題垂直對稱排版（掛軸式），並縮小字體至 13px 以符合視覺層級；同時過濾地圖中巨大的南極洲陸地，解決標題與南極洲重疊卡住的問題。
- **追蹤**：
  1. 將 `customSignature` 署名改為水平置中排版 (`text-anchor = middle`, `x = width / 2`, `y = height - 20`)，並將字體縮減為 `13px`。
  2. 若有設定署名，副標題與主標題各往上推（副標題 `height - 80`，主標題 `height - 50`）以騰出底端空間；若無署名，則保持原位（副標題 `height - 60`，主標題 `height - 30`），使排版平衡。
  3. 在載入 GeoJSON 世界陸地數據的 `loadLandGeo` 函數中，遍歷其 features 並利用經緯度範圍過濾掉所有點的最大緯度小於 `-60` 度的多邊形（即南極洲大陸與附屬邊緣冰架），從根本上防止渲染南極洲且不影響全球投影的 fitExtent 範圍。
  4. 重新執行 `test_map.js` 通過所有端對端測試。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [app.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/app.js)
    - `renderTitleBlock()`：重構文字與簽名排版坐標與字體大小。
    - `loadLandGeo()`：在載入 geojson 後，加入 `data.features.filter()` 機制以過濾南極洲。

## Verify: Success/Fail, Outputs
- **自動化測試**：執行 `node test_map.js`，全部 9 個測試步驟 100% 成功通過，未出現 regression。
- **排版視覺驗證**：
  - 填入簽名後，副標題、主標題與簽名垂直居中，層次感佳，13px 署名比例適中，不再搶眼。
  - D3 向量地圖底部已無巨大的南極洲白色陸地，解決了先前遮擋標題文字的重疊問題，整體畫面清爽開闊。


# 2026-05-24 21:44 LOG

## Diff: Purpose & Action Trace
- **目的**：應使用者要求，將個人簽名/署名文字在垂直排版上調整至主標題的前面（上方），形成「副標題 -> 個人署名 -> 主標題」的優雅順序。
- **追蹤**：
  1. 修改 `app.js` 的 `renderTitleBlock()`，調整有簽名時的 y 軸坐標：副標題保持在 `height - 80`，個人署名（名字）移至 `height - 54`（主標題上方），主標題下移至 `height - 24`。
  2. 調整 SVG 元素的 append 順序，使個人簽名的 text 元素在 DOM 中先於主標題繪製。
  3. 重新執行 `test_map.js` 通過所有端對端測試。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [app.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/app.js)
    - `renderTitleBlock()`：微調 y 軸排列順序及渲染 DOM 順序。

## Verify: Success/Fail, Outputs
- **自動化測試**：執行 `node test_map.js`，9 個步驟全部 100% 成功通過。
- **排版視覺驗證**：署名成功移至主標題上方，視覺流暢，排版比例勻稱。


# 2026-05-24 21:46 LOG

## Diff: Purpose & Action Trace
- **目的**：解決先前垂直三行排版引起的不美觀與地圖底部陸地被擠壓的視覺缺陷，將個人簽名/署名（名字）改為水平融入主標題的前方，還原海報均衡優雅的兩行排版。
- **追蹤**：
  1. 重構 `app.js` 的 `renderTitleBlock()`，取消垂直方向的三層高度計算，將副標題與主標題 y 軸固定回原本的 `height - 60` 與 `height - 30`。
  2. 若 `state.customSignature` 存在，則在主標題最前方以「 · 」將簽名與大標題字串相連（例如 `Nephi · 旅遊足跡  Travel Footprint`）。
  3. 這使得整體字體、顏色完全統一，既能顯眼地在最前方標識署名，又不會佔用地圖的垂直高度，排版極為平衡大氣。
  4. 重新執行 `test_map.js` 通過所有端對端測試。

## Tech: Modified Files, Functions, Logic
- **修改檔案**：
  - [app.js](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/travel-footprint-map/app.js)
    - `renderTitleBlock()`：重構標題渲染逻辑，將簽名合併為標題前置綴。

## Verify: Success/Fail, Outputs
- **自動化測試**：執行 `node test_map.js`，9 個步驟全部 100% 成功通過，未出現任何 Regression。
- **排版視覺驗證**：合併後名字成功呈現在主標題最前方，畫面整體大氣、精細，南美洲及澳洲底部與文字留白合理，不美觀問題順利獲得解決。
