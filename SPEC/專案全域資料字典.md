# 專案全域資料字典 (Project Data Dictionary)

> **本檔位置（正式）**：`CODING/SPEC/專案全域資料字典.md`（**2026-04-25** 自倉庫根目錄合併至此，與各模組 SPEC 同層。）  
> 本文件為「參數一致性」的唯一事實來源。所有前端、後端、API 傳輸與資料庫儲存必須**嚴格遵守**下表定義的變數名稱，嚴禁建立自定義映射表 (Mapping Table)。

## 1. 案場資料 (案場資料 Sheet) - 完整原始欄位

| Sheets 原始標題 | 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- | :--- |
| **siteName** | `siteName` | `String` | 案場地址或自選名稱 |
| **專案狀態** | `專案狀態` | `String` | 目前執行狀態 |
| **address** | `address` | `String` | 完整地址 |
| **業主姓名** | `業主姓名` | `String` | 合約／屋主姓名；**選填**。供主控台查案與客人自助綁定核對。**不寫入進場資訊、不進 LINE 查案** |
| **業主手機** | `業主手機` | `String` | 屋主手機；**選填、純文字**（保留開頭 0）。供主控台查案與綁定核對末四碼。**不寫入進場資訊、不進 LINE 查案** |
| **latitude** | `latitude` | `Number` | 緯度 |
| **longitude** | `longitude` | `Number` | 經度 |
| **建立時間** | `建立時間` | `String` | 案場資料建立時間 |
| **案號** | `案號` | `String` | 唯一專案編號 (如: 742) |
| **專案負責人** | `專案負責人` | `String` | 負責人 LINE UID；可逗號／頓號分隔多位（亦相容姓名） |
| **專案起始日** | `專案起始日` | `String` | 工期開始（案場欄位）；空白時毛利／開工日可改用施工紀錄推算 |
| **結案日** | `結案日` | `String` | 按下結案當天（Asia/Taipei）；未結案可空 |
| **專案分區** | `專案分區` | `String` | 所屬門市/分區 |
| **設計師** | `設計師` | `String` | |
| **助理** | `助理` | `String` | |
| **工務** | `工務` | `String` | |
| **入門方式** | `入門方式` | `String` | |
| **停車方式** | `停車方式` | `String` | |
| **施工進場時間** | `施工進場時間` | `String` | |
| **保證金事宜** | `保證金事宜` | `String` | |
| **備註-管理中心電話** | `備註-管理中心電話` | `String` | |
| **衛浴使用說明** | `衛浴使用說明` | `String` | |
| **備註-施工時間** | `備註-施工時間` | `String` | |
| **備註-特別注意事項** | `備註-特別注意事項` | `String` | |

## 2. 案場資料 (案場資料 Sheet) - 稽核動態欄位

| Sheets 原始標題 | 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- | :--- |
| **audit_items_total** | `audit_items_total` | `Number` | 專案工項總數 (自動補齊) |
| **audit_items_verified** | `audit_items_verified` | `Number` | 已核核工項總數 (自動補齊) |
| **audit_percent** | `audit_percent` | `String` | 整體審核進度 (自動補齊) |
| **audit_last_synced_by**| `audit_last_synced_by`| `String` | 最後同步者姓名 (自動補齊) |
| **audit_last_updated** | `audit_last_updated` | `String` | 最後同步時間 (自動補齊) |

## 3. 員工與權限資料 (Sheet: 員工資料)

> **試算表檔（重要）**：全公司只應有**一份**員工主檔試算表（HUB 考勤用的打卡主檔，例如行政維護的 ○○151 表）。  
> `CheckinSystem` 的 `SPREADSHEET_CHECKIN_ID` 與 `accounting-gas` 的 `CHECKIN_SPREADSHEET_ID` **必須指向同一檔案**；否則 HUB 與會計會對同一 `userId` 查到不同權限。詳見 [`19_HUB與會計全域身分傳承.md`](19_HUB與會計全域身分傳承.md) §1.1。

| Sheets 原始標題 | 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- | :--- |
| **userId** | `userId` | `String` | LINE UID |
| **userName** | `userName` | `String` | LINE 顯示姓名 |
| **shiftStartTime** | `shiftStartTime` | `String` | 上班時間 |
| **shiftEndTime** | `shiftEndTime` | `String` | 下班時間 |
| **身份** | `身份` | `String` | 職位 (如: BOSS, 設計師) |
| **班別** | `班別` | `String` | 排班制度 |
| **組別** | `組別` | `String` | 所屬分店 |
| **權限** | `權限` | `Number` | 權限等級 (1-9) |
| **到職日** | `到職日` | `String` | |
| **離職日** | `離職日` | `String` | |
| **聯絡電話** | `聯絡電話` | `String` | |
| **聯絡地址** | `聯絡地址` | `String` | |
| **緊急聯絡人** | `緊急聯絡人` | `String` | |
| **緊急聯絡人電話** | `緊急聯絡人電話` | `String` | |
| **email** | `email` | `String` | |
| **出生日期** | `出生日期` | `String` | |
| **身分證字號** | `身分證字號` | `String` | |
| **銀行代碼** | `bankCode` | `String` | 3 碼，不足左補 0；**純文字**欄（`@`） |
| **分行名稱** | `branchName` | `String` | |
| **帳號** | `accountNo` | `String` | 僅數字；**純文字**欄 |
| **戶名** | `accountName` | `String` | 薪轉戶名 |
| **統編** | `taxId` | `String` | 選填；**純文字**欄 |
| **薪資類型** | `payType` | `String` | `月薪`／`日薪` |
| **發薪規則** | `payRule` | `String` | 例 `月薪-10`、`日薪-1/11/21` |
| **底薪** | `baseSalary` | `Number` | |
| **全勤（試算表）／出勤獎金（UI）** | ullAttendanceBonusMax | Number | Sheets 欄「全勤」；前端標籤「出勤獎金」；未填＝0 |
| **聯絡電話／緊急聯絡人電話** | `contactPhone`／`emergencyContactPhone` | `String` | **純文字**；非 7 碼市話自動補前導 `0`（試算表勿用數字格式） |
| **彈性時間** | `彈性時間` | `String` | 彈性上班分鐘數 (應對齊 Sheets 為 彈性時間 而非 Flextime) |
| **pcName** | `pcName` | `String` | 設備名稱 |

## 4. 日誌系統 (ProjectLog Sheet)

| Sheets 原始標題 | 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- | :--- |
| **LogID** | `LogID` | `String` | 日誌唯一 ID (ULID/UUID) |
| **Timestamp** | `Timestamp` | `Date` | 建立時間 |
| **ProjectName** | `ProjectName` | `String` | 對應之案號 (注意: Sheets 命名為 ProjectName 但存的是案號) |
| **UserID** | `UserID` | `String` | LINE UID |
| **UserName** | `UserName` | `String` | LINE 顯示姓名 |
| **Title** | `Title` | `String` | 日誌標題 |
| **Content** | `Content` | `String` | 日誌內容全文 |
| **PhotoLinks** | `PhotoLinks` | `String` | 圖片連結 (以半形逗點分隔之 CSV) |
| **Status** | `Status` | `String` | 狀態 (如: 已發布) |
| **AI_AnalyzedAt** | `AI_AnalyzedAt` | `Date` | AI 現場分析時間（施工回報 AI） |
| **AI_Signal** | `AI_Signal` | `String` | 燈號：`green`／`yellow`／`red`／`skipped` |
| **AI_Summary** | `AI_Summary` | `String` | AI 白話摘要（後端驗證後產生） |
| **AI_FindingsJson** | `AI_FindingsJson` | `String` | AI 原始 JSON + validated 區塊 |
| **AI_OpenItemsCount** | `AI_OpenItemsCount` | `Number` | 送入 AI 的同工程未完成工項筆數 |
| **AI_PhotosSent** | `AI_PhotosSent` | `Number` | 送入 AI 的照片張數 |
| **AI_Segments** | `AI_Segments` | `Number` | Gemini 分段呼叫次數 |
| **AI_Model** | `AI_Model` | `String` | 使用的 Gemini 模型 |
| **AI_KeyTier** | `AI_KeyTier` | `String` | 金鑰 tier：`free`／`paid`／`legacy` |
| **AI_TokenUsage** | `AI_TokenUsage` | `Number` | 本次 token 用量合計 |
| **AI_HumanReviewed** | `AI_HumanReviewed` | `String` | 人工覆核標記（預設空） |

## 5. 稽核歷程 (Firebase JSON 內部結構)

| JSON Key | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **price**（可選，工項） | `String` | 單一工項之**總價／小計**（Excel 欄位對應，字串、可含逗號等）；內部驗收可顯示、客戶端不顯示。見 `12_BUDGET_*_SPEC` |
| **audit_logs** | `Array` | 個別工項的操作歷程 |
| └ `ts` | `Number` | Timestamp |
| └ `user` | `String` | 操作者姓名 |
| └ `action` | `String` | 動作描述 (如: "進度變更: 0% -> 50%") |

## 6. 每日回報與打卡整合 API (daily_report / attendance_api)

本章節定義 `daily_report.html` 看板進行考勤與施工日誌聯表 (Join) 時，各 API 欄位的命名約定與對齊規範。

| API 屬性名 | 資料格式 | Sheets 欄位對應 | 系統別 | 說明 |
| :--- | :--- | :--- | :--- | :--- |
| **userId** | `String` | LINE UID | 員工資料 / 考勤 | 小寫 `id`，用於員工基本資料、排班與打卡索引 |
| **UserID** | `String` | LINE UID | 日誌系統 | 大寫 `ID`，用於日報/日誌關聯鍵 |
| **userName** | `String` | LINE 顯示姓名 | 員工資料 / 考勤 | 小寫 `name`，用於員工基本資料、排班 |
| **UserName** | `String` | LINE 顯示姓名 | 日誌系統 | 大寫 `Name` |
| **pcName** | `String` | 設備名稱 | 員工資料 | 員工使用之設備識別名稱 |
| **group** | `String` | 組別 | 員工資料 / API | API 屬性對應 Sheets 中的「組別」，如「設計一部」 |
| **date** | `String` | 日期 | 考勤 / 排班 | 格式為 `YYYY-MM-DD` |
| **checkIn** | `String` | 進場時間 | 考勤 (attendance) | 員工當日進場打卡時間 (e.g., "08:32")，無打卡顯示 "---" |
| **checkOut** | `String` | 退場時間 | 考勤 (attendance) | 員工當日退場打卡時間 (e.g., "17:35")，無打卡顯示 "---" |
| **Content** | `String` | Content | 日誌系統 | 完整回報內文 (含施工內容與問題回報) |
| **PhotoLinks** | `String` | PhotoLinks | 日誌系統 | 以半形逗號分隔之圖片網址清單 |

### 6.1 假勤年度統計 `action=leave_year_stats`（Checkin `attendance_api`）

| 參數／欄位 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **action** | `String` | 固定 `leave_year_stats` |
| **mode** | `String` | `mine`／`refresh`（本人，需 **permission 4 或 5**）；`list`（管理端全員，需 **permission > 4**） |
| **year** | `Number`／`String` | 曆年；省略則當年 |
| **operatorId** | `String` | 操作者 LINE UID |
| **data.year** | `Number` | 回傳統計年度 |
| **data.lastUpdatedAt** | `String` | 快取／計算時間（可空） |
| **data.rows[]** | `Array` | 僅 `list`；見下表 |
| **employeeUserId**／**employeeName**／**employeeGroup** | `String` | 員工識別與組別 |
| **annualLeaveDays**／**sickLeaveDays**／**personalLeaveDays** | `Number` | 特休／病假／事假已用（天，可小數） |
| **annualLeaveQuota**／**annualLeaveRemaining** | `Number`／`null` | 給額／剩餘；無給額時為 `null` |
| **compensatoryDays**／**overtimeDays** | `Number` | 補休／加班（天） |
| **lateMinutes**／**earlyMinutes** | `Number` | 遲到／早退（分） |
| **annualLeaveDetails** | `Array` | 特休使用明細（`mine`／`refresh` 之 `row` 內；來源 `annualLeaveDetailsJson`） |
| **monthly** | `Object` | 各月小計（`row.monthly`；來源 `monthlyJson`） |
| **availableYears** | `Array<Number>` | 僅 `mine`／`refresh`；快取已有之曆年 |
| **yearEndDisposition** | `String` | `carry_over`／`salary`／空 |
| **yearEndDispositionDays** | `Number`／`null` | 年底處置天數 |
| **unusedAnnualLeaveDays** | `Number`／`null` | 未休特休天數（`max(0, remaining)`） |
| **disposition** | `String`／`null` | `carryover`／`cashout`；試算表代碼轉換 |
| **cashoutAmount**／**cashoutAmountPreview** | `Number`／`null` | 折現金額（已登錄／試算）；日薪公式見 SPEC 17 §4.8.3d |
| **carryoverDays** | `Number`／`null` | 保留至次年天數 |
| **dailyWage**／**dailyWagePayType** | `Number`／`String` | 折現試算用日薪與薪資類型 |
| **annualLeaveQuotaOverride** | `Number`／`null` | 員工主檔「特休給額覆寫」；優先於自動試算 |
| **checkInDays**／**absentDays** | `Number` | 實際出勤／缺勤天 |

工作表：`假勤年度統計`（快取；複合鍵 `statYear` + `employeeUserId`）。詳見 `17_個人薪資出勤頁計劃書.md` §4.8。

## 7. 窗簾自助估價 (Curtain Quote) — 見 `16_窗簾自助估價系統規格書.md`

本章欄位用於 `tools/CurtainQuote.html` 客戶草稿、匯出 JSON 及（Phase 3+）Firebase 擴充。**與快速報價之 `curtain_lk`／`curtain_m`／`curtain_sec`（木作窗簾盒）語意分離**；布簾類程式 id 建議 `fabric_*` 前綴。

### 7.1 單扇窗戶 (`windows[]` 元素)

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **curtainWindowId** | `String` | 窗戶唯一識別（UUID 或 `win-n`） |
| **roomLabel** | `String` | 空間名稱（如：客廳、主臥） |
| **curtainCategoryId** | `String` | 大類 id（`cat_soft`／`cat_shade`／`cat_structured`） |
| **curtainTypeId** | `String` | 窗簾種類（含 `fabric_sheer_light`、`blind_dimming`、`blind_cellular`、`blind_venetian`） |
| **fabricGradeId** | `String` | 材質系列 id（§2.3） |
| **fabricSwatchId** | `String` | 布樣 id（§2.5）；影響才價與估價單品名 |
| **linkedCaseStudyIds** | `Array<String>` | 釘選之案例 id 列表 |
| **blackoutLevel** | `String` | `full`／`semi`／`decor` |
| **windowShape** | `String` | `standard`／`bay`／`l_shape`／`floor_to_ceiling` |
| **internalNote** | `String` | 人員模式；不印客戶版 |
| **staffPriceOverride** | `Number` | 人員模式；單價或該窗調整 |
| **pricingTierKey** | `String` | 級距表命中列 id |
| **requiresManualQuote** | `Boolean` | 超出級距或特殊窗；客戶不顯示金額 |
| **openingWidthCm** | `Number` | 開口寬度（公分） |
| **openingHeightCm** | `Number` | 開口高度（公分） |
| **mountType** | `String` | `ceiling`／`inside`／`wall` |
| **dropType** | `String` | `floor`／`sill` |
| **clientNote** | `String` | 客戶可見備註 |
| **selectedAddons** | `Array<String>` | 加購 id 列表（如：`addon_motor`、`addon_double_layer`） |
| **computedTrackShaku** | `Number` | 推算軌道台尺（唯讀） |
| **computedFabricCai** | `Number` | 推算布料才數（唯讀） |
| **computedTrackLengthCm** | `Number` | 推算軌道長 cm（唯讀、顯示用） |
| **computedFabricWidthCm** | `Number` | 推算布寬 cm（唯讀、顯示用） |
| **computedFabricHeightCm** | `Number` | 推算布高 cm（唯讀、顯示用） |
| **computedFabricCaiSheer** | `Number` | 雙層時紗簾才數（可選） |
| **lineTotal** | `Number` | 該窗小計（元，整數） |
| **photoLocalRef** | `String` | Phase 2；本機現場照參照 |

### 7.2 整份估價草稿 (`cq_curtain_draft_v1`)

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **schemaVersion** | `Number` | 草稿結構版本 |
| **quoteMode** | `String` | `client`／`staff` |
| **windows** | `Array` | §7.1 物件陣列 |
| **contactName** | `String` | 選填聯絡人姓名 |
| **contactPhone** | `String` | 選填聯絡電話 |
| **案號** | `String` | 選填；關聯案場時使用（與 §1 案號同名，禁止 `project_no` 等變形） |
| **draftUpdatedAt** | `String` | ISO 8601 最後更新時間 |
| **grandTotal** | `Number` | 全案合計（元） |
| **taxMode** | `String` | `excluded`／`included_5` |
| **priceBookVersionDate** | `String` | 所用價本版本日期（顯示用） |

### 7.3 價本設定 (`cq_curtain_settings_v1` 內 `typeDefs[]` 元素)

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **id** | `String` | 種類 id，對應 `curtainTypeId` |
| **label** | `String` | 客戶可見品名 |
| **fabricPricePerCai** | `Number` | 布料單價（元／才） |
| **trackPricePerShaku** | `Number` | 軌道單價（元／尺） |
| **lumpPricePerWindow** | `Number` | 每窗一式單價（元／式） |
| **installLumpPerWindow** | `Number` | 安裝費（元／窗） |
| **unitFabric** | `String` | 預設 `才` |
| **unitTrack** | `String` | 預設 `尺` |
| **priceSheetTab** | `String` | 雲端試算表工作表名（Phase 2） |
| **priceSheetItem** | `String` | 試算表 A 欄品項全名 |
| **clientNote** | `String` | 估價單預設備註 |

### 7.4 尺寸規則 (`dimRules` 以 `curtainTypeId` 為鍵)

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **trackExtendEachSideCm** | `Number` | 軌道左右延伸（cm） |
| **fullnessRatio** | `Number` | 褶倍率 |
| **topAllowanceCm** | `Number` | 布高上方余量（cm） |
| **bottomAllowanceFloorCm** | `Number` | 落地下方余量（cm） |
| **bottomAllowanceSillCm** | `Number` | 窗台下方余量（cm） |
| **pricingMode** | `String` | `cai_and_track`／`cai_only`／`lump_per_window`／`size_bracket` |
| **sizeBracketTableId** | `String` | 級距表 id |
| **highCeilingThresholdCm** | `Number` | 超高建議加價門檻（cm） |

### 7.5 級距表 (`sizeBracketTables[]`)

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **id** | `String` | 級距表 id |
| **curtainTypeId** | `String` | 綁定種類 |
| **fabricGradeId** | `String` | 可選材質分級 |
| **rows** | `Array` | 列：`maxWidthCm`、`maxHeightCm`、`lumpPricePerWindow`、`tierLabel`（如 M、L） |

### 7.6 布樣庫 (`fabricSwatches[]`)

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **fabricSwatchId** | `String` | 布樣唯一 id |
| **label** | `String` | 客戶可見名稱 |
| **vendorCode** | `String` | 廠商色號 |
| **curtainTypeIds** | `Array<String>` | 適用窗簾種類 |
| **fabricGradeId** | `String` | 所屬材質系列（可選） |
| **fabricPricePerCai** | `Number` | 才價（元／才） |
| **swatchImageUrl** | `String` | 色卡縮圖 URL |
| **swatchTextureUrl** | `String` | 模擬用紋理 URL（Phase 2） |
| **colorHex** | `String` | 模擬 fallback 色碼 |
| **tags** | `Array<String>` | 篩選標籤 |
| **visibleToClient** | `Boolean` | 是否對外顯示 |
| **sortOrder** | `Number` | 排序 |

### 7.7 案例庫 (`caseStudies[]`)

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **caseStudyId** | `String` | 案例唯一 id |
| **title** | `String` | 案例標題 |
| **roomLabel** | `String` | 空間標籤 |
| **curtainTypeIds** | `Array<String>` | 案例涉及種類 |
| **fabricSwatchIds** | `Array<String>` | 案例使用布樣（可套用） |
| **thumbnailUrl** | `String` | 列表縮圖 |
| **imageUrls** | `Array<String>` | 案例多圖 |
| **summary** | `String` | 一句說明 |
| **sourceUrl** | `String` | 外連（官網文章等） |
| **案號** | `String` | 內部案場關聯（可選） |
| **featured** | `Boolean` | 是否精選 |
| **visibleToClient** | `Boolean` | 是否對外顯示 |
| **sortOrder** | `Number` | 排序 |

## 9. 選材表 (MaterialSelections / MaterialSelectionAudit — project-console)

> 試算表：`SPREADSHEET_REPORT_ID`／分頁 `MaterialSelections`、`MaterialSelectionAudit`（已自 accounting-gas 遷出）。  
> 設計師可新增／作廢（**無草稿**：存了客人立刻看得到）；客戶端唯讀。  
> **改錯**：作廢＋新建為主；補充說明／標籤／定案日可改。  
> 前端頁：`modules/projects/designer-material-selection.html`（**不在**會計殼）。  
> **照片**：**主存 Dropbox**（案號夾下 `選材/`，**掃描／修補亦認名稱含「選材」的資料夾**如 `0705選材`）**＋ Google Drive 副本**（`DRIVE_TEMP/{案號}/選材`）。客人看圖另規劃約 30 天快層（與施工回報桶路徑分開；完整上傳下一刀）。**畫面優先顯示雲端硬碟副本**；沒有副本才用 Dropbox 直連圖檔。尚未入列的檔由**每日未結案比對**或設計師「現在補一次」入列（不掃施工照／PIC）；**客人開頁不掃 Dropbox 夾**。開該案選材列表（designer）時：缺路徑／暫時連結失效 → **自動對回 Dropbox 並補連結**；有路徑缺 Drive → 懶補 Drive（皆有上限、非整庫遷移）。portal 列表不即時打 Dropbox。**結案後約 2 個日曆年**清 Drive 選材照（文字列保留；見 `未來開發事項` 附錄與《選材每日比對與看圖快取》）。

### 9.1 MaterialSelections

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **material_id** | `String` | UUID 主鍵 |
| **project_no** | `String` | 案號 FK |
| **space_labels** | `String` | **空間標籤（複選）**，以「、」串接（如 `客餐廳、玄關`）。常用：客餐廳、主臥、次臥、全室、玄關、衛浴、陽台 |
| **part_labels** | `String` | **部位／材料標籤（複選）**，以「、」串接（如 `地板、櫃體`）。常用：地板、磁磚、油漆／乳膠漆、櫃體、門板、中島／枱面、五金、燈具、窗簾、其他。可空（舊資料無此欄視為空） |
| **selected_at** | `String` | **定案日**（Asia/Taipei `yyyy-MM-dd`）；預設＝拍照／建檔當天，可改 |
| **note** | `String` | **補充說明（選填）**：品牌、型號、色號等；可不填＝靠照片 |
| **photo_urls** | `String` | 照片顯示連結（`,` 或 `;` 分隔；每筆 1～3 張）。新資料優先存 Drive 檢視連結；列表 API 會重算（Dropbox → Drive fallback） |
| **drive_file_ids** | `String` | **Google Drive 檔案 ID**（`,` 分隔；供顯示 fallback 與結案後 2 年 sweep）。舊列若誤存 Dropbox `id:…` 會被忽略 |
| **dropbox_paths** | `String` | Dropbox 檔案完整路徑（`,` 分隔），例如 `/添心設計/設計圖/742 王家/選材/客廳_地板_01.jpg` |
| **photo_filenames** | `String` | 實際檔名（`,` 分隔）；命名規則：`空間_部位_流水號.ext`（定案日不寫進檔名） |
| **storage_provider** | `String` | `dropbox+drive`（新資料）／`dropbox`／空白（舊資料） |
| **photos_purged_at** | `String` | 照片已依保留政策清除之時間（ISO）；有值時列表不回傳 `photo_urls` |
| **source_size** | `String` | 來源檔大小（位元組）；每日比對寫入，供快層「一致不搬」 |
| **source_modified_at** | `String` | 來源檔修改時間；同上 |
| **photo_cache_urls** | `String` | 客人看圖快層網址（`,` 分隔）；約 30 天，優先於硬碟縮圖 |
| **photo_cache_expires_at** | `String` | 快層預計失效時間（台北 `yyyy-MM-dd'T'HH:mm:ss`） |
| **photo_cache_fingerprint** | `String` | 快層比對鍵：`檔名|大小|時間`；對得上且未過期就不搬 |
| **status** | `String` | `active`／`void` |
| **created_by** | `String` | 建立者 LINE UID |
| **created_by_name** | `String` | 建立者姓名 |
| **created_at** | `String` | ISO 8601 |
| **updated_at** | `String` | ISO 8601 |
| **voided_at** | `String` | 作廢時間 |
| **version** | `Number` | 版本號（每次更新 +1） |
| **category** | `String` | **舊欄（可讀可忽略）**；新資料不填 |
| **item_name** | `String` | **舊欄（可讀可忽略）**；新資料不填 |
| **spec** | `String` | **舊欄（可讀可忽略）**；新資料不填 |
| **unit** | `String` | **舊欄（可讀可忽略）**；新資料不填 |
| **quantity** | `String` | **舊欄（可讀可忽略）**；新資料不填 |

### 9.2 MaterialSelectionAudit

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **audit_id** | `String` | UUID |
| **material_id** | `String` | FK → MaterialSelections |
| **project_no** | `String` | 案號 |
| **action** | `String` | `create`／`update`（僅照片補傳等）／`void`／`scan_import`（選材夾入列）／`photos_purged`（保留政策清照） |
| **actor_user_id** | `String` | 操作者 LINE UID |
| **actor_name** | `String` | 操作者姓名 |
| **occurred_at** | `String` | ISO 8601 |
| **before_json** | `String` | 變更前 JSON 快照 |
| **after_json** | `String` | 變更後 JSON 快照 |
| **diff_summary** | `String` | 白話差異摘要 |

## 8. 會計主檔 (accounting-gas)

> 試算表欄位名用 **snake_case**；電話規則與 §3 員工 `contactPhone` **相同**，實作在 `accounting-gas/core/SheetTextFormat.js`（`normalizeTaiwanPhone_`）。

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **contact_phone**（廠商） | `String` | **純文字**（`@`）；只留數字；7 碼市話不補 0，其餘缺前導 `0` 則補 |
| **phone**（收款帳戶 payee，**已廢棄 2026-07-18**） | `String` | 舊欄位；勿再新建 payee 列 |

### 8.1 廠商檔案索引 (`vendor_document` 分頁)

> 試算表：`MASTER_SPREADSHEET_ID`／分頁 `廠商檔案索引`。檔名規則見 [`22_廠商檔案上傳與檔名規則.md`](22_廠商檔案上傳與檔名規則.md)。

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **vendor_doc_id** | `String` | UUID 主鍵 |
| **vendor_id** | `String` | 廠商 FK；**純文字** |
| **vendor_name** | `String` | 冗餘廠商名 |
| **doc_type** | `String` | `報價單`／`合約`／`型錄`／`規格書`／`其他` |
| **project_no** | `String` | 案號；有填時寫入 Drive 檔名；**純文字** |
| **order_no** | `String` | 訂編；僅索引；**純文字** |
| **title** | `String` | 備註；不參與 Drive 檔名 |
| **drive_file_id** | `String` | Drive 檔案 ID；**純文字** |
| **drive_url** | `String` | 檔案連結 |
| **filename** | `String` | Drive 實際檔名（後端依規則產生） |
| **mime_type** | `String` | MIME |
| **ocr_json** | `String` | 選用 AI 辨識 JSON |
| **uploaded_at** | `String` | `yyyy-MM-dd'T'HH:mm:ss` |
| **uploaded_by** | `String` | 上傳者 |
| **is_active** | `String` | `是`／`否` |

### 8.1.1 廠商單據快審 (`vendor_quick_review` 分頁)

> 試算表：`MASTER_SPREADSHEET_ID`／分頁 `廠商單據快審`。LINE 傳圖先存廠商夾再人工五類；**不做**自動建請款。見 SPEC 15 §2.1.2、`未來開發事項.md` §1。

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **quick_review_id** | `String` | UUID 主鍵 |
| **vendor_id** | `String` | 廠商 FK；待認領可空；**純文字** |
| **vendor_name** | `String` | 冗餘廠商名；待認領顯示「待認領」 |
| **status** | `String` | `待審`／`待認領`／`已分類` |
| **category** | `String` | 空＝未分；`廢棄`／`請款`／`報價`／`出貨`／`其他` |
| **ai_suggest** | `String` | AI 建議分類（本輪未寫入） |
| **drive_file_id** | `String` | Drive 檔案 ID；**純文字** |
| **drive_url** | `String` | 檔案連結 |
| **drive_folder_url** | `String` | 廠商夾或待認領夾連結 |
| **filename** | `String` | Drive 實際檔名 |
| **mime_type** | `String` | MIME |
| **line_user_id** | `String` | 傳檔 LINE UID；**純文字** |
| **line_group_id** | `String` | 群組 GID（私訊空） |
| **line_message_id** | `String` | LINE message id |
| **source** | `String` | `line_vendor`／`line_group`／`line_dm` 等 |
| **note** | `String` | 備註 |
| **created_at** | `String` | `yyyy-MM-dd'T'HH:mm:ss` |
| **created_by** | `String` | 傳檔者顯示名 |
| **reviewed_at** | `String` | 分類時間 |
| **reviewed_by** | `String` | 分類者 |
| **is_active** | `String` | `是`／`否` |

### 8.2 廠商主檔／請款（匯費相關）

> 試算表：會計主檔／分頁 `廠商`、`廠商請款申請`。完整請款欄位見 accounting-gas `VENDOR_PAYMENT_REQUEST_SPEC`；此處登錄跨前後端契約欄。

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **remit_fee_default** | `String` | 廠商主檔。`是`／`否`。該廠是否接受扣匯費的預設；新建請款帶入 `remit_fee_apply`。空／未填視同 `是`。 |
| **name_aliases** | `String` | 廠商主檔。別名／發票公司名，逗號分隔。請款 OCR 比對時與 `name` 同等；命中則帶入名冊 `name` 與帳戶。 |
| **tax_mode_default** | `String` | 廠商主檔。預設稅別：`inclusive`／`per_row`／`prorate`／空。空＝不強制；請款選廠商且尚未手改稅別時帶入。 |
| **ocr_hint** | `String` | 廠商主檔。專用辨識說明（給 AI 的文字）。第一輪偏弱且已對到此廠時自動第二輪帶入；亦可手動「用此廠商規則再辨一次」。 |
| **ocr_common_hint** | `String` | 全公司請款辨識通用指令（存在 Script Properties，非試算表欄）。名冊頁可編。 |
| **remit_fee_apply** | `String` | 請款母單。`是`／`否`。有勾＝中信實匯少 $30、入帳／毛利不加匯費；沒勾＝公司幫出、入帳＋$30 並加在金額最大分攤案。新建時依廠商 `remit_fee_default`（無廠商或空且金額>30 仍預設 `是`）。 |

### 8.3 FB 發文工作室（accounting-gas `fb_post_*`）

> 詳見 [`21_FB_POST_STUDIO_SPEC.md`](21_FB_POST_STUDIO_SPEC.md)。認證同 AiVisionLab（權限 ≥ 3 或 ingest secret）。

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **action** | `String` | `fb_post_ping`／`fb_post_generate`／`fb_post_edit_image`／`list_project_completion_media`／`fetch_project_completion_media` |
| **photo** | `Object` | `{ data_base64, mime_type }`；前端壓縮 ≤4MB |
| **project_code** | `String` | 案號（list／fetch 完工媒體） |
| **media_type** | `String` | list：`image`｜`video`｜`all` |
| **path** | `String` | fetch：Dropbox 絕對路徑 |
| **data_base64**／**delivery**／**temp_url** | 回傳 | fetch：`delivery=base64|link`；過大可回傳暫存連結 |
| **post_type** | `String` | 文案類型：`完工案例`／`設計分享`／`促銷`／`日常` |
| **tone** | `String` | 語氣（選填） |
| **extra_notes** | `String` | 補充說明（選填） |
| **copy_tags** | `String[]` | 文案組合標籤文字（前綴／中間／後面合成後送入 prompt；選填） |
| **instruction** | `String` | 改圖指令（`fb_post_edit_image` 必填） |
| **aspect_ratio** | `String` | 選填：`1:1`／`4:5`／`16:9` |
| **headline**／**body**／**hashtags**／**cta** | 回傳 | 文案欄位（`fb_post_generate`） |
| **image** | `Object` | `{ mimeType, dataBase64 }`（`fb_post_edit_image`） |

## 10. AI 信箱（project-console）

| 程式/API 變數名 | 資料格式 | 說明 |
| :--- | :--- | :--- |
| **action** | `String` | `agent_inbox_report`／`agent_inbox_list`（需信箱密碼）／`agent_inbox_staff_error`（需員工 userId） |
| **secret** | `String` | 僅 report／list；**勿**放前端 git |
| **userId** | `String` | `agent_inbox_staff_error`：員工 LINE UID |
| **source** | `String` | 自動後端多為 `project-console`；員工大紅錯為 `frontend` |
| **repo** | `String` | `backend`／`frontend` |
| **severity** | `String` | 自動寫入固定 `error` |
| **title**／**summary**／**link** | `String` | 標題／說明／頁面連結 |
| **failedAction** | `String` | 員工大紅錯：當時失敗的動作名（選填） |

---
> [!IMPORTANT]
> **開發規則**：
> 1. 新增欄位必須先在此表更新，再開始撰寫代碼。
> 2. API Payload 必須直接使用上述變數名作為 Key。
> 3. 禁止使用 `projectId`, `project_no`, `ctx_project_no` 等任何變形。
