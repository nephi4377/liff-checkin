# 會計系統全面檢測報告（2026-07-11）

> 執行完成。範圍：CODING 會計前端 + `backend/accounting-gas` + 相關 SPEC。  
> **未** commit／push／部署。

## 摘要

| 項目 | 結果 |
|------|------|
| 完成度 | **已完成**（A–F） |
| 掃描範圍 | 前端 HTML 17 頁、shared `accounting_*` 12 支、HUB 會計入口、accounting-gas WebApp／主模組、SPEC 15／18／19／20／21＋accounting-gas/SPEC |
| 已修小問題 | **6** 項 |
| 高嚴重度缺失 | **3** 項（見下） |
| 待決大項 | **4** 項 |
| 後端冒煙 | L1 smoke **全過**（version 27）；**有寫入正式試算表測試列**（見「需真人補測」） |

**一句話**：主路徑（HUB→會計選單→請款／審核／待匯款／毛利／薪資）程式齊全且 smoke 可通；最大落差在「規格寫全員／≥2，實作鎖 ≥4」、毛利明細不可編輯、以及列表／主檔長 TTL 在多人協作時易看舊資料。

---

## 已修復小問題

| # | 檔案 | 改了什麼 |
|---|------|----------|
| 1 | `modules/accounting/vendor_payment_approve.html` | `emptyHint` 重複 `class` 導致樣式失效 → 改為 `class="sub hidden"` |
| 2 | `modules/accounting/index.html` | 舊頁說明「已併入收支審核」→「請款審核」；bump `accounting_ui`／`accounting_shell` 版本 |
| 3 | `modules/accounting/vendor_status.html` | 廠商無法登入（原走員工 `initSession`≥4）→ 改 `initPaymentRequestSession`＋廠商／財務門檻 |
| 4 | `spa/HubLeftSidebar.js` | 款項待辦「待審／待匯」列不可點 → 加上連到審核／待匯款路由 |
| 5 | `shared/js/accounting_shell.js` | 殼層標題缺「追加減與收款／客戶 portal」→ 補 `PAGE_TITLES` |
| 6 | `shared/js/accounting_ui.js` | 操作紀錄頁名缺薪資／追加減等 → 補 `PAGE_LABELS` |
| 7 | `SPEC/18_會計與主檔快取策略.md` | §3 案場 map 仍寫「Checkin 不會清」→ 與 §6.3 對齊（會 POST invalidate） |

---

## 缺失／缺口清單

### 高

| ID | 標題 | 說明 |
|----|------|------|
| H1 | **匯款請款權限：規格「全員」vs 實作「財務≥4 或廠商」** | **已修 2026-07-11**：請款登入／context／款項進度改為在職員工即可；直接待匯款仍 ≥4。 |
| H2 | **案件毛利明細只能新增、不能改／刪** | **已修 2026-07-11**：明細列有修改／刪除；自動列不可刪、不可改金額。 |
| H3 | **列表快取 3 天＋單人模式：多人協作易看舊待審／待匯** | `AccountingListCache` 現行 SINGLE_USER：TTL 3 天、SWR 24h。審核／待匯款頁若未按「重新載入」，他人剛核准的單可能長時間看不到。SPEC 18 已註明，產品上仍易誤判。 |

### 中

| ID | 標題 | 說明 |
|----|------|------|
| M1 | **SPEC 15／主檔計劃權限與薪資過期** | **已修 2026-07-11**：對齊 ≥4／≥5／登錄 ≥2／請款在職員工；薪資標為已上線（`payroll_review`／`payroll_finance`）。 |
| M2 | **請款統一規格 P3／P4 未完成** | P3：LINE 全導本頁、下架群內核對卡；P4：待匯款頁移除重複「新增」表單。`LINE_OA_SPEC` 仍以核對卡為主路徑。 |
| M3 | **零用金請款（PETTY）僅規格** | `PETTY_REIMBURSEMENT_SPEC` 標「待實作」；無前端頁、無 GAS action。 |
| M4 | **靜態資源 `?v=` 各頁不一致** | 同檔 `accounting_api.js` 有 v=26／27／30／33／34；改共用 JS 後部分頁可能吃到舊快取。 |
| M5 | **`ACCOUNTING_MASTER_DATA_SPEC` 快取敘述過期** | 仍寫 key `v1`、進頁 SWR；實作 key `v4`、主檔包不做進頁背景重抓（以 SPEC 18 為準）。 |
| M6 | **收款帳戶（payees）與廠商主檔欄位語意分岔** | payees 用 `branch_code`；廠商／請款用 `branch_name`。舊 payees 頁已隱藏，但 entity 仍在，易混淆。 |

### 低

| ID | 標題 | 說明 |
|----|------|------|
| L1 | 舊頁仍保留 | `payment_request_compose`（轉址）、`payees`、`vendor_payment_approve` 仍可直連；選單多已隱藏。 |
| L2 | 客戶 portal 列表 HTML 未跳脫 | `customer-finance-portal` 用 innerHTML 拼項目名；資料來自內部，風險低但非最佳。 |
| L3 | 靜態頁 OCR 上傳（SPEC 15 §2.5） | 後端 OCR 有；「靜態頁上傳 OCR」標待需要時再加——請款頁已有 OCR，收支登錄頁無獨立 OCR 上傳。 |

---

## 流暢度問題

| 路徑 | 評估 |
|------|------|
| HUB ≥4 → `#/accounting` → 選單 → 子頁（殼層 iframe） | 順；身分靠 OperatorContext／query 傳承（SPEC 19） |
| HUB ≥2 → `#/accounting-ingest` | 順；選單不再放收支卡片（避免雙入口） |
| 待付款申請 → 請款審核 → 廠商待匯款 → 匯出 | 主流程完整；smoke 驗證 create／export／submit |
| 薪資：出勤送審 → `payroll_review` → `payroll_finance` | 程式齊；需真人走出勤入口 |
| 廠商：`vendor_register` → 請款 → `vendor_status` | 本次已修 status 登入；需 LINE 真人驗 |
| 追加減／收款：設計師頁 ↔ 請款審核 ↔ 客戶 portal | 連結有；客戶 LIFF 需正式環境 |
| HUB 左側「款項待辦」 | 本次已可點進審核／待匯；超過 5 筆才有「還有 N 筆」 |

**卡點（已解）**：一般員工可進「待付款申請／款項進度」（H1 已實作）。

---

## 易誤判 UX

| 現象 | 風險 |
|------|------|
| 列表／主檔長 TTL，畫面像「沒單」其實別人已處理 | 誤判漏審／重複匯款（有「重新載入」可解，但不明顯） |
| 「請款審核」頁同時審廠商請款＋客戶收款／現金簽核 | 功能正確但名稱偏窄，易以為只審廠商 |
| 廠商待匯款頁仍可「新增待匯款」；規格 P4 想併回請款頁 | 雙入口，財務可能不知該用哪頁 |
| 薪資待匯款文案「發薪完成後自動 LINE 通知」 | 後端 `finalizePayslipOnDisburse_` 有接；若 Push 失敗僅 log，前端仍顯示成功 |
| 權限標籤：選單寫「財務以上」實際 ≥4；審核核准 ≥5 | 文件已於 2026-07-11 對齊；實作門檻不變 |

---

## 待你決定的大項

> **2026-07-11 更新**：D1／D2 已拍板並實作；D4 已停 LINE 傳圖自動請款；D3 仍待決。方向見 [`未來開發事項.md`](未來開發事項.md)。

### D1. 匯款請款要不要開放一般員工？（對應 H1）— **已拍板 A，已實作 2026-07-11**

| 選項 | 做法 | 狀態 |
|------|------|------|
| **A. 對齊規格「全員」** | 前端放寬請款專用登入；後端 context／status 對齊 | **已選 A 且已改程式（2026-07-11）** |
| **B. 改規格為「財務≥4＋廠商」** | 選單對 <4 隱藏卡片 | 未採 |

### D2. 毛利明細要不要在網頁上改／刪？（對應 H2）— **已拍板 A，已實作 2026-07-11**

| 選項 | 狀態 |
|------|------|
| **A. 補 UI**（改／刪；自動列限制） | **已選 A 且已改程式** |
| **B. 維持唯讀＋試算表改** | 未採 |

### D3. 列表快取要不要改回多人模式？（對應 H3）— **尚未拍板**

| 選項 | 建議（2026-07-11） |
|------|------|
| **A. 改多人短暫存** | 不必一次改成極短狂打後端；可先縮短暫存＋醒目「重新載入」 |
| **B. 維持 3 天＋強化「重新載入」提示** | 單人財務為主時可接受 |

使用者擔心 GAS 變慢：短暫存／背景更新主要打「列表」API，不是每次狂掃整本試算表；實務上先強化手動重新載入、必要時再縮 TTL。

### D4. LINE 請款要不要執行 P3（廢核對卡、全開網頁）？— **已停自動請款（2026-07-11）**

| 狀態 | 說明 |
|------|------|
| **已做** | `LINE_IMAGE_AUTO_PAYMENT_ENABLED=false`：傳圖不再核對卡／自動開建單；請款改網頁 |
| **仍未來** | AI 歸類後再建單；帶圖進網頁可再補 |

---

## 尚未能自動驗證、需真人補測的項目

- [ ] LIFF 正式登入（關閉 `ACCOUNTING_AUTH_BYPASS`）全路徑
- [ ] 廠商 LINE：`vendor_register` → `#請款`／傳圖 → 審核 → 匯款 → `vendor_status`
- [ ] 客戶 LIFF：`customer-finance-portal` 雙階段確認＋簽名
- [ ] 薪資：出勤個人頁送審 → 主管核准 → 薪轉檔 → 員工收到摘要
- [ ] HUB iframe 身分傳承（換子頁後 uid／permission 不丟）
- [ ] **本次 smoke 寫入資料清理**（標記 `TEST_AUTO`／案號 `SMOKE998`／`SMOKE999`／冒煙廠商支出 $1／毛利 smoke 99 元）。建議測完立刻清；可用清理腳本或人工刪。收支明細／毛利測試列可能需人工補清。（2026-07-11：冒煙腳本已加測完自動清請款／狀態類假單）
---

## 檢測覆蓋清單

### A. 盤點
- [x] 會計 HTML 頁（17）
- [x] shared/js `accounting_*`（12）
- [x] GAS WebApp action（含 crud_／margin_customer_／margin_adjustment_／margin_receipt_ 前綴）
- [x] SPEC：15、18、19、20、21、流程稿、accounting-gas/SPEC 主要文件

### B. 規格 vs 實作
- [x] 缺口矩陣（本報告「缺失／缺口」＋待決）

### C. 靜態審查
- [x] 前端 `accounting_api` action ↔ WebApp（前綴路由已對上；無明顯「前端有、後端無」死呼叫）
- [x] 主檔 entity ↔ `MasterSchema`／`SHEET_COLUMN_MAP`（收支欄位與請款分攤語意已對照；payee vs vendor 分行欄位差異已記）

### D. 可驗證路徑
- [x] `tools/smoke/run-smoke.ps1`（health／policy／auth／bootstrap／crud／margin／form_submit／vendor_payment／payment_request）
- [ ] 本機 `serve` 開站點擊（未開；靜態審查＋smoke 為主）
- [ ] 正式 LIFF／廠商／客戶（需真人）

### E. 小問題修復
- [x] 見「已修復小問題」

### F. 最終報告
- [x] 本檔

---

## A. 盤點明細

### A1. 前端 HTML（`modules/accounting`）

| 頁面 | 檔案 | 選單 | 狀態 |
|------|------|------|------|
| 會計選單／殼層 | index.html | — | ✅ |
| 待付款申請 | payment_request.html | 全員可見 | ✅（權限見 H1） |
| 款項進度 | vendor_status.html | 全員可見 | ✅（本次修廠商登入） |
| 廠商名冊 | vendors.html | ≥4 | ✅ |
| 請款審核 | ledger_review.html | ≥4 | ✅ |
| 薪資審核 | payroll_review.html | ≥5 | ✅ |
| 薪資待匯款 | payroll_finance.html | ≥4 | ✅ |
| 廠商待匯款 | vendor_payment_finance.html | ≥4 | ✅ |
| 單據附件 | attachments.html | ≥4 | ✅ |
| 案件毛利 | project_margin.html | ≥4 | ✅（缺編輯 UI） |
| 追加減與收款 | designer-customer-finance.html | ≥3 | ✅ |
| 客戶 portal | customer-finance-portal.html | 客戶 LIFF | ✅（不在員工選單） |
| 廠商自填 | vendor_register.html | LINE | ✅ |
| 收支登錄 | accounting_ingest.html | 僅 HUB ≥2 | ✅ |
| 精細請款（舊） | payment_request_compose.html | 隱藏／轉址 | ✅ 轉址 |
| 收款帳戶（舊） | payees.html | 隱藏 | 保留 |
| 廠商審核（舊） | vendor_payment_approve.html | 隱藏 | 保留 |

### A2. shared JS

`accounting_api`、`accounting_boot`、`accounting_cache`、`accounting_list_cache`、`accounting_nav`、`accounting_shell`、`accounting_ui`、`accounting_master_data`、`accounting_form_helpers`、`accounting_context`、`accounting_action_queue`、`accounting_lightbox`（＋ `customer_finance_ui`／`customer_finance_bind`）

### A3. 後端 action 族群（摘要）

| 族群 | 例 |
|------|-----|
| 健康／政策／登入 | health、accounting_policy、accounting_auth_me、bootstrap |
| CRUD | crud_*（vendor、payee、ledger_status、attachment…） |
| 請款 | payment_request_*、vendor_payment_*、staff_payment_submit |
| 薪資 | payroll_request_*、employee_bank_ocr |
| 毛利 | margin_* |
| 客戶財務 | margin_customer_finance_*、margin_adjustment_*、margin_receipt_*、client_portal_* |
| 廠商登記／檔案 | vendor_register_*、vendor_*_files／folder |
| 維運 | setup_*、cleanup、annual_close、ledger_ingest |

### A4. 規格對照（狀態）

| 規格 | 與程式 |
|------|--------|
| 15 會計模組 | 過期（權限／薪資） |
| 18 快取 | 大致準；本次修 §3 矛盾 |
| 19 身分傳承 | 與殼層／OperatorContext 一致 |
| 20 流程圖 | 未逐節重畫；行為與 18／19 一致 |
| 21 毛利擴充／客戶財務 | 前後端有實作 |
| PAYMENT_REQUEST_UNIFIED | P1–P2 有；P3–P4 待做 |
| PETTY | 未實作 |
| PAYROLL_SETTLEMENT | 有實作（與 15 矛盾） |

---

## B. 規格 vs 實作缺口矩陣（精簡）

| 功能 | 規格 | 前端 | 後端 | 結論 |
|------|------|------|------|------|
| 收支登錄 | ✅ HUB≥2 | ✅ | ✅ | OK |
| 匯款請款 | 全員 | 鎖≥4／廠商 | submit 寬、context 鎖≥4 | **H1** |
| 請款審核 | ≥4／核准≥5 | ✅ | ✅ | OK |
| 廠商／薪資待匯款 | ✅ | ✅ | ✅ | OK |
| 案件毛利 | ✅ | 缺改刪 UI | 有 update | **H2** |
| 追加減／收款／portal | ✅ | ✅ | ✅ | OK |
| 薪資結算 | 15 寫未做；專規有 | ✅ | ✅ | **改 15** |
| 零用金 | 規格有 | ❌ | ❌ | M3 |
| LINE 請款 P3 | 待做 | 部分開頁 | 核對卡仍在 | M2 |

---

*報告結束。後續依待決 D1–D4 再改程式或更新 SPEC。*
