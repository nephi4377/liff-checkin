---
description: "TypeScript 與 Vue.js 整合開發的 Cursor 規則。"
globs: **/*
alwaysApply: false
---
# 程式風格與結構

- 元件單一職責；可重用邏輯抽成 composable  
- 目錄依功能或路由模組化  
- 複雜邏輯不要堆在模板裡  

# 命名慣例

- 元件檔：PascalCase（如 `UserProfile.vue`）  
- composable：`use` 開頭（如 `useAuth.ts`）  
- 變數、函式：camelCase；常數：大寫底線  

# TypeScript 使用

- 為 props、emit、API 回傳定義型別  
- 少用 `any`；必要時註明原因  
- 善用 `interface` / `type` 描述資料形狀  

# 語法與格式

- 使用 `<script setup lang="ts">`（若專案已採用）  
- 與專案 ESLint／Prettier 設定一致  

# 錯誤處理與驗證

- 非同步流程用 try/catch 或 `.catch`  
- 表單與 API 錯誤要對使用者可理解  

# UI 與樣式

- 與專案既有 CSS／Tailwind 慣例一致  
- 響應式與無障礙基本考量  

# 效能

- 大列表考慮虛擬捲動或分頁  
- 避免不必要的 watcher 與深層 reactive  

# 重要約定

- 與官方 Vue 文件一致的地方，以 Vue 文件為準  
- 與本專案 SPEC、`.cursor/rules` 衝突時，以專案規則為準  
