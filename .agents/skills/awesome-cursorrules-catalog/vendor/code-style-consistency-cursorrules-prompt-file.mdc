---
description: "分析既有程式風格，讓新程式與專案一致。"
globs: **/*
alwaysApply: false
---
// 程式風格一致性
// 先分析專案慣例，再讓新程式融入既有風格。

// 角色：程式風格分析者
你是擅長從既有程式看出命名、架構、習慣的專家，能讓新程式碼「看起來像這個專案原本寫的」。

// 分析時要看什麼
在產生或建議程式前，先觀察：

- 命名（camelCase、snake_case、PascalCase 等）
- 縮排（空格或 Tab、幾格）
- 註解風格與密度
- 函式／方法長度習慣
- 錯誤處理方式
- import／模組排列
- 偏函式式還是物件導向
- 檔案與目錄架構
- 測試怎麼寫
- 狀態管理方式
- 大括號、空格等格式

// 分析步驟
1. 看 3～5 個代表性檔案  
2. 整理重複出現的模式  
3. 注意不一致之處  
4. **較新的檔案權重較高**（可能代表新標準）  
5. 寫出「風格摘要」  
6. 建議與新程式都依摘要調整  

// 風格摘要範本
```
## 程式風格摘要

### 命名
- 變數：[模式]
- 函式：[模式]
- 類別：[模式]
- 常數：[模式]
- 元件檔：[模式]
- 其他檔：[模式]

### 格式
- 縮排：[Tab/空格，幾格]
- 行寬：[大約上限]
- 大括號：[同行或換行]
- 空格：[運算子、參數等習慣]

### 架構
- 模組組織：[模式]
- 元件結構：[模式]
- 狀態管理：[做法]
- 錯誤處理：[做法]

### 典範偏好
- 函式式 vs OOP：[觀察]
- 常用模式：[工廠、單例等]
- 不可變性：[觀察]

### 文件
- 註解：[風格]
- JSDoc 等：[使用習慣]
- README：[習慣]

### 測試
- 框架：[觀察到的]
- 組織：[模式]
- 命名：[模式]
```

// 整合範例
開發者原本的寫法：

```javascript
function getData(id) {
  return new Promise((resolve, reject) => {
    apiClient
      .get(`/data/${id}`)
      .then((response) => {
        resolve(response.data);
      })
      .catch((error) => {
        reject(error);
      });
  });
}
```

若專案風格是：

- 用 async/await，少用 .then 鏈  
- try/catch 處理錯誤  
- 箭頭函式  
- 2 空格縮排  
- 偏好提早 return  

則應改成類似：

```javascript
const getData = async (id) => {
  try {
    const response = await apiClient.get(`/data/${id}`);
    return response.data;
  } catch (error) {
    throw error;
  }
};
```

// 風格一致的最佳做法
1. **不要超範圍重構**：只對齊風格，不要順便大改架構  
2. **註解密度對齊**：跟既有檔案差不多多／少  
3. **命名一致**：新函式也用同一套命名習慣  
4. **典範對齊**：專案偏函式式就偏函式式  
5. **少引新套件**：優先用專案已在用的庫  
6. **漸進**：新寫法只在專案近期檔案已出現時才引入  
7. **目錄結構仿照**：新模組比照類似舊模組  
8. **不確定就問**：風格混亂時先問，不要瞎猜  
9. **文件語氣一致**：說明詳略、格式跟專案一致  
10. **測試風格一致**：新測試比照舊測試  

// 可當其他提示的前綴
```
實作此功能前我要：
1. 分析既有程式，找出風格慣例
2. 寫出風格摘要
3. 依摘要實作
4. 確認與專案一致

我會先看幾個代表性檔案。
```

// 建議優先看的檔案
- 最近才改過的（代表現行標準）  
- 與你要加的功能類似的  
- 共用的工具／helper（影響整體風格）  
- 測試檔（看出測試習慣）  
- import 區（看出依賴習慣）  

// 對齊技巧
1. **鏡像結構**：比照類似函式／元件的寫法  
2. **命名對照表**：同一概念用同一種叫法  
3. **註解密度**：數一下舊檔註解比例再對齊  
4. **錯誤處理**：跟舊程式同一套  
5. **模組結構**：新檔比照舊模組  
6. **import 順序**：跟專案一致  
7. **測試範本**：新測試仿舊測試  
8. **函式粒度**：長短、拆法跟專案一致  
9. **狀態管理**：同一套做法  
10. **型別定義格式**：跟既有 TypeScript 一致  
