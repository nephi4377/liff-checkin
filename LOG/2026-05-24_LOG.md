# 2026-05-24 工作日誌

## 2026-05-24 旅遊足跡互動地圖
- Diff
  - Purpose：靜態 PNG 光斑過大難讀；改為 CODING/tools 內可載入 JSON 的互動網頁工具。
  - Action Trace：
    - 新增 `tools/travel-footprint-map/`（index.html、app.js、styles.css、開啟.bat、README）。
    - 複製 `data/saved-places.json`、`assets/ne_50m_land.geojson`。
    - D3 羅賓森／麥卡托投影、縮放拖曳、重點城市／全部足跡、國家跳轉、PNG 匯出。

- Tech
  - Path：`CODING/tools/travel-footprint-map/`
  - Logic：固定螢幕像素標記（不再用地圖半徑畫大光斑）；human-comfortable 四態與右側操作面板。

- Verify
  - Success：本機 `http://127.0.0.1:5188` 三端點 200；521 筆有效座標可載入。
  - Fix：`d3.geoRobinson` 不在 d3@7 核心；補載 `d3-geo-projection@3` UMD，`getProjection` 加 NaturalEarth 後備。
  - Re-test：Playwright 驗證 hasRobinson=true、77 markers、status 正常、無 pageerror。
  - v2：6 種質感配色、標記隨縮放變大、海岸線隨縮放變細、標記大小滑桿、Carto/OSM/Esri 圖磚底圖、Google 底圖（本機金鑰）。
  - v3：地圖／選單分離捲軸、map-frame 深淺邊框、拖曳限制 2/3 圖寬、Google Takeout 圖文教學連結。
  - v4：向量模式縮放卡頓修復（110m 陸地、縮放節流、移除雙層海岸 zoom 重繪、修正 zoom 迴圈）。
  - v5：修正日本等偏側區域放大被推出畫面（邊界平移 clamp + zoom 同步）；地圖下工具列（重設/匯出）；底圖收進進階；圖磚模式說明與匯出按鈕文案。
  - v5.1：clamp 改保留焦點位置（不強拉回中央）；國家跳轉改 fitBounds；重設視角改即時回 k=1；Playwright 自測 20 次放大通過。
  - v6：海報配色（白陸藍海／深藍綠陸）、海洋填色、放大後海岸線固定像素不變淡；預設改掛軸風。
  - v7：自訂單點標記樣式與大小滑桿優化。
    - 標記大小滑桿最下限自 0.6× 調降至 0.15×，提供更細緻的微型標記。
    - 新增「預設標記圖示」下拉選單（光點、旗子、圖釘），並引入 customMarkerTypes 單點標記儲存狀態。
    - D3 向量模式：重構 updateMarkerZoom 以動態編排與繪製旗子（桿＋面）及大頭針圖釘 SVG；實作 CSS 絕對定位的 #mapPopup 供點擊變更單點圖標。
    - Leaflet 模式：使用 L.divIcon 整合 HTML SVG 標記使視覺一致；監聽 popupopen 並以 marker.setIcon() 實現點擊切換且不中斷彈窗的極佳 UX。
    - 彈窗美化：設計深色毛玻璃、質感金邊（配合 Theme 變色）、按鈕 Active 呼吸光與 Hover 等精美 UI 動效。
  - v7.1：海報自訂文字、裝飾元素與本地快取同步。
    - 旗子圖標微調：桿高降為 r*2.2，旗寬降為 r*1.3，並將其 Glow 光暈 opacity 設為 0，實現乾淨聚焦。
    - 地圖手帳裝飾：在 SVG 底下疊加繪製精緻的「立體復古指北針 (Compass)」與「左上角小飛機帶點狀虛線航線 (Airplane)」，具備 Checkbox 開關可隨時隱藏，顏色自動適應配色主題。
    - 海報文字即時編輯：側邊欄新增「海報字體與裝飾」卡片，使用者可輸入自訂「主標題」、「座右銘/英文句」與「署名/簽名」，地圖即時重繪並可在匯出 PNG 時完整保留。
    - 穿透疊加設計：圖磚模式下同樣保留 D3 羅盤、飛機與海報文字，並藉由 pointer-events 穿透至 Leaflet，維持高質感 UI 一致性。
    - 本地快取與設定備份：實作 LocalStorage 自動存取，開檔自動載入上次狀態；支援設定 JSON 檔的匯出備份與載入還原。
