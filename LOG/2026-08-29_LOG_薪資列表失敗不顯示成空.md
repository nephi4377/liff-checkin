# 2026-08-29 薪資審核／待匯列表：失敗不顯示成空

## Diff／目的

會計輪巡檢：薪資審核頁第一次（或背景更新）向後端要列表失敗時，舊程式會先清空畫面，背景失敗還把錯誤吞掉，看起來像「目前沒有待審」。財務待匯頁失敗則可能停在「載入中…」。

未改發薪、審核、獎金規則；只改「失敗怎麼顯示、快取要不要存失敗」。

## 技術

- `shared/js/accounting_list_cache.js`：`success === false` 不寫入；讀到舊失敗快取會丟掉
- `modules/accounting/payroll_review.html`：先出快取／「更新中」；失敗保留先前列、人話＋再載；篩選變更會重抓
- `modules/accounting/payroll_finance.html`：同上
- `SPEC/18_會計與主檔快取策略.md`、`SPEC/專有名詞白話對照.md`

## 驗證

- Node 模擬 sessionStorage：失敗不寫入、真的空清單可寫入、失敗不覆寫先前成功快取、舊失敗快取會被忽略
- 靜態頁含「載入失敗，請再按重新載入最新」「仍顯示先前內容」
- 正式站需合進 `main`（GitHub Pages）；本環境未跑 `upload.bat`

## 部署狀態

待合進 main。雲端無法 `upload.bat`／clasp。
