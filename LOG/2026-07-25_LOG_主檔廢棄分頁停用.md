# 2026-07-25 LOG — 會計主檔廢棄分頁停用（不自動刪表）

延續 2026-07-18 主檔收斂、2026-07-20 多餘分頁清理。使用者重申截圖中整排要拿掉。

## 範圍（試算表分頁名）

| 分頁 | 處置 |
|------|------|
| 收款帳戶 | 已廢棄；**程式不再重建**；bootstrap 空 |
| 費用分類 | 已廢棄；**程式不再重建**；bootstrap 空 |
| 收支狀態輔助 | 已停用；同步寫入改空操作；**不重建** |
| 訂編案號對照 | **程式停用讀寫／換號**；不重建；毛利總覽「訂編」欄停填 |
| 廠商檔案索引 | **仍保留**（存檔歸檔會寫）；無獨立維護頁；可手動「隱藏分頁」，勿刪 |

## 根因（為何刪了又回來）

財務進入會計時 `ensureMasterEntitySheets_` 會對缺頁**自動 insertSheet**，把已刪的廢棄分頁又建回來。

## 本機改動（未 commit／未 deploy）

- `backend/accounting-gas/core/MasterBootstrap.js`：只確保仍在用的分頁；廢棄 entity bootstrap 固定 `[]`；表單案號改案場
- `backend/accounting-gas/master/MarginModule.js`：停用對照索引；總覽訂編欄停填；案號只認案場
- `backend/accounting-gas/core/SheetCrud.js`：對照／收款／分類 list 空、禁寫
- `backend/accounting-gas/master/VendorPaymentModule.js`：`ledger_status` 同步空操作
- `CODING/modules/accounting/payment_request.html`：拿掉「訂編」輸入欄
- SPEC／本 LOG 標廢棄

## 請使用者手動

1. 打開「會計主檔」試算表
2. 對 **收款帳戶／費用分類／收支狀態輔助／訂編案號對照**：隱藏或封存（可刪；本任務不代刪）
3. **廠商檔案索引**：若不想看到 → 隱藏即可；**不要刪**（存檔還要寫）

## 部署前

需 clasp 部署 accounting-gas ＋ 前端靜態頁更新後才生效；部署前建議再驗：收支登錄案號、請款、毛利、存檔歸檔。
