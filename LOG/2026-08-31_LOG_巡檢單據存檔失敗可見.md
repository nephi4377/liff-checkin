# 2026-08-31 單據與存檔：讀失敗不裝成沒單據／沒選到廠商

## Diff／目的

會計開「單據與存檔」時：
- 待審清單第一次讀失敗雖有人話，沒有自動再試，也沒有清單內「再試一次」
- 手動存檔選了廠商後讀失敗被吞掉，結果區仍寫「請先選擇廠商」，像沒選到

改成載入中／真的空／讀失敗分開；失敗自動再試一次；有舊清單就留著；畫面上可再試。

## 技術

- `modules/accounting/quick_review.html`：`fetchListRetry`／`renderFailList`；手動存檔 `renderArchiveFail`，不再 `.catch` 吞掉
- SPEC：`15` v1.32、`18` 單據與存檔閉環、`專有名詞白話對照.md`「失敗≠空」補本頁

未改商業規則、未改 GAS、未改權限。雲端無法跑 `upload.bat`；正式站需合進 `main`（[PR #51](https://github.com/nephi4377/liff-checkin/pull/51)）。

## 驗證

- `node --test modules/accounting/quickReviewLoadFail.test.mjs`
- 靜態頁可開；完整 LINE 登入本輪未抽測
