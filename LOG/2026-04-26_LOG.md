# 2026-04-26

## 快速報價（QR）腳本拆檔

- **Date**: 2026-04-26（接續對話）
- **Diff**: 將 `tools/QuickRenovationQuote_v2.html` 內大型 IIFE 腳本改為外部四支 JS，置於 `tools/quick-renovation-quote/`；HTML 改為依序 `<script src>` 載入。
- **Tech**:
  - `qr-config-state.js`：常數、`DEFAULTS`、`state`、`esc`
  - `qr-persist-scheme-menu.js`：Firebase、設定合併、方案菜單／規則 UI、試算表 gviz 等
  - `qr-admin-client-forms.js`：管理表單、`fillTemplateSelect`、`readAdminForm` 等
  - `qr-run.js`：客戶端畫面、`renderAllocTable`、`loadSettings` 啟動與按鈕事件
- **Verify**: `node --check` 四檔通過；請本機用瀏覽器開 `QuickRenovationQuote_v2.html` 實測（路徑須與 `quick-renovation-quote/` 相對位置一致）。

## 快速報價：菜單分區表單＋管理員版面

- **Date**: 2026-04-26（接續）
- **Diff**: 各品項卡片可編輯顯示名、`menuBand`、同區排序、`groupTitle`、`quoteSection`；新增 `escAttr`；方案菜單區與品項區 HTML 改版（步驟 ①②③、卡片、留白）。
- **Tech**: `qr-config-state.js`（`escAttr`）、`qr-admin-client-forms.js`（`fillAdminForm`／`readAdminForm`）、`QuickRenovationQuote_v2.html`（`adminItemPricePanel`、`schemeMenuSection` 結構）。
- **Verify**: `node --check` 於 `qr-config-state.js`、`qr-admin-client-forms.js` 通過；瀏覽器開頁檢管理員區。
