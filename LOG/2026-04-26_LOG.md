# 2026-04-26

## 室內快速報價 v2 試作（第二段：先選菜單＋分空間品項＋定量／變量模型）

- **Time**: 2026-04-26（工作階段）
- **目的**: 在坪數推估之後、先讓客戶選**報價方案**；品項依**全室／客餐廳／主臥／次臥逐間**分組；菜單語意＝**項目 × 定量或變量（邊＋%）**；管理員**開發階段**免 PIN。
- **變更檔案**:
  - `tools/QuickRenovationQuote_v2.html`：新 `itemDefs`＋`schemaVersion` 遷移、規則 `shaku_edge_scaled`／`floor_ping_lk_beds`／`edge`、次臥 `perSecondary` 多列、`getDisplayItemRows`、步驟二內先選方案、步驟三上一步、移除管理頁密碼鎖。
  - `SPEC/14_INTERIOR_RENOVATION_QUOTATION_SYSTEM_SPEC.md`：§1.4.4～1.4.6 與分空間品項摘要。
  - `SPEC/GLOSSARY_ZH.md`：定量、變量（估價語境）。
- **驗證**: 靜態檢閱邏輯；瀏覽器端請手動：計算分配 → 選方案 → 帶出明細 → 重選方案 → 產生摘要。

### 續

- 報價明細**數量欄**改為以 **0.5／整數** 為主：`step=0.5`、失焦四捨五入至 0.5 的倍數；畫面說明不建議 0.01 微調。
- **變更**: `tools/QuickRenovationQuote_v2.html`。

## 續 2：schema 4、管理員欄位與分區摘要

- **Time**: 2026-04-26
- **目的**: 完成遷移條件（`floor_p`+收邊列）、`fill`/`read` 地坪/收邊/細清/水電/試算表 id、gviz 讀入單價、表單套用 `rules`、收邊依 `cabWood` 小計×%、報價**依分區**小表、SPEC/詞庫紀錄。
- **變更檔案**:
  - `tools/QuickRenovationQuote_v2.html`：`loadSettings` 偵測 schema4；`mepPerRoom`→`mepPerZone` 相容；`fillFormMenuSelects`／`loadPricesFromGviz`／`applyFormRuleToScheme`；`renderQuote` 多段小表；地坪列排序 28.5/38.5/57.5+。
  - `SPEC/14_INTERIOR_RENOVATION_QUOTATION_SYSTEM_SPEC.md`：v0.6、§1.4.4 新 type 列、`1.4.7` 試作補充。
  - `SPEC/GLOSSARY_ZH.md`：gviz 一條。
- **驗證**: 未在環境內實測 gviz 網路讀表；以瀏覽器開本機 HTML 手測建議：還原預設→讀入→產生報價→確認分區表與收邊列。

### 續 5：菜單速寫（6 欄 | 轉成 JSON）

- **變更** `tools/QuickRenovationQuote_v2.html`：新增「速寫」`textarea`（`品項|規則|定值|比例|空間|邊`）、**合併/整包取代**、**轉成規則**／**從方案產生**；`buildMenuRuleObject` 共用在表單與速寫；`export` 固定六欄便於從表貼上。
- **文檔** `SPEC/14`：一句內帶到速寫。

### 續 4：方案菜單表單化（不必寫 JSON）

- **變更** `tools/QuickRenovationQuote_v2.html`：管理員「方案與菜單」改為**選方案→一覽表＋帶到下方＋套用**；新增／複製／刪除方案；`edge` 下拉；規則型別**白話選單**；`floor_115` 只存型別不寫壞多區；`adminSchemesJson` 收進**進階**摺疊；`儲存`前補欄位與表單名稱寫入；`只還原方案` 會 `fillAdmin`＋確認。

### 續 3：試算表為單價權威＋啟動自動同步

- **變更**: `tools/QuickRenovationQuote_v2.html` 新增 `priceSheetAutoOnOpen`（預設開）、`syncPricesFromGviz` 與頁首狀態列；開頁且已填表 ID 時向 gviz 拉表覆寫 `itemDefs` 單價；「計算空間分配」會等首輪同步完成再算。`SPEC/14` 補一句。

### 續 7：`~添心設計標準計價表.xlsx` 對價字串（依實表 A 欄）

- **依據**：GitHub `SPEC/~添心設計標準計價表.xlsx` 解析；衣櫃＝`開門高衣櫃-D60H240內`；鞋櫃／收納＝`開門高櫃-D40H240內`；移除不存在的分頁常數（系統櫃體／地板工程）。
- **變更**：`tools/QuickRenovationQuote_v2.html` 內建 `DEFAULTS.itemDefs` 之 `priceSheetTab`／`priceSheetItem`／`clientNote`（餐櫃、沙發背牆、置頂、書桌、有線電視、水電雜項等為表內無完全對應列之**暫對**，註在 `clientNote`）。

### 續 8：補齊對價＋`PRICE_SHEET_GAP_NOTES`（找不到／近似列）

- **變更**：`wardrobe2`、`trim_molding`（壁紙收邊條參考）、`touchup_clean`（油漆細清後修補參考）、`mep_misc`（保護工程粗步清潔）、`mep_tv` 改「一孔電視」、餐櫃改「開門中高櫃-D60H180內」；程式內集中註解 `PRICE_SHEET_GAP_NOTES`。
- **文檔**：`SPEC/14` §1.4.5（四）一句指到 `PRICE_SHEET_GAP_NOTES`。
