# 開發日誌 2026-03-28

## 2026-03-28（時間依本機）

**Diff：** 在 `backend` 根目錄新增 `.cursorignore`，將 `CheckinSystem/` 排除於 Cursor 語意索引。

**Tech：** 新增檔案 `D:\Dropbox\CodeBackups\backend\.cursorignore`；內容為註解 + `CheckinSystem/` 規則。

**Verify：** 成功；PowerShell `Set-Content` 寫入，檔案可讀。

## 2026-03-28（更正）

**Diff：** Checkin 相關 Cursor 忽略規則應在 `backend/CheckinSystem/.cursorignore`，不應在 `backend/.cursorignore` 排除整個 `CheckinSystem/`。已刪除 `backend/.cursorignore`，並新增 `CheckinSystem/.cursorignore`；Checkin 日誌改寫入 `CheckinSystem/LOG/2026-03-28_LOG.md`。

**Tech：** 同上。

**Verify：** 已執行。

## 2026-03-28（Budget 工具）

**Diff：** `BudgetWeb_Standalone.html` 依案號將分區取消與移除工項（指紋）寫入 `localStorage`，下次同案號「開始精煉解析」時自動套用；`toggleSection`／`removeItem`／`forceParse` 會同步更新記憶。`BudgetAuditor_Standalone.html` 的 `renderList` 於各分區內將有 `audit_remark` 的工項排序至最上方，已完成區亦同。

**Tech：** `CODING/tools/BudgetWeb_Standalone.html`（`PARSER_PREFS_KEY_PREFIX`、`applyParserPrefs`、`saveParserPrefs`、`propagateZonesFromHeaders`、`itemFingerprint`）；`CODING/tools/BudgetAuditor_Standalone.html`（`sortItemsWithRemarkFirst`）。

**Verify：** 靜態檢視邏輯無語法斷裂；建議以同案號重複載入 Excel 手動驗證記憶套用與驗收表排序。

## 2026-03-28（BudgetWeb 解析記憶補強）

**Diff：** 分區取消改以「分區列 raw + 分頁名」鍵 `cancelledHeaderKeys` 儲存，避免同名分區（如兩個「客廳」）互相誤傷；舊版僅有 `cancelledZones` 的資料仍會以區名向後相容套用。新增「清除本機解析記憶」按鈕，刪除該案號之 localStorage 解析偏好。

**Tech：** `BudgetWeb_Standalone.html` — `normalizeHeaderRaw`、`headerCancelKey`、`sheet_source` 寫入 header 列、`clearParserPrefsForProject`。

**Verify：** 程式面已接好；若兩個分區列 Excel 原文完全相同，仍會一併取消（同指紋）。

## 2026-03-28（人員排程計劃書歸檔）

**Diff：** 將 Cursor 計劃檔「木作系統排程網頁計劃」複製至 `CODING/SPEC/人員排程網頁計劃書.md`；YAML 增加 `sourceNote`；內文連結改為相對路徑（`../PROJECT_DATA_DICTIONARY.md`、`../../backend/CheckinSystem/...`、`../tools/BudgetAuditor_Standalone.html`）；修正案號 regex 小節之 markdown 與編號。

**Tech：** `D:\Dropbox\CodeBackups\CODING\SPEC\人員排程網頁計劃書.md`

**Verify：** 檔案已存在且可開啟。
