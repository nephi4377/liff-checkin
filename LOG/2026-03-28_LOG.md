# 開發日誌 2026-03-28

## 2026-03-28（時間依本機）

**Diff：** 在 `backend` 根目錄新增 `.cursorignore`，將 `CheckinSystem/` 排除於 Cursor 語意索引。

**Tech：** 新增檔案 `D:\Dropbox\CodeBackups\backend\.cursorignore`；內容為註解 + `CheckinSystem/` 規則。

**Verify：** 成功；PowerShell `Set-Content` 寫入，檔案可讀。

## 2026-03-28（更正）

**Diff：** Checkin 相關 Cursor 忽略規則應在 `backend/CheckinSystem/.cursorignore`，不應在 `backend/.cursorignore` 排除整個 `CheckinSystem/`。已刪除 `backend/.cursorignore`，並新增 `CheckinSystem/.cursorignore`；Checkin 日誌改寫入 `CheckinSystem/LOG/2026-03-28_LOG.md`。

**Tech：** 同上。

**Verify：** 已執行。
