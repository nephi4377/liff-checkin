# 2026-08-30 巡檢（客人）｜收款失敗當空回歸

## 目的（人話）

8/29 已合過：客人開「收款確認」，讀失敗要說「這不代表沒有收款」並可再試。後來合併綁定／選材修正時，這段從畫面消失。客人又會看到「—」或「目前無待確認收款」，以為沒帳單。

本輪把失敗／真的沒收款／載入中分開；開頁身分失敗也改人話＋再試，不再只停在「載入失敗」。

## 改了哪些檔

- `modules/accounting/customer-finance-portal.html`：還原收款失敗人話＋再試；開頁身分失敗同款；本機 `mock=fail`／`empty`／`recbootfail`
- `SPEC/18_會計與主檔快取策略.md`、`SPEC/21.案件毛利擴充與薪資連動.MD`（對齊畫面）
- `TOS/巡檢回報.md`

未改 GAS、未改商業規則。快取「只在成功才寫」仍在 `accounting_list_cache.js`。

## 本機怎麼看

頁面：`modules/accounting/customer-finance-portal.html`（僅 `127.0.0.1`／`localhost` 吃 `mock`）

| 模擬 | 網址 |
|------|------|
| 收款列表讀失敗 | `?mock=fail` → 看到「這不代表沒有收款」與再試，不可出現「目前無待確認收款」 |
| 真的沒收款 | `?mock=empty` → 「目前無待確認收款」 |
| 開頁身分失敗 | `?mock=recbootfail` → 人話＋再試／重新登入 LINE |

## 部署

雲端無法跑 `upload.bat`、不能自行合 `main`。合併後才算正式站。
