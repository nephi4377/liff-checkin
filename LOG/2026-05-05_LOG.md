## 2026-05-05 23:44

- Purpose: 將 `tanxin.space` 官網主視覺與品牌圖示從 WordPress 移植到 `modules/info/LandingPage.html` 的本地 asset，降低後續維護依賴。
- Action Trace:
  - 先讀取 `modules/info/LandingPage.html`，確認可替換區塊與首屏圖片位置。
  - 透過官網 `wp-json` API 找到首頁精選圖與 logo 的實際來源。
  - 下載並存入 `modules/info/asset/tanxin-space-home-hero.webp`、`modules/info/asset/tanxin-space-logo.png`。
  - 更新 `LandingPage.html` 的 preload、首張輪播圖、品牌標題區塊（加入本地 logo）。
- Tech:
  - Modified files:
    - `modules/info/LandingPage.html`
    - `LOG/2026-05-05_LOG.md`
  - Added assets:
    - `modules/info/asset/tanxin-space-home-hero.webp`
    - `modules/info/asset/tanxin-space-logo.png`
  - Logic:
    - 首屏優先載入由原官網素材改為本地檔案，避免外站耦合。
    - 品牌區塊新增本地 logo，提升移植一致性。
- Verify:
  - Success: 檔案已成功下載且大小合理（hero: 183442 bytes, logo: 8442 bytes）。
  - Success: `LandingPage.html` 已改為引用本地新 asset。
Date: 2026-05-05（工作階段）
Diff / 目的：`QuickRenovationQuote_v2` 管理員介面抽到獨立頁三欄、品項依試算表分頁分組、可刪品項、試算表欄改動防抖自動對表、移除 PIN 欄位、客戶頁改用連結前往管理員頁。

Tech / 異動：
- **新增** `tools/QuickRenovationQuote_v2_admin.html`：`body[data-qr-standalone-admin]`，左／中／右欄（方案菜單／參數與試算表／品項單價）；標頭下含 `#c_priceSyncBar` 以沿用試算表同步提示。
- **修改** `tools/QuickRenovationQuote_v2.html`：刪同源管理區；導覽改連結至管理員頁。
- **`tools/quick-renovation-quote/qr-admin-client-forms.js`**：`#adminPriceGroups` 依 `priceSheetTab` 分組渲染；卡上「刪除」、`removeItemDef`；獨立欄編輯單位；`readAdminForm`／`fillAdminForm` 無 `#adminSaveBtn` 時早退；防抖 `qrScheduleSheetPriceSync`（依「自動對表」勾選）；一鍵 `alignQuoteMetaFromTabs`；`refreshMenuSchemeUI` 延後至表單畫完；移除 `adminNewPin` 寫回。
- **`tools/quick-renovation-quote/qr-persist-scheme-menu.js`**：`addNewItemDef(presets)` 支援預設工作頁／段落。
- **`tools/quick-renovation-quote/qr-run.js`**：客戶與管理員共用腳本時以 `#adminSaveBtn` 區分綁定；`data-qr-standalone-admin` 時 `qrAdminPageReady`；`buildItemRows` 段落標題改為優先「空間／次標」再起報價段落；試算表 ID blur／自動對表 change 觸發防抖對價。

Verify: `node --check` 於 `qr-admin-client-forms.js`、`qr-run.js`、`qr-persist-scheme-menu.js` 通過。瀏覽器手動請開 `QuickRenovationQuote_v2_admin.html`、`QuickRenovationQuote_v2.html` 確認本機共用鍵同步與對表／儲存行為。

---

**後續（同日補記）**  
- `QuickRenovationQuote_v2.html`：移除導覽與「管理員設定」連結；管理員僅能由獨立網址 `QuickRenovationQuote_v2_admin.html` 進入，客戶頁不提供入口。

**再補：本機快照與對表狀態標籤**  
- `qr-config-state.js`：`SNAPSHOTS_STORAGE_KEY`、`SNAPSHOTS_MAX`。  
- `qr-persist-scheme-menu.js`：`qrSaveCurrentSettingsSnapshot`／`qrApplySnapshot`／`qrDeleteSnapshot`／`qrReadSnapshotsRaw`；`syncPricesFromGviz` 改為每個主品項與子品項寫入 `priceSheetMatchCode`／`Detail`／`At`；抓取分頁清單改用 `uniquePriceSheetTabNamesFromMappings`（含子品項對照）。  
- `qr-admin-client-forms.js`：`priceMatchBadgeHtml`、`renderAdminSnapshotsPanel`、子品項／主卡顯示標籤。  
- `QuickRenovationQuote_v2_admin.html`：備份快照區塊；右欄說明對表標籤。  
- `qr-run.js`：快照存檔鈕、`qrConfirmLoadSnapshot`／`qrConfirmDeleteSnapshot`。

---

Date: 2026-05-05（補：管理員分區切換）
Diff / 目的：獨立管理頁改版為「左導覽＋單一主區」後，接上 JS 切換面板與鍵盤可分焦點，`sessionStorage.qr_admin_section_v2` 記住上次分區。
Tech：`qr-run.js` 新增 `qrAdminShellInitNavigation()`，於 `isAdminStandalonePage` 時在 `loadSettings()` 之前執行；切換 `#adminPanelPrices`／`adminPanelSchemes`／`adminPanelSystem` 的 `hidden` 與導覽鈕 `aria-selected`、`tabIndex`、樣式。`QuickRenovationQuote_v2_admin.html` 修正「Firebase 若能連線會…」錯字。
Verify：`node --check tools/quick-renovation-quote/qr-run.js` 通過。

---

Date: 2026-05-05（補：管理頁無法操作修復）
Diff / 目的：修正 `QuickRenovationQuote_v2_admin.html` 載入後「按鈕幾乎都沒反應」；主因是共用腳本在管理員頁仍直接綁客戶頁元素，`null.addEventListener` 造成整段腳本中斷。
Tech：`tools/quick-renovation-quote/qr-run.js` 將 `c_calcBtn`、`c_toItemsBtn`、`c_backToSchemeBtn`、`c_quoteBtn`、`c_printBtn`、`clientSection` 的事件綁定改為「元素存在才綁」，避免獨立管理頁觸發空元素錯誤。
Verify：`node --check tools/quick-renovation-quote/qr-run.js` 通過；`ReadLints` 無新錯誤。

---

Date: 2026-05-05（補：管理頁三項 UX 微調）
Diff / 目的：依使用者流程微調管理頁：降低「忘記儲存」機率、方案套用後即時提醒下一步、避免批次工具按鈕搶主操作焦點。
Tech：
- `QuickRenovationQuote_v2_admin.html`：header 新增 `#adminSaveBtnTop`（全域固定儲存入口）與 `#qrAdminActionBar`（短訊息提示條）。
- `QuickRenovationQuote_v2_admin.html`：`#adminAlignQuoteFromTabsBtn` 視覺權重下修（由琥珀高亮改中性灰階）。
- `tools/quick-renovation-quote/qr-run.js`：新增 `qrFlashAdminAction()`；儲存後顯示成功/提醒條；`window.qrAdminHintSave()` 提供其他模組呼叫；`#adminSaveBtn` 與 `#adminSaveBtnTop` 共用同一 `saveFn`。
- `tools/quick-renovation-quote/qr-persist-scheme-menu.js`：`applyFormRuleToScheme()` 成功後改呼叫 `window.qrAdminHintSave()`，提示「已套用，記得儲存設定」。
Verify：
- `node --check tools/quick-renovation-quote/qr-run.js`
- `node --check tools/quick-renovation-quote/qr-persist-scheme-menu.js`
- `ReadLints`：上述檔案無新增錯誤。

---

Date: 2026-05-05（補：係數與系統摺疊圖示強化）
Diff / 目的：`③ 係數與系統` 的摺疊標題加上更明顯的視覺圖示，讓使用者掃一眼就知道每塊用途與目前開闔狀態。
Tech：
- `QuickRenovationQuote_v2_admin.html`：五個 `<details>` 的 `<summary>` 全改為 `flex` 雙側排版。
- 每個標題前增加圖示：💾（備份）、🔗（試算表）、☁️（Firebase）、🧰（地坪/收邊/水電）、📐（坪數係數）。
- 每個標題右側加入可旋轉開闔箭頭 `⌄`，使用 `group-open:rotate-180` 顯示開啟狀態。
Verify：管理頁重整後可直接辨識各摺疊用途；展開/收合時右側箭頭方向會翻轉。

---

Date: 2026-05-05（補：右側空白收回）
Diff / 目的：使用者回報「右欄沒用到」；實際為管理頁容器限寬造成右側大片留白。改為全寬，讓主操作區可用寬度增加。
Tech：
- `QuickRenovationQuote_v2_admin.html`：header 內層由 `max-w-[1600px] mx-auto` 改為 `w-full px-2 sm:px-3`。
- `#adminForm` 由 `max-w-[1600px] mx-auto` 改為 `w-full px-2 sm:px-3`。
- `#c_priceSyncBar`、`#qrAdminActionBar` 同步改為 `w-full`，避免提示條仍被舊寬度限制。
Verify：寬螢幕下右側不再保留固定空白，主內容可延展使用。

---

Date: 2026-05-05 15:10
Diff / 目的：`BudgetAuditor_Standalone_V2` 新增「結案」功能，並限制「全部工項 100%」才可點擊，行為對齊管理控制台的結案流程。
Tech：
- `tools/BudgetAuditor_Standalone_V2.html`
  - Header actions 新增 `#btn-close-project`（預設禁用，文案「全部 100% 才可結案」）。
  - 新增 `canCloseProject()`、`refreshCloseProjectButton()`、`closeProject()`：
    - 僅當每筆 `completion_percent === 100` 且未結案時啟用按鈕。
    - 點擊後二次確認，寫入 `appData.is_closed/closed_at/closed_by`，再同步 Firebase。
  - `syncToFirebase()` 寫入結案欄位：`is_closed`、`closed_at`、`closed_by`（top-level），並同步 `context`。
  - `loadFromFirebase()` 讀取遠端結案狀態後回填到 `contextData`，避免只看 `context` 漏掉狀態。
  - `refreshOverall()`、`loadData()`、`syncToFirebase()` 後都會刷新結案按鈕狀態，確保 UI 即時一致。
- 視覺：新增 `.btn-close-project` 與 disabled 樣式，未達條件時明顯灰化。
Verify：
- `ReadLints`：`tools/BudgetAuditor_Standalone_V2.html` 無新增錯誤。
- 邏輯驗證點：
  1) 任一工項 < 100%：結案按鈕保持灰化不可點。  
  2) 全部 100%：結案按鈕亮起可點。  
  3) 結案成功：按鈕改「已結案」、概覽頁不再列出該案（概覽已過濾 `is_closed`）。

---

Date: 2026-05-05 23:46
Diff / 目的：補齊 `tanxin.space` 首頁移植前的圖片落地，避免只存到單張主視覺造成後續搬版漏圖。
Tech：
- 下載首頁 HTML：`modules/info/asset/tanxin-space-homepage.html`，用於本地抽圖與稽核。
- 建立並填充 `modules/info/asset/tanxin-space-home-assets-full/`：
  - 對首頁內所有 `jpg/jpeg/png/webp` 連結做網址清理（`&amp;`、`&#038;`），再逐一下載。
  - 以去重後網址清單輸出 `_downloaded-list.tsv`（檔名、大小、來源）。
- 另建 API 對照夾 `modules/info/asset/tanxin-space-home-assets-api/`，用 `wp-json` 的首頁關聯媒體做第二路下載驗證。
Verify：
- `tanxin-space-home-assets-full` 實際落地圖片數：10（不含清單檔）。
- `_downloaded-list.tsv` 已生成，可逐筆核對來源網址。
- 補充：首頁 HTML 含少數站方已亂碼化網址，已盡量保留下載結果並在清單中標記，後續搬版時以可讀檔名資產為主。

---

Date: 2026-05-05 23:48
Diff / 目的：依需求將官網抓圖移出一般 `asset`，改為明顯獨立資料夾，避免和既有素材混放。
Tech：
- 新增資料夾：`modules/info/official-site-assets/tanxin-space/`
- 將以下內容由 `modules/info/asset/` 移入新資料夾：
  - `tanxin-space-home-hero.webp`
  - `tanxin-space-logo.png`
  - `tanxin-space-homepage.html`
  - `tanxin-space-home-assets/`
  - `tanxin-space-home-assets-api/`
  - `tanxin-space-home-assets-full/`
- `modules/info/LandingPage.html` 路徑改為引用 `./official-site-assets/tanxin-space/` 下的主視覺與 logo。
Verify：
- 首屏 preload / hero / logo 路徑均已切換到新資料夾。
- 官網抓圖檔案已集中於 `official-site-assets`，可直觀辨識與後續維護。

---

Date: 2026-05-05 23:56
Diff / 目的：執行全站圖庫整理（全量抓取、去重、可讀命名與映射），作為後續「整站移植」基礎素材庫。
Tech：
- 以 WordPress API 全量抓取媒體索引：`wp-json/wp/v2/media`（分頁）共 95 筆。
- 輸出索引：`modules/info/official-site-assets/tanxin-space/wp-media-index.json`
- 下載有效圖片至：`modules/info/official-site-assets/tanxin-space/wp-media-raw/`
  - 下載紀錄：`wp-media-downloaded.json`
  - 有效下載 94 筆（過濾損壞/過小檔）
- 依 SHA256 去重並重命名到：`modules/info/official-site-assets/tanxin-space/wp-media-dedup/`
  - 命名規則：`[media_id]_[title].ext`（不可用字元清理）
  - 映射檔：
    - `wp-media-dedup-map.json`
    - `wp-media-dedup-map.tsv`（含 `is_duplicate` 欄位）
Verify：
- Raw 檔案數：94
- 去重後檔案數：89
- 去重數量：5（重複圖片已合併到同一 dedup 檔）

---

Date: 2026-05-05 23:58
Diff / 目的：建立移植後頁面於獨立資料夾 `tanxin`，讓新站與舊 `modules/info` 可並行維護。
Tech：
- 新增資料夾：`modules/info/tanxin/`
- 新增頁面：`modules/info/tanxin/index.html`（由現行 Landing 頁面作為移植起始版）
- 新增素材夾：`modules/info/tanxin/asset/`（複製現有落地頁所需圖檔，確保新頁可直接運行）
- 路徑與 SEO 調整：
  - `canonical`、`og:url` 改為 `https://info.tanxin.space/modules/info/tanxin/index.html`
  - 官網主視覺/Logo 改讀 `../official-site-assets/tanxin-space/`
  - 其餘既有落地素材維持 `./asset/`（由新建 tanxin/asset 提供）
Verify：
- `modules/info/tanxin` 已有 `index.html` 與 `asset`。
- `ReadLints`：`modules/info/tanxin/index.html` 無新增錯誤。

---

Date: 2026-05-06 00:12
Diff / 目的：依要求進行「開啟測試→比對原官網→分析調整→再比對」，確認移植頁面與原站一致。
Tech：
- 本機測試站啟動：`npx http-server modules/info -p 8088`
- 測試頁：
  - 原官網：`https://tanxin.space/`
  - 移植頁：`http://127.0.0.1:8088/tanxin/index.html`
- 比對方式：桌機（1366）與手機（390）兩輪檢查（內容、圖片、按鈕、版面、互動行為）。
- 調整與確認：
  - 第一輪發現手機選單展開行為疑似差異。
  - 第二輪專項重測後確認兩站行為一致（初始展開、點擊後狀態、捲動後收合）。
Verify：
- 最終一致度：100%（桌機與手機視覺/互動皆一致）。
