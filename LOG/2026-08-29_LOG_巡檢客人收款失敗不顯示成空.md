# 2026-08-29 客人「我的裝修案」收款紀錄：失敗不顯示成空

## Diff／目的

客人從 LINE 開收款確認時，第一次向後端要收款列表失敗，舊程式只在上方紅字、列表停在「—」，沒有再試；失敗回應還會寫進 7 天快取，之後再開仍像沒資料。人會以為沒有收款，其實是連線失敗。

## 技術

- `shared/js/accounting_list_cache.js`：`isUsablePortalPayload`；只有 `success` 才寫客人收款快取；失敗快取略過重抓
- `modules/accounting/customer-finance-portal.html`：收款載入中／真的沒有／失敗分開；失敗人話＋再試；有舊列表就留；本機 `mock=fail`／`mock=empty`
- SPEC：`18_會計與主檔快取策略.md`、`21.案件毛利擴充與薪資連動.MD`

未改商業規則、未改 GAS。雲端無法跑 `upload.bat`／合進 `main`，正式站需合併後才算上線。

## 驗證

- `node --check` 通過
- 快取單元：失敗不寫、成功才寫、舊失敗快取會重抓
- 本機 `mock=fail` 要看到「這不代表沒有收款」＋再試；`mock=empty` 才寫目前無待確認收款
