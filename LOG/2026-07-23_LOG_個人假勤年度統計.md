# LOG — 個人假勤年度統計（Slice 2～4）

**日期**：2026-07-23  
**Diff／目的**：個人頁「年度統計」完整 MVP：曆年快取、特休給額試算、逐筆明細、各月小計、夜間重算、年底處置欄位預留。

## 後端（CheckinSystem）

- `Logic_LeaveYearStats.js` — 給額（勞基法第38條 pro-rata）、`annualLeaveDetailsJson`、`monthlyJson`、`yearEndDisposition*`、擴充 API row
- `EmployeeLogic.js` — 員工主檔欄位「特休給額覆寫」
- `ScheduledTasks.js` — `nightlyLeaveYearStatsRecalc_` 每日 06:00；`setupScheduledTriggers()` 註冊

## 前端（CODING）

- `modules/attendance/leave_year_stats_panel.js` — 新檔
- `modules/attendance/attendance_report.html` — 個人第 4 分頁「年度統計」

## SPEC

- `SPEC/17_個人薪資出勤頁計劃書.md` §4.8 更新
- `SPEC/專案全域資料字典.md` §6.1 擴充
- `SPEC/未來開發事項.md` — 個人假勤全覽改 MVP 完成

## 部署後必做

1. 後端 push → GAS 部署綠燈
2. GAS 執行一次 `setupScheduledTriggers()`（註冊 06:00 觸發器）
3. 個人頁 HUB → 我的出勤與假勤 →「年度統計」驗收

## Phase B（未做）

- 年底處置選擇 UI（12/15 截止）
- 折現串薪資發放（會計發薪明細）

## 追加（同日）

- 後端 API 折現試算欄位（§4.8.3d）；假勤核准事件重算 ✅
- `leave_year_stats_panel.js` 年底處置區顯示未休天數與折現試算

## 部署前驗收清單（給使用者）

1. **你先部署後端**：`backend/upload.bat` → 等 Actions 綠燈（本輪 agent 未代跑）
2. **GAS 執行一次** `setupScheduledTriggers()`（註冊 06:00 夜間重算）
3. **測 API**：`leave_year_stats&mode=mine&year=2026&operatorId=<你的UID>` 應有 `success:true` 與 `row`
4. **開個人頁**：HUB → 我的出勤與假勤 →「年度統計」→ 按重新整理，看特休／各月／折現試算
5. **對照管理端**（權限≥5）：出勤儀表板 →「假勤年度統計」，同一人同年度數字應一致

## 權限閘門（2026-07-23 追加）

- 個人「年度統計」僅 **perm 4 或 5** 可見；後端 `mine`／`refresh` 同門檻
- `attendance_report.html`、`Logic_LeaveYearStats.js`
- perm 6+ 不可見（刻意 `===4 || ===5`，見 backend LOG）

## 部署（2026-07-23 10:46）

- commit `d976e14`（v26.07.01.1）；備份 `BAK/CODING_20260723_1046_TX34`
- 後端 commit `637a2e2`；Actions 綠燈待確認
