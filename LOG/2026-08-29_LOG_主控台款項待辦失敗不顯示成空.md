# 2026-08-29 主控台款項待辦：失敗不顯示成空

## Diff／目的

總監開主控台時，左側「款項待辦」若第一次向會計後端要列表失敗，舊程式仍把空清單寫進當日快取，畫面變成「目前沒有待辦款項」，也沒有再試。人會以為今天沒單，其實是連線失敗。

## 技術

- `spa/hubPaymentTodos.js`：成功才覆寫；失敗保留先前內容
- `spa/app.js`：待審／待匯改並行讀取；無快取先標更新中；失敗不寫日快取
- `spa/HubLeftSidebar.js`：失敗人話＋「再試一次」；空與失敗分開
- `index.html` 資源版號 `v26.08.29.1`
- SPEC：`19_HUB與會計全域身分傳承.md`、`18_會計與主檔快取策略.md`

未改商業規則、未改 GAS。雲端無法跑 `upload.bat`／合進 `main`，正式站需合併後才算上線。

## 驗證

- `node --check` 通過
- `mergeHubPaymentTodosFetch` 單元情境：成功覆寫、失敗不把空當沒事、有舊資料則保留
- 本地靜態站可開 `index.html`；完整 LINE 主控台需正式身分，本輪未登入抽測
