# 2026-06-30 CODING LOG — 待付款申請 OCR 分行、審核分攤同步

## 目的

- 待付款申請頁：分行名稱欄、OCR 帶入匯款四欄
- 請款審核頁：多案分攤編輯（與待付款申請同步）
- 廠商名冊／待匯款頁：UI 與中信 Big5 提示

## 技術

| 區塊 | 檔案 |
|------|------|
| 待付款 | `modules/accounting/payment_request.html` |
| 審核 | `modules/accounting/ledger_review.html` |
| 待匯款 | `modules/accounting/vendor_payment_finance.html` |
| 廠商 | `modules/accounting/vendors.html` |
| API | `shared/js/accounting_api.js`（v22 分攤核准） |
| 主檔 | `shared/js/accounting_master_data.js`（v14 銀行名稱） |

## 驗證（待測）

1. 待付款申請 OCR → 分行、帳號空白欄自動填入
2. 審核頁可編輯多案分攤後核准
3. 待匯款匯出提示 BIG-5

## 部署

- `CODING/upload.bat` → `564a6d0` push main（v26.06.28.1242）
- 備份：`BAK/CODING_20260630_1253_TX34`

---

## 2026-06-30（午）— deploy-runbook 對齊

### 目的

- 規則改指向 `deploy-runbook` skill；刪舊 `.agents/workflows/deploy.md`
- 待付款廠商入口已含於 `564a6d0`（`initPaymentRequestSession`）

### 技術

- `.cursor/rules/CODING-upload-deploy.mdc`、`.agents/AGENTS_GUIDE.md`

### 部署

- 本輪若 `upload.bat`：僅文件／規則，無功能 diff

---

## 2026-06-30（晚）— 收支登錄 UX 與會計全模組載入審核

### 目的

- 收支登錄：付款方式標籤、廠商 datalist、申請人自動帶入、快取優先載入
- 會計各頁統一：bootstrap 快取先顯示、API 背景補齊

### 技術

| 區塊 | 檔案 |
|------|------|
| 收支登錄 | `modules/accounting/accounting_ingest.html` |
| 待付款申請 | `modules/accounting/payment_request.html` |
| 待匯款 | `modules/accounting/vendor_payment_finance.html` |
| 廠商名冊 | `modules/accounting/vendors.html` |
| 收款帳戶／附件 | `payees.html`、`attachments.html` |
| 共用 | `shared/js/accounting_cache.js`（v20 peekVendors）、`accounting_form_helpers.js`（新） |

### 驗證（待測）

1. 收支登錄：廠商建議、申請人唯讀、付款標籤、HUB 快取進頁即有案場
2. 待付款／待匯款：有名冊快取時登入後立刻可選廠商
3. 廠商名冊：有快取時不擋「載入中」等 bootstrap

### 部署

- 待 `upload.bat`

---

## 2026-06-30（晚②）— HUB iframe 會計 API 錯誤修復

### 問題

- Console：`Unexpected token '<'`（GAS 回 HTML 被當 JSON）
- `script.googleusercontent.com/macros/echo` 404（與會計靜態頁無直接關聯，多為 GAS 部署 HTML 含失效 library）

### 修正

| 檔案 | 內容 |
|------|------|
| `shared/js/accounting_api.js` | 安全 JSON 解析；HUB 父層 LIFF token postMessage；`hub_liff_id` fallback |
| `shared/js/accounting_ui.js` | 選單連結保留 `uid`／`permission` query |
| `spa/app.js` | 回應 `request_hub_liff_token`；iframe 帶 `hub_liff_id` |
| `modules/accounting/vendors.html` | `vendorListFiles` 加 `.catch` 避免未處理 rejection |

---

## 2026-06-30（晚③）— 會計模組啟動收斂（整包部署前）

### 目的

- 全站統一 `OperatorContext`、`AccountingNav`、`AccountingBoot`，HUB 切頁保留 `uid`／`permission`
- 廠商附件改按鈕 lazy 載入（取消 N+1 `vendorListFiles`）
- **整包完成後再一次 `upload.bat`**（使用者要求）

### 技術

| 區塊 | 檔案 |
|------|------|
| 操作者 | `shared/js/operator_context.js`（`tanxin_operator_v1`） |
| 導覽 | `shared/js/accounting_nav.js` |
| 啟動 | `shared/js/accounting_boot.js` |
| API | `shared/js/accounting_api.js` v23（安全 JSON、HUB token、initPaymentRequestSession） |
| UI | `shared/js/accounting_ui.js` v7 |
| 快取 | `shared/js/accounting_cache.js` v20（peekBootstrap／Vendors／Payees） |
| HUB | `spa/app.js`（`request_hub_liff_token`、`hub_liff_id`） |
| 全頁 | `modules/accounting/*.html`（除舊 `payment_request_compose.html`） |

### 驗證（部署後）

1. HUB `#/accounting` → 各子頁 URL 仍帶 `uid=`、`permission=`
2. Console 無 `Unexpected token '<'`（API 錯誤改顯示可讀訊息）
3. 廠商名冊：列表快取先顯示；附件須按「載入附件列表」
4. 收支登錄：廠商建議、申請人、付款標籤
5. 待付款申請：廠商／員工入口、OCR 仍正常

### 部署

- 待使用者確認後執行 `CODING/upload.bat`
- 已知：`macros/echo` 404 為 GAS 部署／library 問題，非本輪靜態頁可修

---

## 2026-06-30（晚④）— deploy-runbook 整包上線

### 目的

- **薪資核對**：勞基法試算 UI（缺勤比例、遲早扣款、單次打卡暫不扣提示）
- **會計模組**：OperatorContext／AccountingBoot 全頁收斂（晚③）
- **HUB iframe**：accounting API 安全 JSON、token postMessage（晚②）

### 技術

| 區塊 | 檔案 |
|------|------|
| 薪資 | `modules/attendance/payroll_review_panel.js` |
| 會計全頁 | `modules/accounting/*.html`、`shared/js/accounting_*.js`、`operator_context.js` |
| HUB | `spa/app.js` |
| SPEC | `SPEC/17_個人薪資出勤頁計劃書.md` §B1 |

### 驗證（部署後）

1. 個人頁薪資核對：加項／減項明細、單次打卡暫扣提示
2. HUB 會計子頁：uid／permission 保留、廠商快取先顯示
3. 與 backend 薪資 API 欄位 `lateMinutesPayroll` 一致（需 backend 先或同輪部署）

### 部署

- `CODING/upload.bat` → `6f4a683` push main（v26.06.28.1242）
- 備份：`BAK/CODING_20260630_1739_TX34`
- 順序：backend `628fd01` 先、CODING 後

### 部署狀態檢查（2026-06-30 晚⑤）

- deploy-runbook Step 0：`HEAD`＝`6f4a683`（含 `payment_request.html` 匯費預覽）；與 origin 同步
- **結論**：整包已上線，**不重複部署**
- deploy-runbook skill 已加 **Step 0 部署狀態檢查**

---

## 2026-06-30（晚⑥）— HUB iframe `hubLiffId` 修復

### 問題

- 開 `#/employee-editor` 等 iframe 頁：`Cannot read properties of undefined (reading 'HUB_LIFF_ID')`
- 原因：`6f4a683` 模板使用 `CONFIG.HUB_LIFF_ID`，但 Vue `setup()` 未暴露給模板

### 修正

| 檔案 | 內容 |
|------|------|
| `spa/app.js` | `return { hubLiffId: CONFIG.HUB_LIFF_ID }`；模板改 `hubLiffId` |

### 驗證（部署後）

1. HUB → 員工資料編輯：頁面可開、無 Console TypeError
2. 其他 iframe 子頁（出勤、會計）仍帶 `hub_liff_id` query

### 部署

- `CODING/upload.bat` → `df18c03` push main（v26.06.28.1242）
- 備份：`BAK/CODING_20260701_0007_NEPHI筆電001`
