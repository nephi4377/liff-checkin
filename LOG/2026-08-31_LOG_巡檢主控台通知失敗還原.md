# 2026-08-31 巡檢（總監）｜主控台通知失敗不可看起來已辦完

## 目的（人話）

總監在主控台對智慧通知按「回覆／標完成／清除」時，若這次沒送到，那則通知要回到畫面上，並說還沒記下。不能先消失讓人以為已經辦完。

## 改了哪些檔

- `spa/hubNotificationActions.js`：拿掉／還原清單
- `spa/app.js`：失敗還原後再提示
- `index.html`：前端版號
- `SPEC/18_會計與主檔快取策略.md`、`SPEC/專有名詞白話對照.md`
- `TOS/巡檢回報.md`

未改 GAS、未改商業規則（誰該收到、什麼算完成不變）。

## 本機怎麼看

```text
node --test spa/hubNotificationActions.test.mjs
```

主控台：展開智慧通知 → 按標完成或回覆但連線失敗 → 該則還在，並提示還沒記下。

## 部署

雲端無法跑 `upload.bat`、不能自行合 `main`。合併後才算正式上線。
