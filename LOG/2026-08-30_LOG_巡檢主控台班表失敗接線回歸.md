# 2026-08-30 巡檢：主控台班表失敗接線回歸

## Diff／目的

總監開主控台看「人員出席」。8/29 已做過「班表讀失敗不顯示成沒人休假」（PR #24），畫面與輔助函式都在；後來合併專案看板／款項待辦時，`spa/app.js` 把失敗狀態與「再試一次」接線蓋掉。結果：讀失敗時人仍看到「目前沒有排班制員工資料」，或空班表被畫成大家都上班。

## 技術

- `spa/app.js`：接回 `scheduleError`、`refreshScheduleInBackground`（失敗自動再試一次、不寫空快取）；Dashboard／右側今日燈號接到人話＋再試
- `index.html`：`v26.08.30.4`
- SPEC：`18_會計與主檔快取策略.md` 4.3 補「必須接到畫面」；白話對照加「人員出席」

未改排班規則、權限、資料表、LINE 選單。雲端無法 `upload.bat`；合 main 後 GitHub Pages 才算正式站。

## 驗證

- `node --check spa/app.js` 通過
- `mergeHubScheduleFetch`：失敗保留舊班表、不寫空；成功才覆寫
- `app.js` 模板含 `:scheduleError` 與 `@retry-schedule`
