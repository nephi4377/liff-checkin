# 2026-07-25｜案件毛利：只抓該案明細＋加速＋畫面精簡

## 問題

1. 開一案（如 782）明細把同百位分頁（`明細_700全部`）**整頁列**載進來。
2. 收支總結重複顯示同一差額多遍，感覺多餘且慢。
3. 本機 `MarginModule.js` 曾有「明細_X00＋依案號過濾」，後來被蓋回舊「一案子一分頁」讀法。

## 變更（本機，待部署）

### 後端 `accounting-gas/master/MarginModule.js`

- 還原合併分頁邏輯；優先 `明細_X00全部`，沒有才用 `明細_X00`
- 詳情／列表讀取改 `readMarginLinesForProject_`：**只回該案號列**
- 加速：附件 URL 改點開再抓；請款清單同請求快取；客戶財務摘要不重複算

### 後端 `CustomerFinanceModule.js`

- 檢查清單可重用已算好的客戶財務摘要

### 前端 `project_margin.html`

- 三角核對保留；與三角重複的檢查列隱藏
- 通過／資訊列摺疊；有三角時隱藏下方重複摘要卡

## 驗收（部署後）

1. 開案件毛利 → 782：明細只見 782，不見 700～799 其他案
2. 分頁名優先 `明細_700全部`
3. 收支總結：上方三角＋少數紅項；其餘在「其他檢查」摺疊內
4. 開案等待應比先前短

## 部署

- 待使用者指示：`accounting-gas/deploy.bat` ＋ `CODING/upload.bat`

## 後續（同日）

- 寫入 API 改回整包詳情、前端不再重抓：見 2026-07-25_LOG_案件毛利整包回覆.md
