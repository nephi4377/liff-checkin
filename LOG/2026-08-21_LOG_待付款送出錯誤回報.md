# 2026-08-21｜待付款送出：真正錯誤被蓋掉＋錯誤回報三鍵

| 項目 | 內容 |
|------|------|
| 觸發 | 待付款按「送出請款」出現 `action is not a function`；使用者要一鍵複製、寄信、也能給 Cursor |
| 人怎麼走 | 失敗時看人話原因；可「複製錯誤」「開信寄出」；要給助理看就按「複製給 Cursor」再貼回對話 |
| 原因 | 送出超過約 60 秒被中斷後，狀態列把「動作名稱」誤當成函式呼叫，把真正的「連線逾時」蓋掉 |
| 變更 | 修正誤呼叫；送出改等較久；失敗列／紅提示加三鍵。SPEC 19 v1.10 |

未部署；需 CODING 前端發布後硬重新整理才看得到。

2026-08-22 已走 CODING `upload.bat` 發布，見 `LOG/2026-08-22_LOG_部署_待付款錯誤回報.md`。
