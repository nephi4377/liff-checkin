# 2026-07-25 — FB 發文工作室 PR-B：步驟①從案場匯入完工照（前端）

## Diff／目的

在發文工作室步驟①「選圖」旁新增「從案場匯入」：輸入案號 → 載入雲端完工照清單 → 多選縮圖 → 匯入既有圖庫（壓縮同本機上傳）。**不**部署、**不**commit、**不**改後端行為。

## 技術

| 區 | 檔案 |
|----|------|
| UI | `tools/fb-post-studio/index.html`（案號／載入／縮圖格／匯入所選；設定可勾假資料） |
| 邏輯 | `tools/fb-post-studio/studio.js`（list API、四態、防連點、CORS 代理 fallback、假資料） |
| 設定 | `tools/fb-post-studio/config.js` → `COMPLETION_MEDIA` |
| SPEC | `SPEC/21_FB_POST_STUDIO_SPEC.md`（步驟①對照表／分期／驗收／風險） |

## 行為重點

- 呼叫 `list_project_completion_media`（`media_type=image`、`include_preview`）；影片選項 disabled 預留
- 匯入走既有 `resizeImageFile` → `addPhotos`／`state.images`
- 縮圖／位元組：`preview_url` 直連；CORS 失敗試 `IMAGE_CORS_PROXIES`
- 本機測：設定勾「案場匯入用假資料」或 `?mock_completion=1`
- 本機拖放上傳保留

## 後端建議（未改）

若正式環境 Dropbox `preview_url` 常被 CORS 擋死、公開代理又不穩：建議另加「依 path 取圖 → base64」小 action（認證同 FB 工作室），前端匯入改走該路徑。

## 部署

本輪**未部署**。真連案場需 Phase 0 後端（accounting-gas＋project-console）已上線。

## 手動驗（本機）

1. `npx --yes serve -l 3000 .`（CODING 根目錄）→ 開 `http://127.0.0.1:3000/tools/fb-post-studio/?mock_completion=1`
2. 步驟①：填案號（如 734）→ 載入 → 應見假縮圖；空案號應提示；勾選 → 匯入所選 → 圖庫增加
3. 本機拖放仍可用；關掉假資料後若後端未部署，載入應有人話錯誤＋引導開假資料
