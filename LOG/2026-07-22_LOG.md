# 2026-07-22 — UploadQueue Drive 快取防再犯＋救回已達上限

## 目的

資料夾快取失效導致「找不到指定 ID」→ 當場重建；主佇列檔案開失敗改進重試；救回 `處理失敗(已達上限)` 堆積。

## 對應

- backend `project-console`：見該模組 LOG
- SPEC：`施工回報_系統完整_SPEC.md` 失敗／重試與防再犯段落

---

## 2026-07-22 — 打卡案場快取背景更新（方案 A）

- **Diff**：`checkin.html` 開頁有快取也背景向後端要最新案場清單（stale-while-revalidate）；修正 GPS 預熱搶先開按鈕；背景失敗或 API 回空不覆寫舊快取。
- **Tech**：`v26.07.22.2354`；`refreshSitesInBackground`、`loadSites` 空陣列保護；SPEC `03_員工打卡系統規格書.md` §2.5。
- **Verify**：待部署後 LINE 開打卡頁；有快取時操作紀錄應見「案場快取就緒」→「案場背景更新」；案場燈號短暫「更新中」。
