# 2026-08-29 追加減與收款：失敗不顯示成空

## Diff／目的

設計師輪巡檢：開「追加減與收款管理」時，案件列表讀失敗會停在「載入中…」或只在紅字帶一句；待辦讀失敗則把區塊清成空白，看起來像沒有待辦。沒有再試按鈕。

未改追加減／收款狀態機、權限或試算表；只改載入狀態與再試。

## 技術

- `modules/accounting/designer-customer-finance.html`：載入中／失敗／真的空分開；失敗保留先前名單；「重新載入最新」；本機 `?mock=fail`／`?mock=empty`（僅 127.0.0.1／localhost）
- `SPEC/21.案件毛利擴充與薪資連動.MD` B5.8、`SPEC/18_會計與主檔快取策略.md`、`SPEC/專有名詞白話對照.md`

未改 GAS。雲端無法跑 `upload.bat`／合進 `main`，正式站需合併後才算上線。

## 驗證

- 從 HTML 抽出頁內腳本，`node --check` 通過
- 本機 `mock=fail`：案件列表人話＋再試，不可出現「尚無案件」；待辦不可空白裝成沒事
- 本機 `mock=empty`：才寫「尚無案件」
- 完整 LINE 會計需正式身分，本輪未登入抽測

## 部署狀態

待合進 `main`（GitHub Pages）。本環境未跑 `upload.bat`。
