# 2026-09-01 — 巡檢 P0：主控台班表失敗再接到畫面

總監視角：開主控台看「誰今天休假」。畫面早就有「班表讀不到／再試一次」，但主程式後來被部署提交蓋掉，失敗沒傳進畫面。沒快取時會把大家都畫成「上班」，人以為沒人休。

## Diff／目的

把失敗再接到人員出席卡與右側今日燈號。自動再試一次；失敗不寫空快取；有舊資料就留著並標「讀不到」。

## 技術

- `spa/app.js`：接回 `hubSchedule.js`；`scheduleError` 傳進主畫面與右側燈號；失敗自動再試、可按再試
- `index.html`：版號 `v26.09.01.1`
- SPEC：`18_會計與主檔快取策略.md` 註明畫面必須接到失敗，不可只留 helper

未改排班規則、權限或資料表。Cloud Agent 無法跑 Windows `upload.bat`；合 main 後才算正式站。[PR #63](https://github.com/nephi4377/liff-checkin/pull/63)

## 驗證

- `node --check spa/app.js` 通過
- `mergeHubScheduleFetch`：失敗保留舊班表、不寫空快取；成功才覆寫
- 本機靜態站可開主控台殼；完整 LINE 身分本輪未登入抽測
