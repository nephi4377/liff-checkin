# 開發日誌 2026-04-23

## 2026-04-23（時刻以本機為準）
- **Diff 目的**: 主控台手機版頂部與案場資訊區壓縮版面。
- **Tech**: 修改 `modules/projects/managementconsole.html`（頂列結構、漢堡斷點、頂欄「複製案場資訊」、main.js 版本 bump）、`assets/css/style.css`（移除窄螢幕 body 大段 padding、頂列左內距、頂欄複製鈕／隱藏桌面範本列、案場二列樣式）、`modules/projects/js/ui.js`（案場精簡為地址＋導航列＋摺疊、範本鈕移入摺疊內窄螢幕用）、`modules/projects/js/main.js`（複製雙鈕綁定、範本點擊改 document 委派）。
- **Verify**: 待瀏覽器實機 ≤1024px 確認頂列高度、複製與範本、地圖連結與摺疊。

## 2026-04-23（續）
- **Diff 目的**: 案場資訊還原為「專案基本資料」直列格局；範本僅在「工程排程」空狀態顯示；手機案場卡加高與字級以利閱讀。
- **Tech**: `ui.js` `displayProjectInfo` 重構；`scheduleActions.js` `renderSchedulePage` 空排程卡片＋範本鈕；`managementconsole.html` 移除 `#actions-container`；`main.js` 移除頂列範本顯示邏輯、委派僅 `btn-import-new/old`；`style.css` 移除二列／摺疊案場樣式、調整 `.right-sidebar` 高度與手機字級、新增導航小連結樣式。
- **Verify**: 實機切「工程排程」於無任務時應見兩範本鈕；案場卡應見完整區塊與捲動。

## 2026-04-23（續 2）
- **Diff 目的**: 手機預設不顯示案場卡；側邊新增「專案基本資料」分頁切至右欄內容；頂列保留「複製案場資訊」。
- **Tech**: `managementconsole.html`（`app-main-row` 包 main＋aside、新 nav）、`style.css`（二欄 grid、`app-main-row` 寬螢幕 flex、≤1024 隱藏／顯示邏輯、寬螢幕隱藏專案分頁鈕）、`main.js` `setupNavigation`（`project-basic` 與 `app-main-row--mobile-project-basic`）。
- **Verify**: ≤1024 預設僅見日誌區；選「專案基本資料」應全幅案場卡可捲；≥1025 右欄常駐且側邊不出現該分頁。

## 2026-04-23（續 3）
- **Diff 目的**: 驗收表（BudgetAuditor）啟用 7 天內 localStorage 快取；先前 `loadFromCache` 未接上 `loadFromFirebase`。
- **Tech**: `tools/BudgetAuditor_Standalone.html`（`AUDIT_LOCAL_CACHE_TTL_MS`、`loadFromCache` 判斷、`loadFromFirebase` 先快取再雲端、雲端成功 `saveToCache`、失敗／無資料時若有快取則沿用）。
- **Verify**: 二次開同案驗收表應先出快取字樣再變雲端時間；離線僅快取時不彈致命 alert。

## 2026-04-23（續 4）
- **Diff 目的**: SPA 主控台與 `#/new-site` 路由標題改為「新增／修改案場資料」，與 `NewSiteForm` 可選既有案場更新之行為一致。
- **Tech**: `spa/app.js`（`routes['#/new-site'].title`）、`spa/Dashboard.js`（功能卡片標題與說明）、`modules/projects/NewSiteForm.html`（預設與 `resetFormToCreateMode` 之 `form-title`）。
- **Verify**: 主控台卡片與進入表單後預設標題應顯示「新增／修改」；選既有案場後仍顯示「更新案場資料 (#案號)」。
