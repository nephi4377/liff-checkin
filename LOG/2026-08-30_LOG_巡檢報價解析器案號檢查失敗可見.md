# 2026-08-30 報價單解析器：案號檢查失敗不卡在「檢查中」

## Diff／目的

設計師開主控台「報價單解析器」、填案號後，舊程式向雲端問「此案有沒有工項」若拋錯或一直沒回來，畫面停在「檢查中」、上傳區維持鎖住、錯誤只寫在主控台。人會以為系統當掉或案號無效。改成：逾時約 8 秒當失敗；人話＋再試；雲端沒連上仍可先解析本機 Excel。

## 技術

- `tools/BudgetWeb_Standalone.html`：`verifyProjectNo`；失敗／逾時寫入 `#projectVerifyStatus` 並提供 `#btnRetryVerify`；`fb_audit_config` 壞 JSON 不整頁掛掉
- SPEC：`12_報價單審核與主控台整合規格書.md` §2.3／§2.4、v3.3；`專有名詞白話對照.md` 補「報價單解析器」

未改商業規則、未改 GAS、未改權限。雲端無法跑 `upload.bat`；正式站需合進 `main`。

## 驗證

- 本機開解析器：填案號後若雲端失敗，不可維持「檢查中」；要看到「讀不到」與「再試」；上傳區可點
- 清空案號後上傳區應再鎖上
