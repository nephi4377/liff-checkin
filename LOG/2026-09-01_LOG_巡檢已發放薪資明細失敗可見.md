# 2026-09-01 巡檢（行政）｜已發放薪資明細失敗要看得見

## Diff／目的

員工在「我的出勤與假勤 → 薪資」看已發放明細時，第一次讀失敗會把錯誤寫進「尚無發放紀錄」那格（灰色空狀態），沒有分開、也沒有自動再試。再試成功但本期真的沒單時，還可能留下上一句失敗文字。

## 技術

- `modules/attendance/payroll_review_panel.js`：空／失敗分開；自動重試一次；人話＋再試；本機 `mock=payslip-fail`／`mock=payslip-empty`
- `modules/attendance/attendance_report.html`：失敗區塊與再試按鈕
- 沿用 `attendanceListFetch.js` 的成功／空／失敗分類

未改 GAS、未改商業規則（發薪金額與誰能看不變）。不上正式（Cloud 無法 upload.bat）。

## 驗證

```text
node --test modules/attendance/payslipHistoryLoad.test.mjs
```

| 情況 | 預期 |
|------|------|
| 讀失敗 | 紅字「讀不到」＋再試，不可出現「尚無發放紀錄」 |
| 真的沒單 | 才寫尚無發放紀錄 |
