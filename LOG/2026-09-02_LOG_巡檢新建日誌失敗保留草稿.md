# 2026-09-02｜專案工作區新建日誌失敗保留草稿

## Diff／目的

現場在專案工作區寫進度、附照片後按發佈。舊程式一送出就清空輸入，還沒等到後端。網路差失敗時文字與照片都沒了，半透明卡片還留著像已發出去。

## 技術

`modules/projects/js/logActions.js` `handleCreateNewPost`：成功才清空；失敗拿掉假卡片、人話可再按；按鈕等結果才恢復。

## 驗收

送出後未成功前，輸入框與照片預覽還在。模擬後端失敗：不可出現「已成功」；內容還在、可再按發佈。
