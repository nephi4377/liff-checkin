# 2026-08-14｜LINE 指令說明補測試選單

> **目的**：說明日後測客人底部大圖，用 `#測試選單`，不必每次重建。

## Diff／目的

指令說明頁加上 `#測試選單 員工｜四格｜六格`。SPEC 總覽同步。

## 驗證

- 說明頁看得到這三句怎麼打
- 實際切換要等後端部署

## 後續（2026-08-29）

後端 8/14 已認 `#測試選單`（只換傳訊者）。本機再補「權限不夠」回覆；**尚未部署**。見 `LOG/2026-08-29_LOG_測試選單切換.md`。
