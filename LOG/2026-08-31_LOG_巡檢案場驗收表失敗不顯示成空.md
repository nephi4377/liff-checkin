# 2026-08-31 CODING LOG — 巡檢 P0：案場驗收表載入誠實

## Diff／目的

設計師視角：主控台「案場驗收表」開頁讀 Firebase 失敗時，卡片區會一直停在骨架（像還在載、又像沒案子）；點進一案失敗時，畫面回到「輸入案號」且 toast 很快消失。改成載入／空／失敗分開，失敗自動再試一次，人話＋可再按。有舊快取就留著。

## 技術

- `tools/BudgetAuditor_Standalone_V2.html`：概覽失敗清掉骨架、寫「讀不到案場概覽」＋再試；真的 0 案才寫「目前沒有使用中的驗收案場」；開案失敗在輸入區寫「讀不到這案」；自動重試一次
- 本機：`?mock=dashboard_fail`／`dashboard_empty`／`project_fail`
- SPEC：`SPEC/12_報價單審核與主控台整合規格書.md` §7.3、§8 v2.8

未改：假勤政策、權限模型、資料表、LINE 選單、結案規則。

## 驗證

- `?mock=dashboard_fail`：要看到「讀不到」與再試，不可停在灰色骨架、不可寫成沒案子
- `?mock=dashboard_empty`：才寫「目前沒有使用中的驗收案場」
- `?mock=project_fail&projectNo=742`：輸入區要看到「讀不到這案」，不可只剩「輸入案號」
