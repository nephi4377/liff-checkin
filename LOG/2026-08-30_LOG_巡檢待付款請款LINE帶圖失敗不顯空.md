# 2026-08-30 CODING LOG — 巡檢 P0：待付款請款 LINE 帶圖失敗不裝成沒傳

## Diff／目的

會計視角：從 LINE `#請款` 開「待付款請款」時，帶入照片或辨識草稿若第一次失敗，錯誤被吞掉，檔案區維持空白，看起來像沒傳圖。改成自動再試一次、人話說明「這不代表你沒傳圖」、可再帶一次；仍可手動上傳。

## 技術

- `modules/accounting/payment_request.html`：`accounting_pending_photos`／辨識草稿失敗與空白分開；自動再試一次；錯誤區＋「再帶一次」
- SPEC：`SPEC/18_會計與主檔快取策略.md` 4.4 增列待付款請款 LINE 帶圖；`SPEC/15_會計系統模組規格書.md` 頁面表；`SPEC/專有名詞白話對照.md`「失敗≠空」

未改：假勤政策、權限模型、資料表、LINE 選單、金額規則。

## 驗證

- 讀碼：`loadPendingPhotos`／`loadOcrDraft` 不再 `.catch(function () {})` 吞掉失敗
- 人話驗收：LINE 帶圖進來讀失敗應看到「沒帶進來」與再帶一次，不可只剩空白檔案區；真的沒圖才維持空白＋人話
