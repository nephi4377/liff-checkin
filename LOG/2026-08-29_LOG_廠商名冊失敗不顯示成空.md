# 2026-08-29 廠商名冊：失敗不顯示成空

## Diff／目的

會計輪巡檢：開「廠商名冊」時，若本機沒有主檔、或快取裡廠商是空的，舊程式可能不向後端重抓，列表空白或寫成「沒有符合的廠商」。讀失敗時也沒有「再載一次」。人會以為名冊是空的。

未改廠商欄位、權限或請款規則；只改載入狀態與再試。

## 技術

- `modules/accounting/vendors.html`：無快取／快取廠商為空必重抓；載入中／失敗／真的空／篩選沒命中分開；失敗保留先前名單；「重新載入最新」
- `SPEC/18_會計與主檔快取策略.md`、`SPEC/專有名詞白話對照.md`

未改 GAS。雲端無法跑 `upload.bat`／合進 `main`，正式站需合併後才算上線。

## 驗證

- 從 HTML 抽出頁內腳本，`node --check` 通過
- 靜態頁含「重新載入最新」「載入失敗，請再按」「仍顯示先前內容」「目前沒有廠商」
- 本地靜態站可開頁面骨架；完整 LINE 會計需正式身分，本輪未登入抽測

## 部署狀態

待合進 `main`（GitHub Pages）。本環境未跑 `upload.bat`。
