## 2026-05-22（QuickRenovationQuote_v2 可用性與設定簡化）

- **目的**：客戶報價頁可走完四步；管理設定收斂為「初次設定一鍵」；避免雲端覆寫本機、內建計價表 ID。
- **異動**：
  - `tools/QuickRenovationQuote_v2.html`：簡化步驟①（固定大樓新屋）、頂部就緒提示、連結管理頁。
  - `tools/QuickRenovationQuote_v2_admin.html`：系統區新增「初次設定」卡片與 `#qrQuickSetupBtn`。
  - `qr-config-state.js`：預設 `priceSheetGvizId`（同 v1 計價表）。
  - `qr-persist-scheme-menu.js`：`qrShouldPreferRemoteOnLoad`、`qrAdminQuickSetup`、空 ID 時補預設表 ID。
  - `qr-admin-client-forms.js`：`qrGetClientReadiness`／`qrRenderClientSetupBanner`；`fillAdminForm` 支援 Top 儲存鈕與快速設定欄。
  - `qr-run.js`：本機有資料時不強制 Firebase 覆寫；報價前檢查至少勾一項；移除透天／舊屋攔截（UI 已拿掉）。
- **驗證**：`node --check` 三支 JS 通過。請瀏覽器開客戶頁與管理頁手測一輪報價流程。

## 2026-05-22（human-comfortable 客戶頁 UX）

- **目的**：減少猜測與彈窗；主按鈕明確、手機好點、錯誤欄位旁提示。
- **異動**：`QuickRenovationQuote_v2.html`（步驟 pill、主／次按鈕、手機底欄、進階收合）；`qr-admin-client-forms.js`（`qrClientToast`、`qrSetClientStep`、`qrBtnBusy`、`qrShowPingError`、方案卡片）；`qr-run.js`（客戶流程改 toast／欄位錯誤、按鈕忙碌態）；`showPriceSyncBar` 客戶頁改走 toast；管理頁儲存改橫條、少 `alert`。
