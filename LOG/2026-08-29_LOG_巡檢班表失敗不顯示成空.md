# 2026-08-29 巡檢：班表讀失敗不顯示成沒人休假

## Diff／目的

行政開主控台看「誰今天休假」、或開排班頁調班表時，第一次向後端要當月班表若失敗，舊程式會把空班表當成果：主控台把大家都畫成「上班」，排班頁出現空白日曆。人會以為這個月沒人休，其實只是連線失敗。

## 技術

- `spa/hubSchedule.js`：成功才覆寫；失敗保留先前內容
- `spa/app.js`：讀班表失敗自動再試一次；不寫空快取
- `spa/Dashboard.js`、`spa/StaffTodaySidebar.js`：失敗人話＋「再試一次」；有舊資料就留著
- `modules/attendance/shift_schedule.html`：失敗不把空日曆當沒排休；自動再試＋畫面上可再試
- `index.html` 資源版號 `v26.08.29.2`
- SPEC：`09_排班系統資料格式規格書.md`、`18_會計與主檔快取策略.md`、`19_HUB與會計全域身分傳承.md`

未改排班規則、權限或資料表。雲端無法跑 `upload.bat`／合進 `main`，正式站需合併 [PR #24](https://github.com/nephi4377/liff-checkin/pull/24) 後才算上線。

## 驗證

- `node --check` 通過
- `mergeHubScheduleFetch`：成功覆寫、失敗不把空當沒事、有舊資料則保留
- 排班頁本機可開；完整 LINE 主控台需正式身分，本輪未登入抽測
