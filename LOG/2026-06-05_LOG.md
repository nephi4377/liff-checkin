# 添心生產力助手 - 營運維護日誌 (LOG)

## 2026-06-05 — 員工打卡離場說明未顯示

- **日期**：2026-06-05（UTC+8）
- **Diff**：修正 `checkin.html` 在案場清單尚未載入時誤判「在案場內」，導致略過離場原因選擇、直接送出打卡。
- **Tech**：
  - `AppOffSite`：案場／預設標籤 localStorage 快取（30 天）、`isInitialized`／`initPromise`；有快取即標記已初始化。
  - `isOnSite`：案場清單為空時改回傳「離場」（`false`），不再預設在案場內。
  - `AppCore.execute`：上線且尚未初始化時，定位後先等待案場 API，再比對距離；離場則顯示 `#offSitePanel` 與「確認打卡」。
  - `AppCore.init`：離線也執行 `AppOffSite.init()` 以讀取快取。
- **Verify**：待總監於 LINE 開啟打卡頁，不在案場 200m 內時應先看到琥珀色離場說明區與可點標籤，再按「確認打卡」；未部署（未執行 upload.bat）。

## 2026-06-06 — 案場同步圖示與 10 秒逾時自動標記

- **Diff**：案場載入中顯示藍色橫幅＋「案場」燈號；10 秒未取得案場清單即放行打卡，自動帶入離場標籤「案場資訊異常」。
- **Tech**：`CONSTANTS.TIMEOUTS.SITE_LOAD`、`SITE_ANOMALY_TAG`；`AppOffSite.ensureSitesReady()`、`markSitesAnomalyAuto()`；`AppUI.setSitesSyncVisible()`；`execute` 逾時路徑直接 `submitCheckin`。
- **Verify**：待部署後測試；模擬 API 慢／失敗時應見更新中圖示，10 秒後完成打卡且紀錄含「案場資訊異常」。

## 2026-06-06 — 軌跡寫入 P 欄 + 案場背景重試 + 可重複打卡

- **Diff**：`checkin.html` v26.06.06.1600；`AppActionLog`、案場 1 次 10s 後可點、背景 30s 重試；按鈕不再等案場；P 欄 `offSiteAnomaly` 前端組字串；操作紀錄收起；再次打卡。
- **Tech**：格式 `離場未填說明|[軌跡]…` 或純 `[軌跡]…`；上限 2000 字；同步後端 CheckinSystem。
- **Verify**：待 upload.bat + GAS Actions 綠燈。

## 2026-06-06 — 失敗流程使用者說明強化

- **日期**：2026-06-06（UTC+8）
- **Diff**：打卡失敗、定位失敗、重複按鈕、系統忙碌等情境，改為「標題 + 原因 + 請您這樣做（步驟）」；結尾統一提醒關閉 LINE 視窗重開。
- **Tech**：`checkin.html` v26.06.06.1730；`AppCore.formatFailMessage()`、`FAIL_REOPEN_STEPS`、`PAGE_SESSION_END_MSG`；涵蓋 `endPageSession`、`execute` 定位錯誤、`finalize` busy/error、LIFF 驗證失敗、預熱拒絕定位。
- **Verify**：待部署；模擬定位拒絕／逾時／E002，畫面應有清楚步驟而非技術錯誤字串。

## 2026-06-06 — 移除重複按鈕的失敗文案（靜默防呆）

- **Diff**：`pageSessionEnded`／`pageFlowStarted` 重複觸發改靜默 `return`；按鈕已 disabled，使用者正常不會看到。
- **Tech**：`checkin.html` v26.06.06.1745；`execute` 合併 `pageSessionEnded || isLocked` 與 `pageFlowStarted` 無訊息分支。
- **Verify**：連點測試僅第一次有效，不應彈出「無法再次打卡」類訊息。

## 2026-06-06 — 出勤儀表板人員列表排除廠商與管理員

- **Diff**：人員下拉不應含「廠商」與權限 5；前後端同步篩選。
- **Tech**：`attendance_report.html` 加 `roster=attendance`、`isAttendanceRosterEmployee`；後端 `_isAttendanceRosterEmployee_`、`get_employees` 參數、`employee_map_attendance_cache`。
- **Verify**：待前後端 upload；開儀表板確認下拉無廠商／權限 5。

## 2026-06-06 — 人員名單 mode 收斂（attendance_*）

- **Diff**：篩選規則併入 `_getActiveEmployees_` 的 `attendance_*` mode；移除儀表板 `roster` 參數與前端重複篩選。
- **Tech**：`EmployeeLogic` `_resolveEmployeeListMode_`；`WebApp` 保留 `roster` 過渡別名；`attendance_report.html` 改 `attendance_active_with_recent_resigned`／`attendance_all`；SPEC 11、出勤儀表板 V2、09 排班。
- **Verify**：前後端一起 upload；其它 `get_employees` 頁面行為不變。

## 2026-06-07 — 手動補登後改重新查報表（移除樂觀更新）

- **Diff**：補登成功不再 `optimisticUpdate` 另插一列；查詢區間含補登日且已有報表時自動 `fetchReportData`。
- **Tech**：`attendance_report.html`；後端同日合併邏輯不變。
- **Verify**：5/25 已有出勤再補 08:30 上班，重查後應單列 08:30～原下班，無「手動補登」獨立列。

## 2026-06-07 — 出勤儀表板會計向改版

- **Diff**：每人小計表、點名展開逐日；統計卡僅單人；本月／上月；CSV 小計+明細；含補登資料來源；匯出今日未完成提示。
- **Tech**：`attendance_report.html`；後端 `Logic_AttendanceReport.js`（`stats`、`dataSource`、`_dayHasManualCheckin_`）；SPEC 儀表板 V2 §3。
- **Verify**：待部署；全員查詢見小計表、選單人見卡片；CSV 兩段式；手動補登日資料來源為「含補登」。

## 2026-06-06 — debug-loop 失敗流程模擬測試（一次性）

- **日期**：2026-06-06（UTC+8）
- **Diff**：除錯時曾以 Node 腳本模擬 12 案例（**12/12 通過**）；腳本不納入倉庫，已刪除。
- **Verify**：部署後以 LINE 實機抽測定位拒絕、逾時、busy 即可。
