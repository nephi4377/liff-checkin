# 2026-09-01 巡檢（行政）｜假勤核准失敗不裝成已審完

## Diff／目的

行政在「假勤審核儀表板」按批准／駁回時，舊程式用發射後不理，卡片直接滑走。後端其實回 HTML，前端讀不到成敗。人會以為已經審完。

## 技術

- `modules/attendance/approval_dashboard.html`：讀 JSON；失敗人話＋卡片留著＋可再按；本機 `mock=approvefail`
- `modules/attendance/leave_request.html`：管理分頁同樣改
- SPEC：`17` §4.7.3、資料字典 §6.2
- 後端（Backend_GAS CheckinSystem）：`page=attendance_api` 時 `handle_approval` 回 JSON；Email 連結仍 HTML

未改假勤政策、權限、資料表。雲端無法 clasp／upload.bat。

## 驗證

- 本機 `approval_dashboard.html?mock=approvefail`：有一張模擬假單；按批准要看到「沒審成」、卡片還在
- 真沒單才是空清單；Email 審核連結行為不變
