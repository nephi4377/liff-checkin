# 開發日誌 2026-04-18

## 2026-04-18（文件）

- **目的**：盤點 `LayoutPlanner` 互動式室內設計規劃工具之功能與資料流，不修改程式碼。
- **產出**：新增規格書 `SPEC/InteriorDesigned_LayoutPlanner_SPEC.md`（功能模組、Sheet／JSON 資料字典、計價摘要、已知問題、改進與路線圖）。
- **技術**：涵蓋 `LayoutPlanner.html`、`LayoutPlanner.js`、`utils.js` 與關聯之 `LayoutSheetViewer.html`。
- **驗證**：靜態程式碼檢索與對照 HTML；未執行瀏覽器 E2E。

## 2026-04-18 15:25（GitHub 同步）

- **目的**：依 `upload.bat` 流程將 `main` 推送至 GitHub（非互動執行，略過批次檔結尾 `pause`）。
- **Diff**：`feat(deploy): v26.03.17.1312 at 20260418_1525 (Refactored Core)`，commit `576d832`，12 files（含 SPEC、LOG、InteriorDesigned、reportV3、SchedulePlan 等）。
- **Tech**：`git push origin main` → `https://github.com/nephi4377/liff-checkin.git`。
- **Verify**：推送成功；本機 xcopy 備份步驟回傳碼 4，若需精確備份請在本機直接執行 `upload.bat` 或檢查 `..\BAK` 與 `exclude_list.txt` 路徑。

## 2026-04-18（LayoutPlanner 手冊／效能／UX）

- **目的**：修正使用手冊與實作不一致、移除未使用之 OpenCV、合併手機遮罩說明、改善選取元件後改寬深時選取跳離／失焦。
- **Tech**：`LayoutPlanner.html` 刪除 opencv.js、更新步驟 2／3.5／4 手冊文字、保留極簡 DOMContentLoaded 手機遮罩；`LayoutPlanner.js` 新增 `applyCabinetVisualUpdate`、`handleDimensionChange` 不再 `renderAllCabinets`；`handleKeyDown` 於表單欄位提前 return；`handleGlobalClick` 排除 `#minimized-bar`、`#canvas-header`。
- **Verify**：未跑瀏覽器 E2E；建議手動試：右側輸入寬深連續修改、分類下拉用方向鍵、點底部縮放列不應取消選取。

## 2026-04-18（SPEC v1.1）

- **目的**：更新 `SPEC/InteriorDesigned_LayoutPlanner_SPEC.md`：碰撞邏輯摘要、已處理／待處理對照、後續改進滾動清單、驗收與修訂紀錄；移除過時 OpenCV／手冊矛盾敘述。
- **Tech**：文件版本 1.1；新增第七～九章結構調整（原「錯誤點」整併為對照與建議）。
- **Verify**：靜態審閱與原始碼一致；未執行自動測試。

## 2026-04-18（LayoutPlanner：§8.2 落地實作）

- **目的**：將先前規格所列之「怎麼改」落實為程式：施工面積 DOM、鏡像 SAT、縮放碰撞還原、DEBUG、鍵盤復原、備份與歷史含 `constructionArea`；同步 SPEC v1.2 與使用手冊步驟 3.5。
- **Tech**：`LayoutPlanner.js`：`DEBUG`／`dbgLog`、`getVertices` 納入 `mirrored`、縮放分支 `checkCollision` 還原、`handleKeyDown` 綁定 Ctrl+Z／Ctrl+Y／Ctrl+Shift+Z、`saveState`／`loadState`／`autoSave` 含 `construction-area`；`LayoutPlanner.html`：`#construction-area`、說明 Modal 文案；`SPEC/InteriorDesigned_LayoutPlanner_SPEC.md` v1.2。
- **Verify**：靜態檢查與 linter 無報錯；未跑瀏覽器 E2E。建議手動試：鏡像後拖曳碰撞、拉邊撞另一櫃會卡住、Ctrl+Z／Y、施工面積寫入 JSON／ZIP／還原備份／復原。

## 2026-04-18（LayoutPlanner Playwright 煙霧測試）

- **目的**：協助難以手動驗收時，以可重複指令跑基本載入與 Ctrl+Z／Y（施工面積欄＋歷史）。
- **Tech**：`modules/InteriorDesigned/package.json`（`@playwright/test`）、`playwright.config.cjs`（`npx serve` 靜態伺服器）、`e2e/layout-planner.smoke.spec.cjs`；`.gitignore` 排除 `node_modules` 等。
- **Verify**：`npm run test:e2e` → **3 passed**（頁面載入／施工面積＋Ctrl+Z／Ctrl+Y）。

## 2026-04-18（SPEC §11 補充 E2E 說明）

- **目的**：將 `npm run test:e2e` 之執行目錄、Playwright 檔案與三項涵蓋／未涵蓋範圍寫入規格，避免僅有一句摘要。
- **Tech**：`SPEC/InteriorDesigned_LayoutPlanner_SPEC.md` v1.3，§十一「自動化（可選，Playwright 煙霧）」。
- **Verify**：與 `e2e/layout-planner.smoke.spec.cjs` 行為對照一致。

## 2026-04-18（SPEC §7／§9 與實作對齊）

- **目的**：§7 仍寫「鏡像未納入 getVertices、縮放未做碰撞」，與目前已實作行為不符；§9 測試自動化應註記已有 Playwright 煙霧。
- **Tech**：`SPEC/InteriorDesigned_LayoutPlanner_SPEC.md` v1.4。
- **Verify**：與 `LayoutPlanner.js` 中 `getVertices`、`handleGlobalMove`（縮放分支）一致。

## 2026-04-18（SPEC 路線圖精簡為四項）

- **目的**：依產品決策，後續工程**僅保留** SVG 消毒、大量元件效能、單元測試、擴充 Playwright；其餘路線圖（Sheet query／快取、匯出 hash、CSP、雲端協作、施工面積自動連動計價等）自規格刪除並註明不納入排程。
- **Tech**：`SPEC/InteriorDesigned_LayoutPlanner_SPEC.md` v1.5；刪除原「第十節 未來發展」整章；第九節改寫；第五節 `constructionArea`、第八節表格同步；驗收清單改為**第十節**。
- **Verify**：目錄連貫（九→十→十一）；與使用者指示之四項一致。

## 2026-04-18（LayoutPlanner §9 四項實作）

- **目的**：落實規格第九節：SVG 消毒、大量元件效能（CSS）、單元測試（Vitest）、擴充 Playwright（預算 Modal）。
- **Tech**：`sanitizeSvg.js`；`normalizeSheetSvg`（Sheet／JSON `loadLayoutFromData`）；`lib/geometry.js` + `LayoutPlanner.js` import `cmToFeet`／`getAxes`／`project`／`overlap`；`LayoutPlanner.html` `.placed-cabinet { contain: layout }`；`vitest.config.cjs`、`lib/geometry.test.js`、`lib/sanitizeSvg.test.js`；`e2e/layout-planner.more.spec.cjs`；`package.json` 新增 `test:unit`、`test`。
- **Verify**：`npm test` → Vitest **6 passed**、Playwright **4 passed**；`SPEC/InteriorDesigned_LayoutPlanner_SPEC.md` v1.6。

## 2026-04-18（LP 檔名 `LP_` 前綴）

- **目的**：LayoutPlanner 專用檔案加 **`LP_`** 前綴以利與目錄內其他工具區分。
- **Tech**：`LP_LayoutPlanner.html`／`LP_LayoutPlanner.js`／`LP_utils.js`／`LP_sanitizeSvg.js`／`lib/LP_geometry.js`／`lib/LP_*.test.js`／`e2e/LP_layout-planner.*.spec.cjs`／`LP_playwright.config.cjs`／`LP_vitest.config.cjs`／`LP_LayoutSheetViewer.html`；`spa/app.js` iframe 改為 `LP_LayoutPlanner.html`；`SPEC` v1.7、`FILE_DOCUMENTATION.md`、`SPEC/06_DESIGN_ENGINE_SPEC.md` 路徑同步。
- **Verify**：`npm test`（unit + e2e）通過。
