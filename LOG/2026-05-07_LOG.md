# 2026-05-07 LOG

## 2026-05-07（主控台人員出席）

- **目的**：主控台「人員出席」改為今天／明天兩段；假勤顯示僅假別與時段（不顯示事由）；跨月時合併次月班表。
- **異動檔案**：
  - `spa/Dashboard.js`：`attendanceDays`、假別字串用 `KNOWN_LEAVE_TYPES_SORTED` 剝除事由；`pendingMapsByDate` 依日判斷待審核；排班制加班 chip 可附時段。
  - `spa/app.js`：`fetchLatestScheduleForThisMonth` 於「明天」落次月時追加 `get_latest_schedule` 並 `mergeSchedulePayloads`；Dashboard import 版本串更新。
- **驗證**：未於此環境執行瀏覽器手測；邏輯與既有 SPEC 鍵名相容。
