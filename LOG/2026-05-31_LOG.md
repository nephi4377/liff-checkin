# 添心開發日誌 - 2026-05-31

## 1. 變更目的與歷程 (Diff Purpose & Action Trace)
* **問題背景**：總監反應日誌載入照片功能失效，且之前版本在 CORS 失敗後會移除所有預覽，只顯示紅色警告下載圖示。在後台非同步轉 Base64 載入完成前，總監也只能面對黑格子發呆，造成盲盒體驗。
* **解決方案**：
  * 重構圖片預覽與自動裝載的工作流。
  * 載入日誌的一瞬間，**100% 直載 Google 縮圖 `sz=w600` 作為 `<img>` 預覽**。瀏覽器會自動帶上 Google Cookie 憑證，保證免 CORS 秒速呈現圖片。
  * 在圖片上層覆蓋半透明 Spinner，默默在背景啟動代理 Fetch 轉 Base64。
  * 轉碼成功時，自動塞入 AI 發送清單，更新 Badge 為 `✓ 自動 (AI)` 綠色標籤。
  * 轉碼失敗時（例如無憑證 CORS 403 阻擋），**保留原本已成功顯示的縮圖預覽**，僅更新 Badge 為 `⚠️ 限制 (CORS)`，並允許點選卡片一鍵下載以手動拖回上傳區。

## 2. 技術修改內容 (Tech Spec & Logic)

### 修改檔案
* [index.html](file:///c:/Users/a9999/Dropbox/CodeBackups/CODING/tools/api-token-cost-calculator/index.html)

### 修改函數與變數
* **變數宣告**：
  * 明確在 script 最上方宣告 `let uploadedImagesList = [];` 修正嚴格模式下隱式全域變數問題。
* **照片套用函數 `applyBackendLog(index)`**：
  * 改寫相片元素 DOM 結構，預設直接載入 `https://drive.google.com/thumbnail?id=${fileId}&sz=w600`，並顯示半透明轉圈遮罩與 `編碼中...` 的 Badge。
  * 對接非同步 `downloadImageAsBase64WithFallback(fileId)`。
  * `.then()` 成功分支：
    - 將 Base64 資料 push 至 `uploadedImagesList`。
    - 移除 `.loading-overlay` 遮罩，把圖片 `src` 更新為 Base64（或保留縮圖）。
    - Badge 背景改為綠色並顯示 `✓ 自動 (AI)`。
    - 顯示清除按鈕並綁定刪除對應 index 清單與 DOM 元素事件。
  * `.catch()` 失敗分支：
    - 移除 `.loading-overlay` 遮罩，但**保留原本的 `<img>` 縮圖渲染**。
    - Badge 背景改為黃橘色並顯示 `⚠️ 限制 (CORS)`。
    - 允許點擊卡片另開分頁下載該檔案（方便總監手動拖放回網頁以供 AI 分析），並在 hover 顯示引導 tooltip。
    - 顯示清除按鈕以供移除元素。

## 3. 驗證結果 (Verify Flow & Outputs)
* 驗證日誌載入後，縮圖於第一時間呈現在預覽區，未出現黑色空白。
* 模擬 CORS 失敗，驗證縮圖預覽未消失，且顯示黃色警告提示並能引導點選下載與拖曳上傳，有效杜絕盲盒缺陷。
