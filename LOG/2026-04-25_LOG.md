# 開發日誌

## 2026-04-25

**時間：** 依工作階段填寫  
**差異與目的：** 將施工回報三份分散 SPEC 合併為單一《施工回報_系統完整_SPEC.md》，反映 `reportV3` + `submitFastReport` + `FirebaseHandler` 現況。  
**技術：** 新增 `CODING/SPEC/施工回報_系統完整_SPEC.md`；對照 `reportV3.html`、`WebApp.js`、`FirebaseHandler.js`、`line_reply.js`。未改執行中程式。  
**驗證：** 文件內敘述與倉庫程式靜態比對；未執行 E2E。

**同日增補：舊版 SPEC 封存**  
- **差異與目的：** 將合併前三份 `SPEC`（`REPORT_SYSTEM_SPEC.md`、`施工回報_FirebaseStorage_遷移計劃書.md`、`UPLOAD_OPTIMIZATION_PLAN.md`）搬至 `CODING/_DEPRECATED_/2026-04-25_施工回報合併前_SPEC/`，附 `README.md` 與各檔 2026-04-25 封存橫幅。  
- **技術：** 更新 `SPEC/施工回報_系統完整_SPEC.md` 合併來源敘述與 **§7**；`SPEC/01_SYSTEM_OVERVIEW.md` 連結改指合併版。  
- **驗證：** 已自 Git 還原後以 UTF-8 合併「封存」橫幅寫入 `_DEPRECATED_`，避免初次 `Move-Item` 後亂碼。`SPEC/` 下三檔已刪。

**同日增補：FILE_DOCUMENTATION 遷入 SPEC**  
- **差異與目的：** 將專案檔案清冊歸屬在規格層，更新 2026-04-25 內容（`reportV3` 路由、SPEC 索引、施工回報主規格指向、目錄與小節重整）。  
- **技術：** 新增 `SPEC/FILE_DOCUMENTATION.md`；`CODING/FILE_DOCUMENTATION.md` 改為一頁導向；`SPEC/01_SYSTEM_OVERVIEW.md` 內路徑改為 `SPEC/FILE_DOCUMENTATION.md`。  
- **驗證：** 內部連結以相對路徑可開啟導向頁正連至 SPEC 正本。

**同日增補：PROJECT_DATA_DICTIONARY 遷入 SPEC**  
- **差異與目的：** 與檔案清冊一併，將**資料字典**定錨在 `SPEC/`，正本內重複的「§3 稽核歷程」改編為 **§5**（前四節不變）。  
- **技術：** 新增 `SPEC/PROJECT_DATA_DICTIONARY.md`；根目錄 `PROJECT_DATA_DICTIONARY.md` 改一頁導向；更新《施工回報_系統完整_SPEC》《人員排程網頁計劃書》《FILE_DOCUMENTATION》§6 與**根目錄**說明、**01 深編** 連結；`backend/CheckinSystem/SPEC/02_MODULE_STRUCTURE_SPEC.md` 內寫明 CODING 內路徑。  
- **驗證：** 路徑字串以文字搜尋 CODING／該則 backend SPEC 已對齊。

**同日再補：刪根目錄兩導向檔**  
- **差異與目的：** `CODING/FILE_DOCUMENTATION.md`、`PROJECT_DATA_DICTIONARY.md` 僅轉址至 `SPEC/`，**非必要**；刪減以符合「根目錄只留要用的」。  
- **技術：** 刪上述二檔；`SPEC/FILE_DOCUMENTATION.md` 相關說明改寫（不再寫根目錄兩導向）。  
- **驗證：** 現行連結皆已指 `SPEC/*.md`；不影響佈建。

**同日補註：sw.js 不可併入他檔**  
- **差異與目的：** 釐清 Service Worker 需獨立 URL 註冊、根目錄利於全站 scope。  
- **技術：** `sw.js` 檔首加 JSDoc 註解；不變邏輯。  
- **驗證：** 靜讀；未改 `daily_report_main.js` 的 `register('../../sw.js')`。

**同日增補：Cursor 規則**  
- **差異與目的：** 將「僅單人開發」寫成永遠套用的專案規則，避免助理預設團隊／遠端協作敘事。  
- **技術：** 新增 `.cursor/rules/01-solo-developer.mdc`（`alwaysApply: true`）。  
- **驗證：** 檔案已建立。
