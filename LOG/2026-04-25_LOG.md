## 2026-04-25（補登）

**時間**：2026-04-25（對話接續）

**目的**：報價單同規格同尺寸但圖面上不相鄰時不要合併成一列。

**動作**：
- 在 `calculateFullQuotation` 組完 `allItems` 後呼叫 `quotationAssignSpatialClusterKeys`。
- 合併鍵加上 `spatialCluster`（`loc0`…），同一簽名內用 union-find 依外框邊距 ≤ 55（畫布座標單位）連通才同群；單件不帶後綴。
- 計價迴圈改為共用 `quotationComputePricingParts`，避免重複邏輯。

**技術**：`modules/InteriorDesigned/LP_LayoutPlanner.js` — `quotationComputePricingParts`、`quotationAssignSpatialClusterKeys`、`calculateFullQuotation`。

**驗證**：靜態檢查無 linter 錯誤；行為上同群才併列、modal／CSV 皆走 `calculateFullQuotation`。

---

**時間**：2026-04-25（再接續）

**目的**：合併依「相鄰位置」，不依寬度／坪數；相鄰但寬度不同仍合併；不相鄰即使寬度相同不合併。

**動作**：
- `quotationSizeMergeKey`：櫃體改為常數 `c`、區域 `a`、標註 `n`，不再把寬高或坪數小數寫進合併簽名。
- 合併列規格欄：同列多筆時以 `Set` 收集 `quotationSizeDisplay`，多種則以「、」串起。

**技術**：同上檔案 — `quotationSizeMergeKey`、`calculateFullQuotation` 內 `groups[key]`。

**驗證**：linter 無錯誤。

---

**時間**：2026-04-25（補 SPEC）

**目的**：程式已上傳後補齊規格書，與報價合併行為一致。

**動作**：
- `SPEC/InteriorDesigned_LayoutPlanner_SPEC.md`：§4.6 合併規則、§6 全案報價一句、修訂紀錄 1.9。
- `SPEC/06_DESIGN_ENGINE_SPEC.md`：§3.2 分組對齊敘述 + 連結 §4.6。
- `SPEC/GLOSSARY_ZH.md`：報價合併列一條。

**驗證**：Markdown 僅敘述更新，無程式變更。

---

**時間**：2026-04-25 16:20

**目的**：建立 Cursor Cloud 開發環境，確認本機可安裝依賴、跑測試、啟動網頁。

**動作**：
- 以鎖定檔執行 `npm ci`、`npm ci --prefix richmenu-preview`、`npm ci --prefix modules/InteriorDesigned`。
- 安裝 Playwright Chromium：`npx playwright install chromium --with-deps`。
- 啟動根目錄靜態服務 `http://localhost:8080` 與 Layout Planner 靜態服務 `http://localhost:8765`。
- 以瀏覽器開啟首頁與 `LP_LayoutPlanner`，確認頁面載入並可操作。

**技術**：`README.md` 新增開發環境、測試、啟動與已知提醒；雲端啟動腳本建議使用三個 `npm ci` 指令。

**驗證**：`npm test`（InteriorDesigned）通過 6 個單元測試與 4 個瀏覽器測試；`curl -I` 確認 `8080` 回 200、`8765/LP_LayoutPlanner.html` 正常轉址；手動瀏覽器錄影確認兩個頁面可用。根目錄 `firebase` 依賴目前有 1 個 npm audit 重大弱點提醒，未在本次環境設定中升級版本。
