# 2026-07-25｜案件毛利：778／791 類獨立工作頁釐清

## 結論（白話）

`778_…`、`791_怡雅` 這類分頁是**舊制「一案子一分頁」殘留**，不是現在還會新建。日常帳寫進 `明細_700全部`（沒有「全部」才用 `明細_700`）。

## 本機變更（未 commit／未 deploy）

- `MarginModule.js`：若合併分頁與舊獨立頁**兩邊都有**該案列，讀取時併入舊頁尚未重複的列（避免半套遷移只看到一邊）。**寫入仍只進合併分頁**，不會再建獨立頁。
- SPEC：`15_會計系統模組規格書.md`、`accounting-gas/SPEC/DATA_MASTER_PLAN.md` 補獨立頁／「分頁名稱」欄意義。

## 建議人工（不動表也可先觀察）

1. 對 778、791：比對獨立頁與 `明細_700全部` 該案列是否已齊。
2. 缺的才手動或跑既有「舊頁→合併」遷移／修復；齊了可**隱藏**獨立頁，不建議立刻刪。
3. 總覽「分頁名稱」若仍是獨立名：部署自我修正後，開列表／開該案／寫一筆／重算合計都會改成合併分頁名（見 `2026-07-25_LOG_總覽分頁名稱自我修正.md`）。
