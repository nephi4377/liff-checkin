# 2026-09-01 巡檢（客人）｜收款確認失敗不裝成沒收款

## 目的（人話）

客人從 LINE 開「收款確認」，網路不穩或身分還沒確認到時，不要只看到「—」或工程錯誤。要告訴人「這次沒讀到、可再試」，這不代表沒有收款。

## 改了哪些檔

- `modules/accounting/customer-finance-portal.html`：開頁身分失敗、列表讀失敗都改人話＋再試；自動再試一次
- `SPEC/21.案件毛利擴充與薪資連動.MD`（B5.7）
- `TOS/巡檢回報.md`

未改 GAS、未改商業規則（確認收款兩步驟不變）。

## 本機怎麼看

頁面：`modules/accounting/customer-finance-portal.html`（僅 `127.0.0.1`／`localhost` 吃 `mock`）

| 模擬 | 網址 |
|------|------|
| 開頁身分失敗 | `?mock=recbootfail` → 要看到「讀不到」與再試，不可只留載入中或「—」 |
| 列表讀取失敗 | `?mock=recfail` → 同上，不可出現「目前無待確認收款」 |

## 部署

雲端無法跑 `upload.bat`、不能自行合 `main`。合併後才算正式上線。
