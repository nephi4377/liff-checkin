# 2026-05-23 開發日誌

## 2026-05-23 — 統一結案（主控台／試算表／驗收表）

**目的**：主控台或試算表改為「已結案」後，驗收表主頁與結案鍵應一致；驗收表結案亦回寫試算表。

**變更**：
- `backend/project-console/FirebaseBridge.js`：`applyProjectClosureToFirebase_`、`_reconcileSheetClosedProjectsToFirebase_`
- `backend/project-console/ProjectLogic.js`：`_getSiteProjectStatus_`；`_updateProjectStatus_` 結案時同步 Firebase
- `backend/project-console/WebApp.js`：`get_site_project_status`、`reconcileProjectClosure`
- `CODING/tools/BudgetAuditor_Standalone_V2.html`：試算表狀態對帳、Dashboard 批次對帳、驗收結案回寫試算表、`postMessage`
- `CODING/modules/projects/js/main.js`：主控台結案後通知內嵌驗收表
- `CODING/SPEC/12_BUDGET_AUDITOR_AND_CONSOLE_INTEGRATION_SPEC.md`：**§9 統一結案規格**
- `CODING/SPEC/08_PROJECT_CONSOLE_CRUD_SPEC.md`：連結 §9

**驗證**：待部署後 — 主控台結案 → 驗收主頁消失；試算表手改已結案 → 開啟驗收表或刷新主頁後對帳；驗收表結案 → 試算表狀態為已結案。

**備註**：後端 Firebase 寫入可設 Script Property `FIREBASE_RTDB_AUTH`；未設定時依 RTDB Rules，前端仍會補 `syncToFirebase`。

---

## 2026-05-23 10:15 — 代上傳後 API 自測

- **backend** `upload.bat` → push `c0fb9b7`；Actions `project-console clasp` ✅ 32s
- **CODING** `upload.bat` → push `cd557ea` ✅
- `reconcileProjectClosure`：第一次 `reconciled:3/19`；第二次 `reconciled:16, skippedCache:3, errors:[]` ✅
- `get_site_project_status` 案 675：`isClosed:true`，`firebaseSync.already_closed` ✅
- `get_site_project_status` 案 770：`isClosed:false` ✅
- Firebase `quotations/675/is_closed` = `true` ✅
- **未測**：主控台 UI 點結案、驗收表 UI 結案（需瀏覽器手動）

---

## 2026-05-23 — human-comfortable skill 通用化

**Date**: 2026-05-23（skill 改版）

**Diff**: 將 skill 從「觀測站／桌面／報表」擴為任何會被人看到或操作的產物；原則改為先結論、白話、少驚嚇、可見回饋、明路、可復原；依產物類型檢查表；Windows 桌面與 TanxinTools 範例降為附錄。

**Tech**: `.agents/skills/human-comfortable/SKILL.md`（frontmatter description、正文結構）

**Verify**: 成功；使用者要求多用途、以人用得舒服為主。

---

## 2026-05-23 — human-comfortable：重要性檢視 + 開發期人機介面

**Date**: 2026-05-23

**Diff**: §1 重要性表（可縮減指引）；原則 7→5；新增 §3 開發流程插入點、人機五問、與 debug-loop／tanxin-admin-secretary 協作、可選輸出格式；合併交付前檢查；移除 TanxinTools 大表、修復 frontmatter。

**Tech**: `.agents/skills/human-comfortable/SKILL.md`

**Verify**: 成功；依使用者要求對接 debug-loop 最終驗證與行政文案 skill。

---

## 2026-05-23 — human-comfortable 大幅簡化

**Diff**: 使用者回饋過複雜；刪 §1 維護表、多層小節、五問表、可選輸出格式；收斂為「四件事」+ 4 項檢查 + 三行協作說明（約 40 行）。

**Tech**: `.agents/skills/human-comfortable/SKILL.md`

**Verify**: 成功。

---

## 2026-05-23 — human-comfortable 對齊「成品人機介面」

**Diff**: 主軸改為交付後真人使用的畫面／報表／通知；明確排除 Agent 開發回覆與 log 預設畫面；刪 debug-loop／行政秘書為主軸；加成品驗收與常見問題表。

**Tech**: `.agents/skills/human-comfortable/SKILL.md`

**Verify**: 成功；依使用者澄清「最主要目標是完成後的成品，不是開發階段輸出」。

---

## 2026-05-23 — human-comfortable debug-loop 自審

**Diff**: 補通用人機第五項「全站一致」、表單指到欄位、無障礙基線（色+字）；驗收改「3 步」為流程合理；桌面 pythonw 改通用「不閃黑窗」。

**Tech**: `.agents/skills/human-comfortable/SKILL.md`

**Verify**: 自審結論——已達成品通用人機基本門檻；進階 a11y／鍵盤導覽 intentionally 不寫入以免 skill 膨脹。

---

## 2026-05-23 — human-comfortable 補可執行人機指令

**Diff**: 使用者試用後介面仍不友善；新增 §做介面時必做（版面結構、四態、表單、表格、危險操作、與 Premium 分工）、禁止清單；description 強調改版面必載入。

**Tech**: `.agents/skills/human-comfortable/SKILL.md`

**Verify**: 待下次實際改版面驗證。

---

## 2026-05-23 — human-comfortable 整合使用者三大原則

**Diff**: 核心目標改為直覺好用／減少腦力眼力；正文依「看哪裡清楚、要做什麼簡單、做錯不挫折、設備貼心」重寫；合併原版面四態表單表格指令；Agent 檢查清單採使用者四項並擴充。

**Tech**: `.agents/skills/human-comfortable/SKILL.md`

**Verify**: 成功。
