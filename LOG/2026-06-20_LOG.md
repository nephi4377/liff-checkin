## 2026-06-20 — 主控台燈號看板權限 5 無法進入

- **症狀**：出席區「全員燈號看板」連結不見；開啟顯示權限不足（實際權限 5）。
- **根因**：`get_hub_core_data` 用 `attendance_active` 排除權限≥5 → `currentUser` 落空 → `hasAdminRights` 為 false、iframe 帶 `permission=1`。
- **修正**：後端改 `active` 並回傳 `operator`；`app.js` 以 operator 補 currentUser；看板頁改由 API 驗權、移除前端誤判。
