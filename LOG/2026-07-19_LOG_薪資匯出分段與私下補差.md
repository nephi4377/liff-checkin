# 2026-07-19 LOG — 薪資待匯款三步、走帳／私下補差、事後補登

## 做了什麼

1. **薪資待匯款**對齊廠商：匯出 TXT 不改狀態 → 上傳後「標已發薪」
2. **薪資審核**：走帳（薪轉）金額 vs 私下補差欄位
3. **事後補登發薪**表單（財務頁底部）→ `payroll_request_manual_backfill`
4. SPEC 15/17／架構圖：薪資資料位置改為會計主檔「薪資請款申請」

## 技術

- `payroll_finance.html`、`payroll_review.html`
- `accounting_api.js`：`payrollRequestMarkPaid`（`mark_all`）、`payrollRequestManualBackfill`

## 驗證（待抽測）

- 薪資待匯款：匯出 → 標已發薪 → 補登表單
- 薪資審核：走帳／私下補差顯示與儲存

## 部署（deploy-runbook）

- 目標：CODING `upload.bat`
- git：`9003bcd`（merge `cursor/payroll-off-record-backfill-0126` → main）
- 部署結果：見下方（執行後補）
