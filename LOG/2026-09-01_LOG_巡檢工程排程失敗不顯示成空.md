# 2026-09-01 — 巡檢：工程排程讀失敗不顯示成空

**Diff／目的**：設計師開專案工作區「工程排程」時，後端排程讀失敗會被當成沒任務，並出現「套用範本」。改為人話失敗＋再試，真沒任務才顯示範本。

**技術**
- 前端：`modules/projects/js/main.js`、`scheduleActions.js`、`state.js`、`gantt_main.js`；`managementconsole.html` 快取破除 `?v=26.09.01.1`
- 後端（另倉）：`page=project` 回傳 `scheduleLoadError`，不把 `{ error }` 裝成空 `schedule`
- SPEC：`SPEC/05_專案工作區與協作中心規格書.md` §4

**驗證**
- 讀碼：失敗路徑先判斷 `scheduleLoadError`，不會走到「目前尚無排程任務」
- 開頁若排程失敗會自動再打一次 `page=project`；按鈕「再載入排程」走 `refreshProjectData`
- 有舊畫面時失敗會保留先前任務，不寫空表進快取
- Cloud 無法 `upload.bat`／`clasp deploy`；合 main 並部署 project-console 後才算正式站

**部署**：待合 main（巡檢 P0；遠端未 clasp）
