Date: 2026-05-11 16:59

Diff:
- Purpose: 將 SKILL 集中到 CODING，避免跨專案分散造成觸發不穩。
- Action Trace:
  1) 新增 `CODING/.agents/skills/debug-loop/SKILL.md` 作為正式集中版本。
  2) 刪除 `Tanxin/.cursor/skills/debug-loop/SKILL.md` 重複副本。

Tech:
- Modified files:
  - `d:\Dropbox\CodeBackups\CODING\.agents\skills\debug-loop\SKILL.md`
  - `d:\Dropbox\CodeBackups\Tanxin\.cursor\skills\debug-loop\SKILL.md` (deleted)
- Logic:
  - 以 `CODING/.agents/skills` 作為單一真實來源（Single Source of Truth）。
  - 保留原有 `Cloud-Agent-Runbook`、`Premium-UI-Design`，並新增 `debug-loop` 進同一技能目錄。

Verify:
- Success
- Outputs:
  - debug-loop 技能已可從 `CODING/.agents/skills/debug-loop/SKILL.md` 載入。
  - Tanxin 端重複技能已移除，避免版本混用。
## 2026-05-11 16:35

### Diff: Purpose & Action Trace
- 目的：修正「案場資訊地址偶爾顯示未知/未提供」的偶發問題。
- 動作：
  1. 追查 `managementconsole.html` 對應邏輯後，定位到 `modules/projects/js/main.js` 與 `modules/projects/js/ui.js`。
  2. 在 `main.js` 新增 `normalizeOverview()`，統一把 `address`、`地址`、`siteAddress` 補正到 `案場地址`。
  3. 將正規化套用到首次載入與背景刷新路徑，避免只在部分流程補值。
  4. 在 `ui.js` 右側資訊面板新增 `getAddress()` 容錯，顯示時再做一次地址欄位回退。

### Tech: Modified Files, Functions, Logic
- 修改檔案：
  - `modules/projects/js/main.js`
  - `modules/projects/js/ui.js`
- 主要函式：
  - `normalizeOverview(overview)`（新增）
  - `handleDataResponse(data)`（改為使用正規化後的 overview）
  - `refreshProjectData(showNotification)`（刷新後也先正規化 overview）
  - `displayProjectInfo(overview, schedule)`（新增 `getAddress()` 容錯）
- 邏輯重點：
  - 地址欄位改為多來源容錯，避免後端回傳鍵名不一致時 UI 顯示「未提供」。

### Verify: Success/Fail, Outputs
- 結果：Success（程式碼修改完成）。
- 驗證方式：
  - 靜態檢查確認地址讀取流程已覆蓋「首次載入 + 背景刷新 + 顯示層」三條路徑。
  - 待使用者於工作區實際重整確認「案場地址」是否穩定顯示，不再偶發未知。
