# 2026-07-06 LOG

## 顧客列表快取 24h ＋ 案件收入審核流重構

### 目的

- 顧客列表／LINE 聯絡人搜尋改為本地 24 小時快取，減少每次搜尋打 GAS
- 案件毛利登錄「收入」改走：財務審核 → LINE 推送客戶 → 客戶二階確認後才寫入收支
- 客戶 portal 案號資料 7 天快取 + 背景重讀；收款②前端防誤觸（①後本地顯示②，僅②提交打 GAS）

### 技術

| 檔案 | 內容 |
|------|------|
| `shared/js/accounting_list_cache.js` | `searchMasterList`、`loadPortalData`；localStorage 24h／7d |
| `shared/js/accounting_api.js` | `cachedOfficialCustomerSearch`、`cachedLineContactSearch`、`fetch_all` |
| `shared/js/customer_finance_bind.js` | 改用快取搜尋；綁定後 invalidate |
| `modules/accounting/vendors.html` | LINE 聯絡人快取搜尋 |
| `modules/projects/NewSiteForm.html` | 載入 list_cache |
| `modules/accounting/customer-finance-portal.html` | 7d 快取；stage1→② 前端過渡 |
| `modules/accounting/project_margin.html` | 收入送審提示 |
| `modules/accounting/ledger_review.html` | 客戶收款 tab 說明更新 |
| `backend/.../CustomerFinanceModule.js` | `pending_income_review`、`margin_income` 流、fetch_all 顧客列表 |
| `backend/.../MarginModule.js` | 收入改 `cfCreateReceiptFromMarginIncome_` |
| `backend/.../AccountingLineContactRegistry.js` | `fetch_all` 整包名冊 |
| `SPEC/18`、`SPEC/21 §B5` | 快取 key／新收入流 |

### 驗收

1. 顧客綁定搜尋：第二次同關鍵字應明顯較快（Network 無 `official_customer_search` 或僅首次 `fetch_all`）
2. 廠商名冊 LINE 搜尋：同上（`line_contact` key）
3. 毛利頁新增「收入」→ 提示已送審；`ledger_review` 客戶收款 tab 出現待審列
4. 財務核准 → 客戶 LIFF 收到推送；客戶①後畫面直接出現②（不整頁重載）；②提交後案件收支有列
5. 設計師草稿收款流（舊）仍可：客戶先確認 → 財務核准寫入

### 部署

| 項目 | 內容 |
|------|------|
| 備份 | ``../BAK/CODING_20260706_1741_TX34`` |
| CODING | ``617e390`` v26.07.01.1 upload.bat |
| accounting-gas | ``fec5f33`` clasp @130 |

---

## 追加減／收款按鈕即時回饋 + 設計師頁移除財務簽

### 目的

- 所有 async 按鈕點擊後**同一 tick** 顯示 disabled + 處理中…，避免雙擊
- 設計師頁收款 tab 僅保留設計師簽，財務簽改為唯讀提示導向請款審核
- 客戶 portal、ledger_review 客戶收款 tab 同步改善

### 技術

| 檔案 | 內容 |
|------|------|
| shared/js/customer_finance_ui.js | runBtnAsync 同步設 busy 後以 rAF 延後執行 fn，確保瀏覽器重繪 |
| designer-customer-finance.html | 作廢 prompt 移出 async；tab 切換防連點；v=2 |
| customer-finance-portal.html | 案號切換載入中鎖定 |
| ledger_review.html | 客戶收款 tab 切換即時 busy；審核 confirm 移出 runAsync |

### 驗收

- 設計師頁：送審／建立草稿／設計師簽 點擊即變「處理中…」
- 收款 tab 無「財務簽」按鈕，顯示「待財務簽核 → 請款審核」
- ledger_review 客戶收款 tab 切換有載入態

### 部署

| 項目 | 內容 |
|------|------|
| 備份 | `../BAK/CODING_20260706_1708_TX34` |
| CODING | `0fe604d` v26.07.01.1 upload.bat |
| accounting-gas | `fa1f29d` clasp @128 |

---

## NewSiteForm 客戶 LINE 綁定區塊可見性修正

### 目的

- 案場表單預設即顯示「客戶 LINE 綁定」區塊（不再 `display:none`）
- 無案號時顯示提示；有案號（選現有案場或手動輸入）時載入完整綁定 UI
- 補 `primeHubIdentityFromUrl` 以支援測試模式 auth bypass

### 技術

| 檔案 | 內容 |
|------|------|
| modules/projects/NewSiteForm.html | `refreshCustomerBindPanel` 改為依案號顯示；案號 input 監聽；onload 初始化 |

### 驗收

- 建立新案場模式：可見標題 + 提示「請先選擇現有案場或輸入案號…」
- 下拉選現有案場：顯示完整綁定 UI（與設計師頁一致）
- 手動輸入案號：同上

### 部署

| 項目 | 內容 |
|------|------|
| 備份 | `../BAK/CODING_20260706_1716_TX34` |
| CODING | `3126e0e` v26.07.01.1 upload.bat |
