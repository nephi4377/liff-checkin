# 2026-05-30 開發日誌

## 2026-05-30 14:00

**Diff:** 同步 CheckinSystem 離場打卡說明 UI 至 CODING 員工打卡頁。

**Tech:**
- `modules/attendance/checkin.html`
  - 新增 `#offSitePanel`（可複選 chip + 補充 textarea）
  - 新增 `AppOffSite`：載入 `get_all_sites` / `get_off_site_reason_presets`、200m 案場判斷、送出 `offSiteReasonTags`（JSON）、`offSiteReasonNote`
  - 離場流程：定位後若不在案場 → 顯示面板 → 「確認打卡」送出（可不填）
  - 版本 `v26.05.30.1400`
- 依據：`backend/CheckinSystem/reference/checkin_offsite_liff_snippet.js`、`SPEC/12_OFF_SITE_CHECKIN_REASON_SPEC.md`

**Verify:** 待本機／LINE LIFF 實測：案場外打卡應出現 chip；送出後後端欄位 N～P 有值或「離場未填說明」。

## 2026-05-30 20:28

**Diff:** 修復 Token 計算與測試網頁 AI 模型選項消失之 ReferenceError。

**Tech:**
- `tools/api-token-cost-calculator/index.html`
  - 補回 8 個遺失的控制與渲染 JavaScript 核心函數：
    1. `switchTab`：頁籤切換控制。
    2. `togglePasswordVisibility`：API Key 密碼顯示/隱藏切換。
    3. `updateExchangeRate`：匯率即時更新。
    4. `saveEnvSettings`：儲存金鑰與匯率配置至 `localStorage`。
    5. `renderModelSelectors`：動態渲染下拉選單模型選項。
    6. `renderRatesTableEditor`：動態寫入費率編輯 Table。
    7. `saveModelRatesSettings`：儲存變更後的模型費率至 `localStorage`。
    8. `updateSelectedModelFee`：更新當選用模型之對帳細節與看板。
  - 修復網頁載入時因缺乏上述函數定義而中斷運行的 bug。

**Verify:**
- 成功。網頁載入時 ReferenceError 消失。
- 模型下拉選單 (`live-model` 與 `manual-model`) 已能正確動態生成並渲染出 Gemini 2.5 Flash, Pro 等選項。
- 頁籤切換功能、環境配置儲存、費率矩陣編輯與儲存等交互操作均測試通過。

## 2026-05-30 20:35

**Diff:** 新增「添心設計助理」模型選項並實作本地輸入草稿自動儲存功能。

**Tech:**
- `tools/api-token-cost-calculator/index.html`
  - 新增虛擬模型 `tianxin-design-assistant` 到預設費率矩陣資料庫，其實體 API 請求自動映射至 `gemini-2.5-flash`。
  - 模型切換時，自動連動切換至設計助理（`assistant`）之 System Instruction 與 Prompt 預設範本。
  - 實作本地輸入草稿儲存 `saveInputDraft` 與載入回填 `loadInputDraft`。
  - 於 `DOMContentLoaded` 中自動綁定文字輸入欄位並載入草稿，且於範本切換、歷史還原、規範快捷插入時自動同步更新儲存，重載不遺失內容。

**Verify:**
- 成功。重整網頁後，原先填寫的專案名稱、Prompt、Context 等測試內容均被完美還原。
- 下拉選單中顯示「添心設計助理 (Gemini 2.5 Flash)」，且選取時下方系統指令與提示詞自動連動帶入。

## 2026-05-30 20:45

**Diff:** 串接後端 page=project API，並透過設計友善的照片預覽與一鍵下載引導解決 Google Drive 跨網域政策限制。

**Tech:**
- `tools/api-token-cost-calculator/index.html`
  - 新增「添心系統後端 User ID」輸入框，支援 LINE userId 或 uid 設定，並存入 `tx_backend_user_id`。
  - 將其加入 `DRAFT_FIELDS` 及 `DOMContentLoaded` 還原流程，實作自動草稿記憶。
  - 重構 `loadProjectLogsFromBackend`，改為 fetch `${backendUrl}?page=project&id=${projectId}&userId=${userId}`，並正確讀取 `res.data.dailyLogs`。
  - 重構 `applyBackendLog`，移除了原有的 `get_drive_file_base64` API 依賴。改為使用 `<img>` 標籤以 Drive thumbnail URL 預覽照片以繞過 CORS 限制。
  - 新增 `human-comfortable` 溫柔提示卡片與 hover 下載提示，點擊可開啟新分頁下載，指引總監下載後手動上傳大圖進行 Gemini 分析，防止操作挫折。

**Verify:** 
- 前端能成功載入 User ID 設定並儲存於 LocalStorage。
- 輸入案號載入日誌時，能發送 API 請求現成 `page=project` 路由並動態列出歷史日誌。
- 切換日誌能自動讀入日誌內文至 Context，並以 `<img>` 顯示 Drive 照片，且提供「一鍵新分頁下載」指引。

## 2026-05-30 20:50

**Diff:** 於 Token 計算機中實作預設 API 網址與「俊豪」測試身分之自動補齊與 Fallback。

**Tech:**
- `tools/api-token-cost-calculator/index.html`
  - 於 `DOMContentLoaded` 初始化時，若 `localStorage` 無 `tx_backend_url` 與 `tx_backend_user_id` 設定，則分別預設套用專案主控台網址與「俊豪」本地測試 UID（`Ud58333430513b7527106fa71d2e30151`），對齊其他前端預設資料。
  - 重構 `loadProjectLogsFromBackend`，優先讀取 UI 欄位輸入值，並在無設定時使用硬編碼預設 Fallback。
  - 恢復先前被修改的專案主控台（`main.js`, `config.js` 與 `managementconsole.html`）代碼，保持核心模組穩定不變。

**Verify:**
- 隱私視窗或清除 LocalStorage 開啟 Token 計算機，設定頁面之「WebApp API URL」與「User ID」已自動預載預設資料.
- 無需手動輸入設定，直接輸入案號按「載入日誌」即可成功取得「俊豪」的歷史施工日誌。

## 2026-05-30 23:40

**Diff:** 實作雙 CORS 代理背景自動相片下載與 Base64 轉換，完成日誌載入的 100% 自動化。

**Tech:**
- `tools/api-token-cost-calculator/index.html`
  - 新增 `downloadImageAsBase64WithFallback(fileId)` 函數，透過雙 CORS 代理（`corsproxy.io` 與 `allorigins`）在背景非同步獲取 Google Drive 縮圖的二進制 Blob 並轉為 Base64。
  - 重構 `applyBackendLog`，載入日誌時於預覽區顯示 `fa-circle-notch` spinner 動畫，並在背景非同步執行下載。
  - 下載成功後自動 push 到 `uploadedImagesList`（待傳送陣列），預覽區自動替換為真實圖片並加掛一鍵刪除按鈕，實現一鍵自動發送多圖。
  - 保留 `human-comfortable` 溫柔引導防撞牆機制，若所有代理皆失效，自動降級為手動下載防退區塊，防止流程中斷。

**Verify:**
- 載入日誌後，預覽區自動顯示 loading 狀態，並在 1~3 秒內自動轉為真實施工照片。
- 點選「發送並計算成本」，驗證 AI 順利讀取該照片的 Base64 數據並輸出施工進度稽核報告（代表 Base64 成功在背景自動裝載至 API payload 中）。
- 測試斷網或失效 fileId 時，能順利自動降級為紅色警告並提供手動下載連結。

## 2026-05-30 23:59

**Diff:** 優化背景下載為 Google Drive w800 縮圖，大幅提升加載效能並節省 API Token 成本。

**Tech:**
- `tools/api-token-cost-calculator/index.html`
  - 修改 `downloadImageAsBase64WithFallback` 函數，將下載的相片來源 URL 從原圖 `lh3.googleusercontent.com/d/{fileId}` 改為縮圖連結 `drive.google.com/thumbnail?id={fileId}&sz=w800`（總監建議）。
  - 將下載檔案大小縮減數十倍（降至約 50~150KB），大幅提升 CORS 代理之秒開載入成功率，徹底消除逾時風險。
  - 大量節省 Gemini API 處理多圖時計算的 Image Tokens 成本。

**Verify:**
- 載入日誌後，照片的背景下載轉換幾近「秒開」，網頁加載速度提升極為顯著。
- 發送 API 測試時，Gemini 能正常分析 w800 縮圖，且產生的 API Token 花費與金額顯著低於大圖。
