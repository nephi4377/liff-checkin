# 2026-07-13 部署 — CODING（HUB 會計 LIFF token 第二輪修復）

## Diff／目的

部署後仍出現「無法從主控台取得登入憑證 · 12 秒」：

1. **HUB scope bug**：`refreshHubIdToken` 誤放在 `onMounted`，iframe 訊息處理器取不到 → 永遠回不了 token
2. **iframe 重載**：員工名單回來後 `permission` 變更導致會計 iframe URL 改變 → 整頁重啟又失敗
3. **子頁版本**：多數會計 HTML 仍載入舊 `accounting_api.js`；收支登錄 HUB 內嵌未走 Hub token 路徑

## 技術

| 區 | 檔案 |
|----|------|
| HUB | `spa/app.js`（`hubIdTokenRef`／`iframeMountReady`） |
| API | `shared/js/accounting_api.js` v37（token 快取、暫用身分 fallback） |
| 殼層 | `shared/js/accounting_shell.js` v4（`pendingTokenRequestSource` 轉發） |
| 會計 | `modules/accounting/index.html`（`initSession`、shell v4）、`accounting_ingest.html`（Hub boot） |
| 版號 | 全站會計相關 HTML → `accounting_api.js?v=37` |
| SPEC | `19_HUB與會計全域身分傳承.md` §7 對照表 |

## 備份／部署

- 腳本：`CODING/upload.bat`（`NONINTERACTIVE=1`）
- 備份：`../BAK/CODING_20260713_0004_NEPHI筆電001`
- 結果：**已 push** `2f453a3`（v26.07.01.1／20260713_0004）

## 驗證（待測）

1. LINE → 主控台 → 會計：不再出現 12 秒 token 失敗
2. 收支登錄：案號／經辦下拉正常；狀態紀錄可見「快取登入」或背景驗證成功
3. 子功能（待付款申請等）Network 載入 `accounting_api.js?v=37`

---

## 2026-07-13 追加 — 狀態紀錄 client_log 狂打 API

### Diff／目的

- debug 模式下「API accounting_client_log（偏慢）」連環觸發：上傳慢 → 又記一筆 → 再上傳
- 每筆狀態都即時打 GAS，96 秒內可累積數十次無意義請求

### 技術

| 區 | 檔案 |
|----|------|
| UI | `accounting_ui.js` v14（info 僅本機；遠端 8 秒合併批次） |
| API | `accounting_api.js` v38（`accounting_client_log` 不計入 apiStart/End） |
| 版號 | 全站會計 HTML |
| SPEC | `20_會計快取與身分傳承流程圖.md` §11 |

### 備份／部署

- 腳本：`CODING/upload.bat`（`NONINTERACTIVE=1`）
- 備份：`../BAK/CODING_20260713_0013_NEPHI筆電001`
- 結果：**已 push** `bf9e22b`（v26.07.01.1／20260713_0013）

### 驗證（待測）

1. 開會計 debug：狀態紀錄仍正常顯示，Network 不再狂刷 `accounting_client_log`
2. 錯誤／完成類訊息仍會合併上傳至稽核試算表（約 8 秒一批）
