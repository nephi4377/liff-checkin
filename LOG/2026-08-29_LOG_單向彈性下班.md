# 2026-08-29 CODING LOG — 續1 單向彈性下班（後端對齊）

| 項目 | 說明 |
|------|------|
| 目的 | 早到不能提早走；打卡「預計下班」與出勤早退跟前端同一套公式 |
| 範圍 | backend `CheckinSystem`；前端 `utils.js` 已單向未改 |
| 部署 | CheckinSystem 本機 clasp **@381**；GitHub 先前因 Dropbox 未 push，本次對齊 |

## 結論

後端 7/31 只廢了「早到超過彈性窗口可提早走」；彈性內早到仍會把下班往前移。本次改成 `lateWithinFlex`（與 `shared/js/utils.js` 一致）。

## 改動（backend）

- `Logic_SystemUtils.js`：`_computeFlexOffTimeOneWay_`（打卡、生產力助手）
- `Logic_AttendanceReport.js`：報表早退（仍回 `{ late, early }`）
- `ScheduledTasks.js`：日結應下班
- SPEC／LOG 對齊範例：07:53＋17:10 → 早退 20 分
