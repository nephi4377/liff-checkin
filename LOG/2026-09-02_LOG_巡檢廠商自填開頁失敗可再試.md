# 2026-09-02｜巡檢 P1：廠商自填開頁失敗可再試

## Diff／目的

廠商用 LINE 開資料登記頁，第二次讀資料失敗時只把載入文字改成錯誤、沒有再試。人以為頁面壞了。

## 技術

- `vendor_register.html`：讀取自動再試一次；失敗若登入已帶回舊資料就先填表；否則人話＋再試（不把失敗當成「還沒填過」）

## 部署

巡檢 P1：只開分支，不上正式。

## 驗證

模擬 `vendor_register_get` 失敗：要看到再試，不可永遠轉圈；已有舊資料時表單要先填上。
