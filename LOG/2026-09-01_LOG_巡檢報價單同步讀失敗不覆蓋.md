# 2026-09-01 報價單解析器：讀舊資料失敗不覆蓋驗收進度

## Diff／目的

設計師把解析結果按「同步至雲端」時，舊程式若讀不到此案既有雲端資料，會當成沒有舊筆、整案寫成進度 0%，還跳出同步成功。已記錄的驗收會被蓋掉。改成讀失敗就停、人話說明、不寫入；按鈕可再按。

## 技術

- `tools/BudgetWeb_Standalone.html`：讀 `quotations/{案號}` 失敗則中止寫入；狀態列說明「沒寫上去」；本機 `?mock=syncreadfail`
- SPEC：`12_報價單審核與主控台整合規格書.md` §2.3 路徑 A、v2.8

未改商業規則、未改 GAS、未改權限。雲端無法跑 `upload.bat`；正式站需合進 `main`。

## 驗證

- 本機 `tools/BudgetWeb_Standalone.html?mock=syncreadfail`：按同步，狀態列要看到「沒寫上去」；`window.__budgetWebDidWrite` 仍為 false
- 真有舊資料且讀得到時，行為不變（沿用舊進度／金額）
