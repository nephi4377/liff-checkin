# 2026-08-30 巡檢（客人）｜綁定送出失敗不停在處理中

## 目的（人話）

客人從 LINE 申請綁定，網路不穩時按「送出綁定」不再停在「處理中，請稍候…」。要告訴人「這次沒送到、可再按」，不是假裝還在跑。

從 LINE「我的案場」進來、身分還沒確認到時，選材區要人話＋再試，不要只留「載入失敗」。

## 改了哪些檔

- `shared/js/customer_finance_ui.js`：共用「連線失敗改人話」
- `modules/accounting/customer-finance-portal.html`：綁定送出捕捉失敗；選材開頁失敗可再試
- `SPEC/21.案件毛利擴充與薪資連動.MD`（B5.7）
- `TOS/巡檢回報.md`

未改 GAS、未改商業規則（對上才綁定的條件不變）。

## 本機怎麼看

頁面：`modules/accounting/customer-finance-portal.html`（僅 `127.0.0.1`／`localhost` 吃 `mock`）

| 模擬 | 網址 |
|------|------|
| 綁定送出連線失敗 | `?bind=1&mock=bindfail` → 填任何案號／地址／姓名後按送出 |
| 選材開頁失敗 | `?entity=material&mock=matbootfail` |

## 部署

雲端無法跑 `upload.bat`、不能自行合 `main`。合併後才算正式上線。
