## 2026-07-24 — 權限 2 人員出席燈號誤顯紅燈

- **症狀**：權限 2 開主控台「人員出席」，已上班的人仍顯示紅燈（未打卡）。
- **根因**：燈號背景更新只對權限 4+ 啟動；權限 2 拿不到打卡燈號資料時，前端預設當成未打卡顯示紅燈。核心 API 雖有回傳燈號，前端也未套用。
- **修正**：
  - 權限 2+ 同樣抓／輪詢今日燈號
  - 載入核心資料時一併套用今日燈號
  - 尚無燈號資料且非更新中 → 不顯示燈號（避免誤判）

---

## 2026-07-24 — 會計選單權限 2 被誤擋 ≥4

### Diff／目的

權限 2（例：添心設計-台南店）開「會計系統」顯示「需財務／老闆，權限 ≥ 4」。選單規格本為 ≥2 日常、≥4 財務，但 `initSession` 硬性檢查 ≥4。

### 技術

| 區 | 檔案 |
|----|------|
| 登入 | `shared/js/accounting_api.js`（`initSession` 依 `minPermission`） |
| 啟動 | `shared/js/accounting_boot.js`（把门檻傳入登入） |
| 快取破除 | 會計／相關頁 `accounting_api.js?v=42`、`accounting_boot.js?v=6` |
| SPEC | `15_會計系統模組規格書.md` v1.10、`19_HUB與會計全域身分傳承.md` |

### 部署

- 目標：CODING 前端（`upload.bat`）；**無需** backend／accounting-gas（後端門檻本來就對）
- 同批一併上線：人員出席燈號（權限 2+）、FB 發文工作室 SPEC 狀態註記

### 驗證（待測）

1. 權限 2：HUB「會計系統」→ 見日常款項（收支／待付款／追加減），**不見**財務管理區
2. 權限 2：可開收支登錄；開廠商名冊／請款審核仍應擋 ≥4
3. 權限 2：主控台人員出席燈號不再整排紅燈
