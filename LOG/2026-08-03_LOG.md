## 2026-08-03｜出勤燈號與部分會計入口改權限 3+

- **目的**：人員出勤燈號（右側今日燈號、全員出勤燈號看板）由權限 4+ 改為 **權限 ≥ 3** 可見；廠商名冊、薪資歷史補登、單據附件、案件毛利同步改為 ≥ 3。
- **前端**：
  - `spa/app.js` 新增 `canViewStaffStatusBoard`（`permission >= 3`）；右側今日燈號同門檻。
  - `spa/Dashboard.js` 看板連結與功能卡同門檻；其餘管理功能仍 `hasAdminRights`（≥4）。
  - 會計選單：上述四入口 ≥ 3；請款審核／待匯款等仍 ≥ 4。
  - 看板頁與說明頁文案更新「權限 3+」。
- **後端**：
  - CheckinSystem `get_hub_staff_board`：門檻改 ≥ 3。
  - accounting-gas：新增 `SUPERVISOR_MIN_PERM_ = 3`；廠商／附件 entity、案件毛利、薪資歷史補登改用 ≥ 3（其餘財務仍 ≥ 4）。
- **部署**：
  - CODING：`426f5df`（`upload.bat` 20260803_1237）
  - Backend：`313211e`（`upload.bat`；CheckinSystem 走 Actions）
  - accounting-gas：clasp deploy **@254**
- **線上 PR**：#7／#9／#10 草稿已關閉（內容併入本機）。

## 2026-08-03｜會計選單升權後仍看不到（身分快取）

- **現象**：行政權限已改為 3，主控台右側燈號可見，但會計「財務／主管」區塊仍無。
- **原因**：會計沿用本機舊身分快取（最多約 12 小時）與後端員工快取；背景重驗後未重畫選單。
- **修正**：
  - HUB 帶入權限與快取不同時強制重驗；選單取 HUB／驗證較高權限；重驗後重套選單。
  - accounting-gas 登入驗證前清員工權限快取，改讀試算表最新權限。
- **待部署**：CODING `v26.08.03.2`＋accounting-gas（本段改動尚未上線）。
