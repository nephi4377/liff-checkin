## 2026-08-03 — 全員出勤燈號改權限 3+ 可見

- **目的**：人員出勤燈號（右側「今日出勤燈號」、主控台「全員出勤燈號看板」入口）由權限 4+ 改為 **權限 ≥ 3** 可見。
- **前端**：
  - `spa/app.js` 新增 `canViewStaffStatusBoard`（`permission >= 3`）；側欄改以此門檻顯示。
  - `spa/Dashboard.js` 看板連結／功能卡同門檻；其餘管理功能仍用 `hasAdminRights`（≥4）。
  - 看板頁與說明頁文案改「權限 3+」。
- **後端注意**：看板資料 API `get_hub_staff_board` 仍由 CheckinSystem 驗權；若線上仍鎖 ≥4，權限 3 開頁會「權限不足」—需同步改後端門檻後再部署 GAS（本輪環境無 backend 倉庫）。
