# 2026-08-13｜會計從主控台進：沿用身分、關掉測試模式

## 人會看到什麼

- 從主控台點會計：頂列變 `姓名 · 權限 N · 主控台`，不再寫「測試模式（無登入）」
- 開著主控台超過一小時再進會計：不該再因通行證過期白屏
- 直接開會計網址：仍要自己用 LINE 登入

## 做了什麼

- 從主控台進來：用本分頁記住的人，**不再向主控台借通行證**
- 後端用編號查員工表（權限以表為準）
- 後端短暫失敗會自動多試幾次（減少「重整才好」）
- SPEC `19` v1.7 補失敗分級（FAIL vs 全失敗寄信）
- 每次 API／載入失敗：寫 Logs Explorer（不寄信、不跳出複製）
- 畫面「無法繼續」：Logs Explorer＋自動寄信＋複製錯誤
- 快取：`accounting_api.js?v=51`、`accounting_ui.js?v=20`

## 部署

- 後端 accounting-gas Web App `@274`（commit `81ef598`）
- 關閉測試模式請在 GAS 編輯器執行一次 `setAccountingAuthBypassOnce(false)`
