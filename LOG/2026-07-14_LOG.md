# 2026-07-14 LOG（CODING）

## LayoutPlanner — 報價單整理、手動編輯、分類圖層

### Diff／目的

- 報價單：同類櫃體不再併成一列；副屬性（如二抽）固定緊跟該櫃體；副屬性列視覺縮排
- 報價單：新增「編輯報價」— 可改項目／數量／總價／備註、隱藏列、還原自動計算；覆寫隨佈局儲存
- 畫布：依 Sheet 群組（水電、櫃體、木作…）分類圖層開關；全開／全關

### 技術

| 區 | 檔案 |
|----|------|
| 前端 | `modules/InteriorDesigned/LP_LayoutPlanner.js`、`LP_LayoutPlanner.html` |
| SPEC | `SPEC/04_互動式室內設計規劃工具規格書.md` v2.1；`專有名詞白話對照.md` |
| JSON | 新增 `quotationManualOverrides`、`layerVisibility` |

### 備份／部署

- 腳本：`CODING/upload.bat`（`NONINTERACTIVE=1`）
- 備份：（部署後補 `../BAK/CODING_*`）
- 結果：（部署後補 commit）

### 驗證（待測）

1. 開 LayoutPlanner：放多個同類櫃體＋二抽 → 預算明細副屬性在各自櫃下
2. 編輯報價 → 改總價 → 列印／CSV 反映手動值
3. 分類圖層取消「櫃體」→ 櫃體消失、牆壁仍可獨立顯示
4. 存佈局 JSON → 重載後圖層開關與報價覆寫仍在
