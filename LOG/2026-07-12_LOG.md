# 2026-07-12 部署 — CODING（會計入口整合 + bootstrap SWR）

## Diff／目的

1. **HUB**：單一「會計系統」卡片（≥2）；移除重複收支／請款卡片
2. **會計選單**：日常款項（≥2）與財務管理（≥4）分區；`initLiff` 取代 `initSession`
3. **快取**：`AccountingCache.load` 加入 **24h SWR**；空廠商快取強制重抓
4. **權限**：追加減與收款（`designer-customer-finance`）前端門檻 **≥2**

## 技術

| 區 | 檔案 |
|----|------|
| HUB | `spa/Dashboard.js`、`spa/app.js` |
| 選單／頁 | `modules/accounting/index.html`、`payment_request.html`、`accounting_ingest.html`、`designer-customer-finance.html` |
| API／快取 | `accounting_api.js` v28、`accounting_cache.js` v26、`accounting_master_data.js` |
| SPEC | `15_會計系統模組規格書.md` v1.9、`18_會計與主檔快取策略.md` v1.3 |

## 備份／部署

- 腳本：`CODING/upload.bat`（`NONINTERACTIVE=1`）
- 備份：`../BAK/CODING_20260712_1847_TX34`
- 結果：**已 push** `7e01edc`（v26.07.01.1／20260712_1847）
- 後端：`80c63fe`；accounting-gas clasp **@151**

## 驗證（待測）

1. 權限 2：HUB 見「會計系統」→ 選單有日常款項四項
2. 收支登錄：廠商 autocomplete 有資料
3. 快取逾 24h：頁面先顯示、背景再打 bootstrap（Network）

---

## 2026-07-12 追加 — 使用教學 + 待付款申請 UX

### Diff／目的

1. **使用教學**：頂部按鈕醒目化；主控台移除重複說明卡片與橫幅；流程圖 SVG＋會計總覽響應式（寬螢幕兩排）
2. **文案**：「待請款」正名「待付款申請」；LINE 只傳圖路線改為網頁為主（對齊 2026-07-11 政策）
3. **待付款申請**：廠商選擇改 **①工項分類 → ②下拉選廠商**；保留手動搜尋／新建廠商
4. **Backlog**：`未來開發事項.md` 記錄 LINE 傳圖→廠商夾＋快審（未實作）

### 技術

| 區 | 檔案 |
|----|------|
| HUB／教學 | `spa/app.js`、`spa/Dashboard.js`、`modules/help/*`、`assets/help/**/*.svg` |
| 請款表單 | `modules/accounting/payment_request.html` |
| 工具 | `tools/generate-help-flowcharts.py` |
| SPEC | `未來開發事項.md` |

### 備份／部署

- 腳本：`CODING/upload.bat`（`NONINTERACTIVE=1`）
- 後端（僅 SPEC／註解）：`backend/upload.bat` — LINE 傳圖未來項交叉引用

### 驗證（待測）

1. 主控台頂部「📖 使用教學」；會計總覽流程手機單排／桌機兩排
2. 待付款申請：選分類後下拉選廠商；手動搜尋／新建仍可用
3. 教學頁待付款申請不再寫「#請款＋傳圖自動建單」

---

## 2026-07-12 追加 — 使用教學示意圖 + 收支分開說明

### Diff／目的

1. **收支登錄說明**：收入（A）與支出（B）分開段落；LINE／網頁各自欄位表
2. **流程圖載入**：移除與 HTML 流程重複的 SVG 載入框；`help.js` 載入失敗時自動隱藏
3. **示意圖**：`generate-help-mockups.py` 產出 42 張 SVG（會計／薪資／請款／出勤／案場／設計／報價）
4. **文案**：「會計功能」「主控台 HUB」統一為「會計系統」

### 技術

| 區 | 檔案 |
|----|------|
| 教學頁 | `modules/help/*.html`、`modules/help/help.js` |
| 資產 | `assets/help/**/*.svg` |
| 工具 | `tools/generate-help-mockups.py`、`tools/generate-help-flowcharts.py` |
| SPEC | `15_會計系統模組規格書.md` v1.9 補使用教學 |

### 備份／部署

- 腳本：`CODING/upload.bat`（`NONINTERACTIVE=1`）
- 備份：`../BAK/CODING_20260712_2028_TX34`
- 結果：**已 push** `e062beb`（v26.07.01.1／20260712_2028）
- 範圍：僅 CODING 前端；backend 無變更

### 驗證（待測）

1. `#/help/payroll` 總流程無「載入流程圖中…」虛線框
2. `#/help/accounting-ingest` 收入／支出分頁可跳轉；示意圖正常顯示
3. 薪資／待付款申請各子頁示意圖載入（非虛線佔位）

---

## 2026-07-12 追加 — HUB→會計 LIFF 修復 + 啟動追蹤

### Diff／目的

1. **HUB→會計**：iframe 向父層要官方 LINE token；後端驗證改支援 Channel 2007974938 + 2010424425
2. **除錯即視感**：狀態紀錄自動計時；API 慢速／bootstrap 寫入右側面板；載入畫面副標
3. **debug 模式**：網址 `?debug=1` 或 `#/accounting?debug=1`（父頁 `?debug=1` 會轉給 iframe）

### 技術

| 區 | 檔案 |
|----|------|
| 前端 | `spa/app.js`、`accounting_api.js` v35、`accounting_ui.js` v12、`accounting_boot.js` v5、`accounting_cache.js` v27 |
| 後端 | `accounting-gas/LiffAuth.js`、`config_.js`（`getOfficialLineChannelId`） |
| 入口 | `index.html` v26.07.12.1、`modules/accounting/index.html` |

### 備份／部署

- CODING：`upload.bat`（`NONINTERACTIVE=1`）
- accounting-gas：`deploy.bat` + backend `upload.bat`

### 驗證（待測）

1. LINE → HUB → 會計系統：不再「LIFF 驗證失敗」
2. 右側狀態紀錄見「啟動頁面 · N 秒」「主檔快取 · 直接顯示」
3. `#/accounting?debug=1`：每支 API 都有紀錄
