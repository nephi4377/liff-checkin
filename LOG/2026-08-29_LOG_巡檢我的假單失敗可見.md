# 2026-08-29 線上假勤申請：我的假單失敗要看得到

## Diff／目的

行政打開「我的假單」時，程式會先換掉列表節點再去後端要資料。失敗時把說明寫回舊節點，畫面變成空白，也沒有再試。

## 技術

- `modules/attendance/leave_request.html`：失敗寫到畫面上的區塊；人話＋再試；本機可用 `mock=fail`／`mock=empty`

未改商業規則。不上正式。

## 驗證

- 本機開假勤申請 →「我的假單」`mock=fail`：紅字＋再試，不是空白
- `mock=empty`：才顯示目前沒有假單
