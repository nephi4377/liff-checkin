# 2026-07-25｜案件毛利：寫入後整包回覆（不再重抓）

## 目標

新增／修改／刪除明細成功後，後端直接回「整包詳情」；前端有整包就刷新畫面，不再多打一次「開詳情」。

## 變更（本機，待部署）

### 後端 ccounting-gas/master/MarginModule.js

- 新增／改／刪成功後改回整包詳情（與開詳情同形狀）
- 寫入後組整包前清請款 cache，結束再清（同請求內仍共用）
- 收入走請款審核：維持原訊息；能附整包就附，附不上前端才重抓

### 前端 project_margin.html

- 新增／改／刪成功：有整包就直接刷新；否則才重開詳情

## 驗收（部署後）

1. 新增／改／刪支出成功：Network 不應再多一次開詳情
2. 明細仍只含該案號
3. 既有精簡 UI 不變

## 部署

- 待使用者指示：ccounting-gas/deploy.bat ＋ CODING/upload.bat
