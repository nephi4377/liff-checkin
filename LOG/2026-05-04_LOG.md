# 開發日誌

## 2026-05-04（LandingPage 自管圖）

- **Diff**：`modules/info/LandingPage.html` 首圖、Why 三列交錯圖、中段橫帶、精選影片卡片縮圖改為 `./asset/` 實拍；`og:image`／`twitter:image`／JSON-LD `LocalBusiness.image` 改為 `info.tanxin.space` 上同一張首圖之絕對網址（path 百分號編碼）；移除 YouTube 縮圖 preload／dns-prefetch；首圖 onerror 備援 `./asset/2_0.jpg`。
- **Verify**：本機路徑檔名與 `asset` 目錄列表一致；部署後應以瀏覽器確認圖片與分享預覽。

## 2026-05-04（精選區手機寬度＋貓咪小幫手）

- **Diff**：精選影片橫滑改 `w-[min(76vw,260px)]`、區塊 `overflow-y-hidden`、負邊距略縮；桌機 `scroll-snap-align: unset`。新增固定層貓咪 SVG＋氣泡文案，定時隨機 `left`/`top`（避開手機底欄），點擊同官方 LINE；`prefers-reduced-motion` 時固定右下不徘徊。
- **Verify**：手機直向看影片卡不再過寬；貓可點且不干擾底欄為佳。

## 2026-05-04（貓：僅可視區＋整貓 SVG）

- **Diff**：貓改 `left:0;top:0` + `transform: translate3d` 移動（不動文件捲動）；可視範圍用 `visualViewport` 與氣泡高度預留；`clamp` 在 resize／viewport 變化時執行。移除圓形底，改側身坐姿全身向量圖。
- **Verify**：捲動頁面時瀏覽器不應自動捲到貓；貓應全程留在螢幕可見區。

## 2026-05-04（移除貓咪小幫手）

- **Diff**：依使用者回饋移除 `LandingPage.html` 內貓 SVG、相關 CSS 與腳本；底欄 LINE 按鈕保留。

## 2026-05-04（換圖＋輕量互動）

- **Diff**：首圖與 og／twitter／JSON-LD 改 `2025-03-06客廳 (7).jpg`；Why／橫帶／精選卡改組合（含長虹玻璃、更衣、廚房後場等）。新增頂部閱讀進度條、`.reveal-section` 捲入淡入、`prefers-reduced-motion` 時跳過動畫；服務與亮點間手繪感曲線分隔 SVG。
- **Verify**：部署後檢查圖檔路徑與首屏區塊勿空白過久。

## 2026-05-04（首圖加高換圖、桌機隱藏中段橫帶）

- **Diff**：首圖改 `主臥室.jpg`，區塊高度提高（`.hero-banner-img` min-height 與 `h-[min(vh,rem)]` 加碼）；`og`／preload／JSON-LD 圖同檔。中段全寬圖帶加 `md:hidden`，僅小於 `md` 可見。

## 2026-05-04（精選作品卡縮圖改回 YT）

- **Diff**：六張精選卡 `img` 改 `i.ytimg.com/.../hqdefault.jpg`，與各卡 `watch?v=` 對齊；補 `dns-prefetch` i.ytimg.com。

## 2026-05-04（首屏五張輪播＋下移）

- **Diff**：首圖改 `#hero-slide-root` 內五張 `asset` 疊圖；`@keyframes hero-pan-down` 緩移；每 8 秒換一張＋字幕 `data-hero-caption`；`prefers-reduced-motion` 僅顯示第一張、停用動畫。

## 2026-05-04（首屏輪播改橫向場景）

- **Diff**：輪播去掉主臥、單張廚房、穿堂長虹（易像直幅）；改為新案客廳、客廳玄關、客廳、客廳(2)、工作區。`og`／preload／JSON-LD 首圖改 `2025-03-06客廳 (7).jpg`。

## 2026-05-04（首屏輪播 8s→6s）

- **Diff**：`--hero-pan-duration` 與換圖 interval 改 6 秒。

## 2026-05-04（首屏換圖交叉淡化）

- **Diff**：`.hero-slide-img` 增加 `opacity` transition；換圖時新張 `z-index:2` 疊在上同步淡入、舊張淡出；`prefers-reduced-motion` 關閉 transition。

## 2026-05-04（輪播移除客廳-玄關）

- **Diff**：直幅 `客廳-玄關.jpg` 不利 Ken Burns 平移，改為 `客廳 (3).jpg`，字幕「客廳・開放面｜實景收錄」。

## 2026-05-04（輪播去同案客廳三連）

- **Diff**：首屏輪播移除 `客廳.jpg`／`客廳 (2).jpg`／`客廳 (3).jpg`；保留 `2025-03-06客廳 (7).jpg` 為該系列代表，改插入 `廚房 (2).jpg`、`小女兒房.jpg`、`74A06726.jpg`。

## 2026-05-04（74A06726 字幕）

- **Diff**：`74A06726.jpg` 輪播字幕改為「客廳・美式風格｜實景收錄」（使用者確認為美式客廳）。

## 2026-05-04（首屏輪播 6s 平移＋1s 停格）

- **Diff**：`heroSlideshow` 換圖間隔 `6000`→`7000` ms；Ken Burns 仍 `--hero-pan-duration:6s`，多出的 1s 為動畫結束後停格再交叉淡化。HTML 註解說明。

## 2026-05-04（專案工作區：複製邀請頁網址）

- **Diff**：`managementconsole.html` 頂列（≤1024px）增「邀請頁網址」；右欄專案基本資料增「複製好友推薦邀請頁網址」。`main.js` 複製 `InviteSheet.html` canonical URL；`style.css` 頂列按鈕包在 `.console-header-actions`。**Tech**：`main.js?v=26.05.04.1`。

## 2026-05-04（改置整合主控台首頁）

- **Diff**：同仁較少開專案工作區，改在 `spa/Dashboard.js`（整合主控台 `#/dashboard`）「開啟專案工作區」下方增綠色條「好友推薦邀請頁」＋**複製網址**（`InviteSheet.html` canonical）。撤除專案工作區頂列／右欄之邀請頁按鈕與 `main.js` 綁定；`app.js` 以 `Dashboard.js?v=26.05.04.2` 促快取更新；`main.js?v=26.05.04.2`。

## 2026-05-04（主控台複製改為 LandingPage）

- **Diff**：主控台綠色條改複製 `LandingPage.html`（公開落地頁），文案「公開落地頁」；禮券 `InviteSheet.html` 不由此擴散、日後另覓入口。`Dashboard.js?v=26.05.04.3`。
