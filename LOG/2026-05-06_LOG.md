# 開發日誌 2026-05-06

## 2026-05-06（時刻依本機存檔）

**Diff 目的與追蹤**  
- 修正出勤儀表板 `attendance_report.html` 先前檢視所列問題：API 參數、快取解析、地圖載入、樂觀更新排序與姓名、CSV／報表防呆、XSS 跳脫、補打卡驗證等。

**技術**  
- 修改檔案：`modules/attendance/attendance_report.html`  
- 邏輯：`fetchApi` 僅附加一次 `source`；`loadCache` JSON 解析與結構防呆；Google Maps 以唯一 `callback` 解析並還原 `gm_authFailure`；`showPicker` 能力偵測；樂觀更新依「同員工＋日期升冪」插入；補打卡檢查員工與 `success === false`；報表／匯出使用 `mapPoints` 陣列防呆與 `csvEscapeField`；`renderSummary` 空 summary 防呆；錯誤訊息與動態 HTML 使用 `escapeHtml`／員工選單 `escapeAttr`；匯出後 `URL.revokeObjectURL`；移除無效的 date/time `:out-of-range` 樣式註解行為。

**Verify**  
- 靜態檢視：無語法斷痕；需在瀏覽器實際查詢、補打卡、匯出 CSV、選單員工含特殊字元時驗證（本機尚未於此自動跑瀏覽器）。

---

## Cursor 規則（除錯循環）

**Diff 目的與追蹤**  
- 新增全域規則：使用者以「除錯循環:」開頭提問時，助理依測試／分析／修復／再測試循環執行，並固定四段輸出標題。

**技術**  
- 新增：`/.cursor/rules/debug-cycle.mdc`（`alwaysApply: true`）。

**Verify**  
- 檔案已寫入；其他工作區若要相同行為需複製該檔或於各倉庫各自建立規則。
